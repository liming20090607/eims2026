# 网页无法访问 - 完整排查与修复指南

## 📋 问题现象

**症状：**
- 浏览器无法打开 http://39.106.41.239:8000/
- 页面加载超时或连接被拒绝
- 看到端口 22（SSH）而不是 8000

---

## 🚀 快速修复（推荐）

### 方案一：使用自动修复脚本

#### 步骤 1：SSH 登录服务器

```bash
ssh admin@39.106.41.239
```

输入您找回的 admin 密码登录。

#### 步骤 2：上传修复脚本

在本地 PowerShell 执行：
```powershell
scp E:\EIMS2026\bat\修复网页访问.sh admin@39.106.41.239:/tmp/
```

#### 步骤 3：在服务器上执行修复

SSH 登录后执行：
```bash
sudo su -
bash /tmp/修复网页访问.sh
```

**脚本会自动完成：**
- ✅ 启动 Supervisor 服务
- ✅ 启动 Gunicorn（Django）
- ✅ 配置防火墙规则
- ✅ 测试本地访问
- ✅ 检查服务状态

---

### 方案二：手动修复

#### Step 1：检查并启动 Supervisor

```bash
# 检查状态
sudo systemctl status supervisord

# 如果未运行，启动
sudo systemctl start supervisord

# 设置开机自启
sudo systemctl enable supervisord
```

#### Step 2：启动 Gunicorn

```bash
# 查看 Supervisor 中的服务
sudo supervisorctl status

# 启动 EIMS 服务
sudo supervisorctl start eims

# 如果服务不存在，需要创建配置
sudo nano /etc/supervisord.d/eims.ini
```

**Supervisor 配置内容：**
```ini
[program:eims]
command=/var/www/eims/venv/bin/gunicorn --workers 3 --bind 0.0.0.0:8000 eims.wsgi:application
directory=/var/www/eims
user=admin
autostart=true
autorestart=true
stderr_logfile=/var/log/eims/error.log
stdout_logfile=/var/log/eims/access.log
```

**然后执行：**
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start eims
```

#### Step 3：配置防火墙

```bash
# 检查防火墙状态
sudo firewall-cmd --state

# 如果防火墙开启，添加 8000 端口
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload

# 验证
sudo firewall-cmd --list-ports
```

#### Step 4：测试本地访问

```bash
# 使用 curl 测试
curl http://localhost:8000/admin/

# 或使用浏览器测试（在服务器上）
# 如果有图形界面
```

#### Step 5：检查端口监听

```bash
# 查看 8000 端口
sudo netstat -tlnp | grep 8000

# 或使用 ss
sudo ss -tlnp | grep 8000
```

**正常输出：**
```
tcp        0      0 0.0.0.0:8000    0.0.0.0:*    LISTEN      1234/python
```

---

## 🛡️ 阿里云安全组配置（必须）

**这是最常见的问题原因！**

### 配置步骤

1. **登录阿里云控制台**
   - 访问：https://ecs.console.aliyun.com/

2. **找到您的实例**
   - 搜索 IP：`39.106.41.239`
   - 点击实例 ID 进入详情页

3. **配置安全组**
   - 点击 **安全组** 标签
   - 点击 **配置规则**
   - 选择 **入方向**

4. **添加规则**
   - 点击 **手动添加**
   - 填写以下信息：
     ```
     优先级：1
     策略：允许
     协议类型：TCP
     端口范围：8000/8000
     授权对象：0.0.0.0/0
     描述：Django 应用端口
     ```
   - 点击 **保存**

5. **验证**
   - 确认规则已添加成功
   - 状态为 **生效**

### 安全组规则示例

| 端口范围 | 协议 | 授权对象 | 优先级 | 策略 | 说明 |
|---------|------|---------|--------|------|------|
| 8000/8000 | TCP | 0.0.0.0/0 | 1 | 允许 | Django 应用 |
| 22/22 | TCP | 0.0.0.0/0 | 1 | 允许 | SSH（已有） |

---

## 🔍 详细排查步骤

### 使用诊断脚本

#### 上传诊断脚本

```powershell
scp E:\EIMS2026\bat\诊断网页访问问题.sh admin@39.106.41.239:/tmp/
```

#### 执行诊断

```bash
ssh admin@39.106.41.239
sudo bash /tmp/诊断\ 网页访问问题.sh
```

**脚本会检查：**
- ✅ Gunicorn 进程状态
- ✅ 端口 8000 监听状态
- ✅ Supervisor 服务状态
- ✅ 防火墙配置
- ✅ Django 配置文件
- ✅ 虚拟环境
- ✅ 错误日志
- ✅ 磁盘空间
- ✅ 内存使用
- ✅ 本地访问测试

---

## 📊 常见问题诊断表

| 问题 | 检查方法 | 解决方案 |
|------|---------|---------|
| **Gunicorn 未运行** | `ps aux \| grep gunicorn` | `supervisorctl start eims` |
| **端口 8000 未监听** | `netstat -tlnp \| grep 8000` | 启动 Gunicorn |
| **Supervisor 未运行** | `systemctl status supervisord` | `systemctl start supervisord` |
| **防火墙阻止** | `firewall-cmd --state` | 开放 8000 端口 |
| **安全组未配置** | 阿里云控制台检查 | 添加入方向规则 |
| **Django 配置错误** | 查看 settings.py | 配置 ALLOWED_HOSTS |
| **虚拟环境问题** | `source venv/bin/activate` | 重新激活环境 |
| **日志错误** | `tail -f /var/log/eims/error.log` | 根据错误修复 |

---

## 🔧 高级故障排查

### 1. 查看 Gunicorn 日志

```bash
# 实时查看错误日志
sudo tail -f /var/log/eims/error.log

# 查看最后 50 行
sudo tail -n 50 /var/log/eims/error.log

# 查看 Supervisor 日志
sudo tail -f /var/log/supervisor/eims.err.log
```

### 2. 测试 Django 配置

```bash
cd /var/www/eims
source venv/bin/activate

# 测试 settings.py
python -c "import settings; print('OK')"

# 测试数据库连接
python manage.py check

# 测试 WSGI 应用
python -c "from eims.wsgi import application; print('WSGI OK')"
```

### 3. 检查端口占用

```bash
# 查看谁在使用 8000 端口
sudo lsof -i :8000

# 或使用 netstat
sudo netstat -tlnp | grep 8000
```

### 4. 手动启动 Django 测试

```bash
cd /var/www/eims
source venv/bin/activate

# 直接运行 Django 开发服务器测试
python manage.py runserver 0.0.0.0:8000

# 如果成功，Ctrl+C 停止，然后用 Supervisor 启动
```

---

## ✅ 验证修复

### 测试 1：本地 curl 测试

```bash
curl -I http://localhost:8000/admin/
```

**预期输出：**
```
HTTP/1.1 200 OK
或
HTTP/1.1 302 Found
```

### 测试 2：远程访问测试

在本地 PowerShell 执行：
```powershell
Test-NetConnection 39.106.41.239 -Port 8000
```

**预期输出：**
```
TcpTestSucceeded : True
```

### 测试 3：浏览器访问

打开浏览器访问：
```
http://39.106.41.239:8000/admin/
```

**预期：** 看到 Django Admin 登录页面

### 测试 4：检查进程

```bash
# 查看 Gunicorn 进程
ps aux | grep gunicorn

# 应该看到 1 个 master + 多个 worker
```

---

## 📝 检查清单

修复完成后，请确认：

- [ ] Supervisor 服务正在运行
- [ ] Gunicorn 进程正在运行（至少 3 个 worker）
- [ ] 端口 8000 正在监听（0.0.0.0:8000）
- [ ] 防火墙已开放 8000 端口
- [ ] 阿里云安全组已配置 8000 端口
- [ ] 本地 curl 测试成功
- [ ] 远程端口测试成功
- [ ] 浏览器可以访问

---

## 🔍 仍然无法访问？

### 如果以上方法都无效

#### 1. 检查网络路由

```bash
# 在本地执行
tracert 39.106.41.239

# 或在 Linux/Mac
traceroute 39.106.41.239
```

#### 2. 检查服务器网络

```bash
# SSH 登录后
ip addr show
ping -c 4 www.baidu.com
```

#### 3. 重启所有服务

```bash
sudo systemctl restart supervisord
sudo supervisorctl restart all
```

#### 4. 重启服务器

```bash
sudo reboot
```

#### 5. 检查系统资源

```bash
# 磁盘空间
df -h

# 内存
free -h

# CPU 负载
top
```

---

## 📞 需要帮助？

如果问题仍未解决，请提供以下信息：

1. **诊断脚本输出**
   ```bash
   sudo bash /tmp/诊断网页访问问题.sh
   ```

2. **Gunicorn 进程列表**
   ```bash
   ps aux | grep gunicorn
   ```

3. **端口监听状态**
   ```bash
   netstat -tlnp | grep 8000
   ```

4. **错误日志**
   ```bash
   tail -n 50 /var/log/eims/error.log
   ```

5. **Supervisor 状态**
   ```bash
   supervisorctl status eims
   ```

6. **安全组配置截图**
   - 阿里云控制台 → 安全组 → 入方向规则

7. **测试结果**
   ```powershell
   Test-NetConnection 39.106.41.239 -Port 8000
   ```

---

## 🎯 预防措施

### 1. 设置开机自启

```bash
sudo systemctl enable supervisord
```

### 2. 配置日志轮转

```bash
sudo nano /etc/logrotate.d/eims
```

内容：
```
/var/log/eims/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0640 admin admin
}
```

### 3. 监控服务状态

```bash
# 定期检查
sudo supervisorctl status

# 添加到 crontab
crontab -e
# 每 5 分钟检查一次
*/5 * * * * /usr/bin/supervisorctl status eims | grep -q RUNNING || /usr/bin/supervisorctl start eims
```

### 4. 备份配置

```bash
# 备份 Supervisor 配置
sudo cp /etc/supervisord.d/eims.ini /backup/

# 备份 Django 配置
sudo cp /var/www/eims/settings.py /backup/
```

---

**最后更新：** 2026-04-03  
**适用环境：** Django 4.2.7, Gunicorn, Supervisor, Alibaba Cloud Linux  
**服务器 IP：** 39.106.41.239
