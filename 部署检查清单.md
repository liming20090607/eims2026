# EIMS 阿里云部署快速检查清单

## 📋 部署前准备

### 1. 本地准备
- [ ] 备份本地数据库 `db.sqlite3`
- [ ] 导出数据库数据（如果使用 SQLite）
- [ ] 准备 `.env` 文件模板
- [ ] 生成 Django SECRET_KEY（至少 50 字符）
- [ ] 准备强密码（数据库、管理员等）
- [ ] 测试代码在本地正常运行

### 2. 阿里云服务器
- [ ] 购买 ECS 实例（推荐 Ubuntu 22.04）
- [ ] 配置安全组（开放 80、443、22 端口）
- [ ] 获取公网 IP
- [ ] 准备 SSH 密钥或密码

---

## 🚀 服务器部署步骤

### 第 1 步：连接服务器
```bash
ssh root@你的服务器 IP
```

### 第 2 步：上传代码
- [ ] 使用 Git 克隆 或 SCP 上传 或 FTP 工具
- [ ] 上传到 `/var/www/eims/` 目录
- [ ] 验证文件完整性

### 第 3 步：安装系统依赖
```bash
sudo apt update
sudo apt install -y python3 python3-pip python3-venv git nginx mysql-server libmysqlclient-dev
```
- [ ] 系统更新完成
- [ ] Python3 安装成功
- [ ] MySQL 安装成功
- [ ] Nginx 安装成功

### 第 4 步：配置数据库
```bash
sudo mysql -u root -p
```
```sql
CREATE DATABASE eims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'eims_user'@'localhost' IDENTIFIED BY '强密码';
GRANT ALL PRIVILEGES ON eims.* TO 'eims_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```
- [ ] 数据库创建成功
- [ ] 用户创建成功
- [ ] 权限授予成功

### 第 5 步：配置 Python 环境
```bash
cd /var/www/eims
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
pip install gunicorn mysqlclient
```
- [ ] 虚拟环境创建成功
- [ ] 依赖安装成功
- [ ] 无报错信息

### 第 6 步：配置文件修改
- [ ] 修改 `settings.py` 或创建 `settings_production.py`
- [ ] 创建 `.env` 文件
- [ ] 设置 `DEBUG = False`
- [ ] 配置 `ALLOWED_HOSTS`
- [ ] 配置数据库连接信息
- [ ] 生成 SECRET_KEY

### 第 7 步：数据库迁移
```bash
source venv/bin/activate
python manage.py makemigrations
python manage.py migrate
python manage.py collectstatic --noinput
python manage.py createsuperuser
```
- [ ] 数据库表创建成功
- [ ] 静态文件收集成功
- [ ] 超级管理员创建成功

### 第 8 步：配置 Gunicorn
```bash
pip install gunicorn
```
测试运行：
```bash
cd /var/www/eims
gunicorn --workers 3 --bind 127.0.0.1:8000 wsgi:application
```
- [ ] Gunicorn 安装成功
- [ ] 可以正常启动

### 第 9 步：配置 Systemd 服务
```bash
sudo nano /etc/systemd/system/eims.service
```
添加配置（参考部署指南）
```bash
sudo systemctl daemon-reload
sudo systemctl start eims
sudo systemctl enable eims
sudo systemctl status eims
```
- [ ] 服务文件创建成功
- [ ] 服务启动成功
- [ ] 服务开机自启

### 第 10 步：配置 Nginx
```bash
sudo nano /etc/nginx/sites-available/eims
```
添加配置（参考部署指南）
```bash
sudo ln -s /etc/nginx/sites-available/eims /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl enable nginx
```
- [ ] Nginx 配置创建成功
- [ ] 配置测试通过
- [ ] Nginx 重启成功

### 第 11 步：配置防火墙
```bash
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
```
- [ ] 防火墙规则添加成功
- [ ] 防火墙启用成功

### 第 12 步：配置 SSL（可选但推荐）
```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名
```
- [ ] Certbot 安装成功
- [ ] SSL 证书获取成功
- [ ] 自动续期配置成功

---

## ✅ 部署后测试

### 基础功能测试
- [ ] 访问 http://你的域名 - 首页正常
- [ ] 访问 http://你的域名/admin - 后台登录页正常
- [ ] 使用管理员账号登录 - 成功
- [ ] 静态文件（CSS/JS）正常加载
- [ ] 媒体文件（上传的文件）正常访问
- [ ] 数据库连接正常
- [ ] 所有页面 404 错误正确处理

### 功能测试
- [ ] 人员花名册功能正常
- [ ] 部门角色配置功能正常
- [ ] 审批流程功能正常
- [ ] 文件上传功能正常
- [ ] 表单提交功能正常
- [ ] 搜索功能正常
- [ ] 分页功能正常

### 性能测试
- [ ] 页面加载速度 < 2 秒
- [ ] 并发访问测试（使用 ab 或 wrk）
- [ ] 数据库查询优化
- [ ] 静态文件压缩正常

### 安全测试
- [ ] HTTPS 正常访问
- [ ] CSRF 保护生效
- [ ] SQL 注入防护
- [ ] XSS 防护
- [ ] 敏感信息不泄露

---

## 🔧 监控和维护

### 日志查看
```bash
# 应用日志
sudo tail -f /var/log/gunicorn/error.log

# Nginx 日志
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# 系统日志
sudo journalctl -u eims -f
```
- [ ] 日志文件正常记录
- [ ] 无频繁错误

### 服务状态
```bash
systemctl status eims
systemctl status nginx
systemctl status mysql
```
- [ ] 所有服务运行正常
- [ ] 无重启循环

### 资源监控
```bash
htop  # CPU 和内存
df -h  # 磁盘空间
free -h  # 内存使用
```
- [ ] CPU 使用率正常
- [ ] 内存使用正常
- [ ] 磁盘空间充足

### 数据库备份
```bash
# 手动备份
mysqldump -u eims_user -p eims > backup_$(date +%Y%m%d).sql

# 配置自动备份（crontab）
crontab -e
# 添加：0 2 * * * /var/www/eims/backup_db.sh
```
- [ ] 备份脚本配置成功
- [ ] 定时任务添加成功
- [ ] 测试备份成功

---

## 🐛 常见问题排查

### 问题 1：502 Bad Gateway
```bash
# 检查 Gunicorn
sudo systemctl status eims
sudo tail -f /var/log/gunicorn/error.log

# 重启服务
sudo systemctl restart eims
sudo systemctl restart nginx
```

### 问题 2：静态文件 404
```bash
# 重新收集
source venv/bin/activate
python manage.py collectstatic --clear --noinput
sudo systemctl restart nginx
```

### 问题 3：数据库连接失败
```bash
# 检查 MySQL
sudo systemctl status mysql

# 测试连接
python manage.py dbshell
```

### 问题 4：权限问题
```bash
# 修复权限
sudo chown -R www-data:www-data /var/www/eims
sudo chmod -R 755 /var/www/eims
sudo chmod 644 /var/www/eims/.env
```

---

## 📞 重要提示

### 安全建议
1. ✅ 务必设置 `DEBUG = False`
2. ✅ 修改 `ALLOWED_HOSTS` 为实际域名
3. ✅ 使用强密码（数据库、管理员等）
4. ✅ 定期更新系统和依赖
5. ✅ 配置 HTTPS
6. ✅ 定期备份数据库
7. ✅ 监控日志文件
8. ✅ 限制文件上传大小和类型

### 性能优化
1. ✅ 使用 Redis 缓存（可选）
2. ✅ 配置 Nginx 缓存
3. ✅ 数据库查询优化
4. ✅ 静态文件 CDN（可选）
5. ✅ Gunicorn worker 数量优化

### 扩展建议
1. ✅ 使用负载均衡（高并发）
2. ✅ 配置数据库主从复制
3. ✅ 使用对象存储 OSS
4. ✅ 配置监控告警
5. ✅ 容器化部署（Docker）

---

## 🎉 部署完成！

**访问地址**：
- 前台：http://你的域名
- 后台：http://你的域名/admin
- API：http://你的域名/api/

**文档位置**：
- 部署指南：`部署指南_阿里云.md`
- 快速部署脚本：`deploy.sh`
- 生产环境配置：`settings_production.py`
- 数据库备份：`backup_db.sh`

**需要帮助？**
查看部署指南文档或访问官方文档：
- Django: https://docs.djangoproject.com/
- Gunicorn: https://docs.gunicorn.org/
- Nginx: https://nginx.org/
