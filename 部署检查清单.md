# EIMS 阿里云服务器部署检查清单 ✅

## 📋 部署前准备

### 1. 服务器信息
- [ ] 阿里云 ECS 实例已创建
- [ ] 服务器 IP 地址：____________________
- [ ] SSH 登录密码/密钥已准备
- [ ] 安全组规则已配置（端口 22, 80, 443）

### 2. 域名（可选）
- [ ] 域名已购买
- [ ] 域名已备案（中国大陆服务器必需）
- [ ] DNS 解析已配置到服务器 IP

---

## 🚀 部署流程

### 阶段 1：上传部署脚本到服务器

**方法 1：使用 SCP**
```bash
# 在本地 PowerShell 运行
scp deploy.sh root@你的服务器 IP:/root/
scp 阿里云部署指南.md root@你的服务器 IP:/root/
```

**方法 2：使用 Git**
```bash
# 在服务器上运行
cd /root
git clone https://gitee.com/liming20090607/eims2026.git
cd eims2026
```

**方法 3：手动创建**
- 在服务器上使用 vim 或 nano 创建文件
- 复制粘贴脚本内容

---

### 阶段 2：执行自动部署脚本

```bash
# SSH 登录服务器
ssh root@你的服务器 IP

# 给脚本执行权限
chmod +x deploy.sh

# 运行部署脚本
bash deploy.sh
```

**预计时间**：15-30 分钟（主要花费在编译 Python）

---

### 阶段 3：安装和配置 MySQL

```bash
# 安装 MySQL
yum install -y mysql-community-server

# 启动 MySQL
systemctl start mysqld
systemctl enable mysqld

# 获取临时密码
grep 'temporary password' /var/log/mysqld.log

# 登录 MySQL（替换临时密码）
mysql -u root -p

# 执行以下 SQL 命令
```

**SQL 命令**：
```sql
-- 修改 root 密码（替换 YourPassword）
ALTER USER 'root'@'localhost' IDENTIFIED BY 'YourPassword';

-- 创建数据库
CREATE DATABASE eims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'eims'@'localhost' IDENTIFIED BY 'EimsPassword2026';

-- 授权
GRANT ALL PRIVILEGES ON eims.* TO 'eims'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

---

### 阶段 4：修改项目配置

```bash
# 编辑 settings.py
vim /var/www/eims/settings.py
```

**修改内容**：

**1. 数据库配置**（约第 80 行）：
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'eims',
        'USER': 'eims',
        'PASSWORD': 'EimsPassword2026',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
            'charset': 'utf8mb4',
        },
    }
}
```

**2. 允许的主机**（约第 28 行）：
```python
ALLOWED_HOSTS = [
    '你的服务器 IP',
    'localhost',
    '127.0.0.1',
    '你的域名'  # 如果有
]
```

**3. 关闭 DEBUG**（约第 27 行）：
```python
DEBUG = False
```

**4. 静态文件根目录**（约第 140 行）：
```python
STATIC_ROOT = '/var/www/eims/staticfiles/'
```

**5. 媒体文件根目录**（约第 145 行）：
```python
MEDIA_ROOT = '/var/www/eims/media/'
```

---

### 阶段 5：数据库迁移

```bash
cd /var/www/eims
source venv/bin/activate

# 执行迁移
python manage.py makemigrations
python manage.py migrate

# 创建超级用户
python manage.py createsuperuser
# 输入：
# - 用户名：admin
# - 邮箱：（可选）
# - 密码：********

# 收集静态文件
python manage.py collectstatic --noinput

# 退出虚拟环境
deactivate
```

---

### 阶段 6：重启服务

```bash
# 重启 Gunicorn
supervisorctl restart eims

# 重启 Nginx
systemctl restart nginx

# 检查服务状态
supervisorctl status eims
systemctl status nginx
```

---

### 阶段 7：配置阿里云安全组

1. 登录阿里云控制台
2. 进入 ECS → 安全组
3. 添加入方向规则：
   - [ ] 端口 80，授权对象 0.0.0.0/0
   - [ ] 端口 443，授权对象 0.0.0.0/0（可选）
   - [ ] 端口 22，授权对象 你的 IP（建议限制）

---

### 阶段 8：验证部署

**1. 访问网站**
```
http://你的服务器 IP
```
- [ ] 首页正常显示
- [ ] 静态文件加载正常
- [ ] 无 500 错误

**2. 访问管理后台**
```
http://你的服务器 IP/admin
```
- [ ] 可以登录
- [ ] 后台功能正常

**3. 检查日志**
```bash
# Gunicorn 日志
tail -f /var/log/gunicorn/error.log

# Nginx 日志
tail -f /var/log/nginx/error.log

# Supervisor 日志
tail -f /var/log/gunicorn/supervisor.log
```

---

## 🔧 可选：配置 HTTPS

### 使用 Let's Encrypt 免费证书

```bash
# 安装 Certbot
yum install -y certbot python3-certbot-nginx

# 获取证书（替换为你的域名）
certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 自动续期测试
certbot renew --dry-run
```

**配置自动续期**：
```bash
# 编辑 crontab
crontab -e

# 添加每天凌晨 2 点检查续期
0 2 * * * certbot renew --quiet
```

---

## 📊 性能优化（可选）

### 1. 优化 Gunicorn

编辑 `/var/www/eims/gunicorn.conf.py`：
```python
workers = 4  # CPU 核心数 * 2 + 1
threads = 2
worker_connections = 1000
max_requests = 1000
max_requests_jitter = 50
```

### 2. 优化 Nginx

编辑 `/etc/nginx/nginx.conf`：
```nginx
worker_processes auto;
worker_connections 10240;

http {
    # 开启 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
    
    # 缓存静态文件
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### 3. 数据库优化

编辑 `/etc/my.cnf`：
```ini
[mysqld]
innodb_buffer_pool_size = 256M
innodb_log_file_size = 64M
max_connections = 200
query_cache_size = 32M
```

---

## 🔒 安全加固

### 1. 配置防火墙

```bash
# 安装 firewalld（如果未安装）
yum install -y firewalld

# 启动防火墙
systemctl start firewalld
systemctl enable firewalld

# 配置规则
firewall-cmd --permanent --add-service=ssh
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload

# 查看规则
firewall-cmd --list-all
```

### 2. 禁用 root SSH 登录

```bash
# 编辑 SSH 配置
vim /etc/ssh/sshd_config

# 修改：
PermitRootLogin no
PasswordAuthentication no  # 如果使用密钥登录

# 重启 SSH
systemctl restart sshd
```

### 3. 创建普通用户

```bash
# 创建用户
adduser eimsuser

# 设置密码
passwd eimsuser

# 添加 sudo 权限
usermod -aG wheel eimsuser

# 修改项目目录权限
chown -R eimsuser:eimsuser /var/www/eims
```

---

## 📝 日常维护

### 1. 查看服务状态

```bash
# 查看所有服务
supervisorctl status
systemctl status nginx
systemctl status mysqld
```

### 2. 重启服务

```bash
# 重启应用
supervisorctl restart eims

# 重启 Nginx
systemctl restart nginx

# 重启 MySQL
systemctl restart mysqld
```

### 3. 更新代码

```bash
cd /var/www/eims

# 停止服务
supervisorctl stop eims

# 拉取代码
git pull origin master

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 数据库迁移
python manage.py makemigrations
python manage.py migrate

# 收集静态文件
python manage.py collectstatic --noinput

# 重启服务
supervisorctl start eims
```

### 4. 数据库备份

```bash
# 创建备份脚本
vim /var/www/backup_db.sh

# 内容：
#!/bin/bash
BACKUP_DIR="/var/backups/eims"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
mysqldump -u eims -p'EimsPassword2026' eims > $BACKUP_DIR/eims_$DATE.sql
find $BACKUP_DIR -name "*.sql" -mtime +7 -delete

# 设置权限
chmod +x /var/www/backup_db.sh

# 添加定时任务
crontab -e
# 每天凌晨 2 点备份
0 2 * * * /var/www/backup_db.sh
```

---

## ❌ 故障排查

### 问题 1：网站无法访问

**检查步骤**：
```bash
# 1. 检查服务状态
supervisorctl status eims
systemctl status nginx

# 2. 检查端口
netstat -tlnp | grep 80
netstat -tlnp | grep 8000

# 3. 查看日志
tail -f /var/log/nginx/error.log
tail -f /var/log/gunicorn/error.log

# 4. 检查防火墙
firewall-cmd --list-all

# 5. 检查安全组（阿里云控制台）
```

### 问题 2：数据库连接失败

**检查步骤**：
```bash
# 1. 检查 MySQL 状态
systemctl status mysqld

# 2. 测试连接
mysql -u eims -p'EimsPassword2026' eims

# 3. 查看 MySQL 日志
tail -f /var/log/mysqld.log
```

### 问题 3：静态文件 404

**解决**：
```bash
# 1. 重新收集静态文件
python manage.py collectstatic --noinput

# 2. 检查 Nginx 配置
nginx -t

# 3. 重启 Nginx
systemctl restart nginx
```

---

## 📞 获取帮助

### 日志文件位置

- **应用日志**：`/var/log/gunicorn/error.log`
- **Supervisor 日志**：`/var/log/gunicorn/supervisor.log`
- **Nginx 日志**：`/var/log/nginx/error.log`
- **MySQL 日志**：`/var/log/mysqld.log`

### 常用命令

```bash
# 查看 Supervisor 状态
supervisorctl status

# 查看进程
ps aux | grep gunicorn
ps aux | grep nginx
ps aux | grep mysql

# 查看端口占用
netstat -tlnp

# 查看磁盘空间
df -h

# 查看内存使用
free -h

# 查看 CPU 使用
top
```

---

## ✅ 部署完成检查清单

- [ ] Python 3.14 安装成功
- [ ] 项目代码克隆成功
- [ ] 虚拟环境创建成功
- [ ] Python 依赖安装成功
- [ ] MySQL 安装并运行
- [ ] 数据库创建成功
- [ ] settings.py 配置完成
- [ ] 数据库迁移成功
- [ ] 超级用户创建成功
- [ ] 静态文件收集成功
- [ ] Gunicorn 运行正常
- [ ] Nginx 运行正常
- [ ] 安全组配置正确
- [ ] 网站可以访问
- [ ] 管理后台可以登录
- [ ] 日志文件正常生成

---

**🎉 如果所有项目都已勾选，恭喜您部署成功！**

**文档位置**：`/var/www/eims/阿里云部署指南.md`  
**快速脚本**：`/var/www/eims/deploy.sh`
