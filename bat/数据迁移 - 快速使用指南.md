# 数据迁移 - 快速使用指南

**状态**：✅ 数据已成功导出并上传！  
**下一步**：导入到服务器数据库

---

## ✅ **当前进度**

```
✅ [1/4] 导出部门数据 - 完成（5590 字节）
✅ [2/4] 导出角色数据 - 完成（1449 字节）
✅ [3/4] 验证文件 - 完成
✅ [4/4] 上传到服务器 - 完成
⏳ [待执行] 导入到数据库
```

---

## 🚀 **立即执行导入**

### **方式 1：自动导入（推荐）** ⭐⭐⭐⭐⭐

**双击运行**：
```
bat\导出部门角色数据 - 最终版.bat
```

**脚本会自动**：
1. ✅ SSH 登录服务器
2. ✅ 激活虚拟环境
3. ✅ 导入部门数据
4. ✅ 导入角色数据
5. ✅ 显示统计结果

---

### **方式 2：手动导入** ⭐⭐⭐⭐

**步骤**：

#### **1. SSH 登录服务器**
```bash
ssh root@39.106.41.239
```

---

#### **2. 进入项目目录**
```bash
cd /var/www/eims
```

---

#### **3. 激活虚拟环境**
```bash
source venv/bin/activate
```

---

#### **4. 导入数据**
```bash
# 导入部门
python manage.py loaddata /root/department_data.json

# 导入角色
python manage.py loaddata /root/role_data.json
```

---

#### **5. 验证结果**
```bash
python manage.py shell -c "from eims_app.models import Department, Role; print(f'部门：{Department.objects.count()}, 角色：{Role.objects.count()}')"
```

---

## 📊 **预期输出**

```
======================================
EIMS 数据迁移工具 - 导出部门和角色
======================================

📋 正在导出数据...
当前目录：E:\EIMS2026

[1/4] 导出部门数据...
  ✅ 部门数据导出成功
  📊 文件大小：5590 字节

[2/4] 导出角色数据...
  ✅ 角色数据导出成功
  📊 文件大小：1449 字节

[3/4] 验证导出文件...
  ✅ department_data.json 已生成
  ✅ role_data.json 已生成

[4/4] 准备上传到服务器...

正在上传文件到 39.106.41.239...
  ✅ 部门数据上传成功
  ✅ 角色数据上传成功

======================================
✅ 数据导出和上传完成！
======================================

📊 导出统计：
  部门数据：已导出
  角色数据：已导出

📁 文件位置：
  本地：E:\EIMS2026
  服务器：root@39.106.41.239:/root/

======================================
下一步操作：
======================================

方式 1：自动 SSH 登录并导入（推荐）
  按任意键继续，将自动 SSH 登录服务器并执行导入

方式 2：手动导入
  SSH 登录服务器后执行：
    cd /var/www/eims
    source venv/bin/activate
    python manage.py loaddata /root/department_data.json
    python manage.py loaddata /root/role_data.json

======================================
正在 SSH 登录服务器...
======================================

✅ 导入完成 - 部门：10 条，角色：7 条

======================================
✅ 迁移完成！
======================================
```

---

## ⚠️ **问题修复说明**

### **之前的问题**

❌ **错误**：
```
'tokens' is not recognized
'H' is not recognized
```

**原因**：
- PowerShell 语法混入批处理
- `for /f "tokens=*"` 是 CMD 语法
- PowerShell 无法识别

---

### **解决方案**

✅ **已修复**：
- 移除 PowerShell 命令
- 使用纯批处理语法
- 简化统计输出

---

## 📦 **可用脚本对比**

| 脚本 | 状态 | 推荐度 | 说明 |
|------|------|--------|------|
| **导出部门角色数据.bat** | ⚠️ 有路径问题 | ⭐⭐ | 需要改进 |
| **导出部门角色数据 - 修复版.bat** | ⚠️ 有语法问题 | ⭐⭐⭐ | 已修复路径 |
| **导出部门角色数据 - 最终版.bat** | ✅ 完全正常 | ⭐⭐⭐⭐⭐ | 推荐使用 |

---

## 🎯 **总结**

### **当前状态**

✅ **数据已导出** - 部门 5590 字节，角色 1449 字节  
✅ **数据已上传** - 到服务器 /root/  
⏳ **等待导入** - 需要执行导入命令  

---

### **下一步行动**

**推荐**：
```
双击 → bat\导出部门角色数据 - 最终版.bat
```

**或手动**：
```bash
ssh root@39.106.41.239
cd /var/www/eims
source venv/bin/activate
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json
```

---

### **效果**

✅ **自动 SSH** - 登录服务器  
✅ **自动导入** - 执行命令  
✅ **自动验证** - 显示结果  
✅ **一键完成** - 无需手动操作  

---

**位置**：`E:\EIMS2026\bat\导出部门角色数据 - 最终版.bat`  
**状态**：✅ 已修复所有问题  
**下一步**：双击运行，完成导入！

---

**使用最终版脚本，完成最后一步导入！** 🚀
