# 🔧 导入功能不显示？完整修复指南

## ❓ 为什么看不到"导入"按钮？

可能的原因:
1. ❌ `django-import-export` 未正确安装
2. ❌ `admin.py` 配置不完整
3. ❌ 服务器未重启
4. ❌ 浏览器缓存问题

---

## ✅ 解决方案 (按顺序执行)

### **步骤 1: 检查安装状态**

双击运行:
```
E:\EIMS2026\bat\check-import-export-status.bat
```

**或者手动 SSH 检查:**
```bash
ssh root@39.106.41.239
cd /var/www/eims
source venv/bin/activate
pip show django-import-export
```

**如果看到类似信息，说明已安装:**
```
Name: django-import-export
Version: 3.3.7
Location: /var/www/eims/venv/lib/python3.10/site-packages
```

---

### **步骤 2: 如果没有安装或安装失败**

```bash
# SSH 登录
ssh root@39.106.41.239

# 进入虚拟环境
cd /var/www/eims
source venv/bin/activate

# 重新安装
pip install django-import-export==3.3.7

# 验证安装
python3 -c "from import_export.admin import ImportExportModelAdmin; print('OK!')"
```

---

### **步骤 3: 修改 admin.py (已自动完成)**

我已经帮您修改好了 `admin.py`,添加了:

```python
# 导入导入导出功能
try:
    from import_export.admin import ImportExportModelAdmin
    from import_export import resources
    IMPORT_EXPORT_AVAILABLE = True
    
    # 创建 Resource 类
    class ProjectResource(resources.ModelResource):
        class Meta:
            model = Project
            import_id_fields = ('id',)
    
    class EmployeeResource(resources.ModelResource):
        class Meta:
            model = Employee
            import_id_fields = ('id',)
    
    class ContractResource(resources.ModelResource):
        class Meta:
            model = Contract
            import_id_fields = ('id',)
except ImportError:
    IMPORT_EXPORT_AVAILABLE = False

# 注册 Admin 类
@admin.register(Project)
class ProjectAdmin(ImportExportModelAdmin if IMPORT_EXPORT_AVAILABLE else admin.ModelAdmin):
    resource_classes = [ProjectResource]  # 关键！

@admin.register(Employee)
class EmployeeAdmin(ImportExportModelAdmin if IMPORT_EXPORT_AVAILABLE else admin.ModelAdmin):
    resource_classes = [EmployeeResource]  # 关键！

@admin.register(Contract)
class ContractAdmin(ImportExportModelAdmin if IMPORT_EXPORT_AVAILABLE else admin.ModelAdmin):
    resource_classes = [ContractResource]  # 关键！
```

---

### **步骤 4: 重启服务器**

```bash
# SSH 登录
ssh root@39.106.41.239

# 重启 Gunicorn
supervisorctl restart eims
```

**或者双击运行:**
```
E:\EIMS2026\bat\restart-and-verify.bat
```

---

### **步骤 5: 清除浏览器缓存**

**重要!** 浏览器缓存可能导致看不到新按钮

**方法 1: 硬刷新**
- Windows: `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**方法 2: 清除缓存**
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

**方法 3: 隐私模式**
- Chrome: `Ctrl+Shift+N`
- Firefox: `Ctrl+Shift+P`
- Edge: `Ctrl+Shift+N`

---

### **步骤 6: 检查是否显示**

刷新后应该看到:

```
+----------------------------------+
|  Django Admin - 员工管理          |
+----------------------------------+
|  [+ 添加员工]  [📥 导入]  [📤 导出]|
+----------------------------------+
```

---

## 🔍 **故障排查**

### **问题 1: 还是不显示"导入"按钮**

**检查:**
```bash
ssh root@39.106.41.239
cd /var/www/eims
source venv/bin/activate
python3 manage.py shell
```

```python
# 检查是否安装
try:
    from import_export.admin import ImportExportModelAdmin
    print("✓ 导入成功")
except:
    print("✗ 导入失败，需要重新安装")

# 检查 admin.py
from eims_app.admin import EmployeeAdmin
print(f"EmployeeAdmin 基类：{EmployeeAdmin.__bases__}")
print(f"是否有 resource_classes: {hasattr(EmployeeAdmin, 'resource_classes')}")
```

---

### **问题 2: 显示但点击报错**

**解决:**
```bash
# 检查模板文件
ls -la /var/www/eims/venv/lib/python3.10/site-packages/import_export/templates/
```

---

### **问题 3: ImportError 错误**

**重新安装:**
```bash
cd /var/www/eims
source venv/bin/activate
pip uninstall django-import-export -y
pip install django-import-export==3.3.7
supervisorctl restart eims
```

---

## 📋 **完整诊断脚本**

创建一个诊断脚本 `diagnose-import.bat`:

```bash
@echo off
echo 检查服务器上的 django-import-export...
ssh root@39.106.41.239 "cd /var/www/eims; source venv/bin/activate; python3 << 'EOF'
try:
    import import_export
    print(f'✓ django-import-export 已安装')
    print(f'  版本：{import_export.__version__}')
    print(f'  位置：{import_export.__file__}')
except ImportError:
    print('✗ django-import-export 未安装')

try:
    from import_export.admin import ImportExportModelAdmin
    print('✓ ImportExportModelAdmin 可以导入')
except Exception as e:
    print(f'✗ 导入失败：{e}')

# 检查 admin.py
from eims_app.admin import EmployeeAdmin
print(f'✓ EmployeeAdmin 基类：{EmployeeAdmin.__bases__}')
if hasattr(EmployeeAdmin, 'resource_classes'):
    print(f'✓ resource_classes: {EmployeeAdmin.resource_classes}')
else:
    print('✗ resource_classes 未设置')
EOF
"
pause
```

---

## 🎯 **快速修复流程**

```
1. 运行 check-import-export-status.bat
   ↓
2. 如果未安装 → 运行 install-import-export-tool.bat
   ↓
3. 如果已安装 → 检查 admin.py 是否正确
   ↓
4. 运行 restart-and-verify.bat
   ↓
5. 浏览器 Ctrl+F5 硬刷新
   ↓
6. 访问 http://39.106.41.239:8000/admin/eims_app/employee/
   ↓
7. 应该看到 [导入] 按钮!
```

---

## ✨ **成功标志**

成功后，您应该看到:

**员工列表页面:**
```
+--------------------------------------------------+
| Django 管理 | 站点管理                            |
+--------------------------------------------------+
| 欢迎，admin. 查看站点 / 修改密码 / 注销          |
+--------------------------------------------------+
|                                                  |
| 员工信息管理                                     |
|                                                  |
|  [+ 添加员工]  [📥 导入]  [📤 导出]               |
|                                                  |
|  搜索：[_______]  [搜索]                         |
|  过滤：[性别 ▼] [学历 ▼]                         |
|                                                  |
|  员工列表 (2 条)                                  |
|  +----+------+--------+------------+--------+    |
|  | ✓ | 编号 | 姓名   | 部门       | 职位   |    |
|  +----+------+--------+------------+--------+    |
|  |   | 001  | 张三   | 技术部     | 工程师 |    |
|  |   | 002  | 李四   | 市场部     | 经理   |    |
|  +----+------+--------+------------+--------+    |
+--------------------------------------------------+
```

---

## 💡 **如果还是不行**

请提供以下信息:

1. **安装检查结果** (运行 `check-import-export-status.bat`)
2. **Admin 页面截图** (显示没有导入按钮的页面)
3. **服务器日志** (运行 `tail -50 /var/log/supervisor/eims.err.log`)

我会帮您进一步诊断！

---

**现在请执行步骤 1: 运行检查脚本!**

```
双击：E:\EIMS2026\bat\check-import-export-status.bat
```
