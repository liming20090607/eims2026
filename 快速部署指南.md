# 快速部署指南 - 立即执行

**当前状态**：
- ✅ django-extensions 已安装
- ❌ Werkzeug 未安装（需要安装）
- ✅ SSH 已连接服务器

---

## 🚀 本地 HTTPS 开发（立即解决）

### **问题：Werkzeug 未安装**

**错误信息**：
```
CommandError: Werkzeug is required to use runserver_plus.
```

---

### **解决方案**

#### **方式 1：使用批处理脚本（推荐）** ⭐⭐⭐⭐⭐

**双击执行**：
```
bat\安装 HTTPS 完整依赖.bat
```

**然后**：
```
双击 run_https.bat 启动 HTTPS 服务器
```

---

#### **方式 2：手动安装** ⭐⭐⭐⭐

**PowerShell 执行**：
```powershell
cd E:\EIMS2026
pip install werkzeug
```

**验证**：
```powershell
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8000
```

---

#### **方式 3：一次性安装所有** ⭐⭐⭐⭐⭐

**PowerShell 执行**：
```powershell
pip install werkzeug django-extensions
```

---

### **启动 HTTPS 服务器**

安装完成后，**双击**：
```
run_https.bat
```

**访问**：
```
https://localhost:8000
```

---

## 🌐 服务器部署（备案通过后）

### **当前已连接到服务器**

您已成功 SSH 登录到阿里云服务器：
```
root@39.106.41.239
```

---

### **步骤 1：上传部署脚本**

**在本地 PowerShell 执行**：
```powershell
# 上传基础环境安装脚本
scp E:\EIMS2026\服务器基础环境安装.sh root@39.106.41.239:/root/setup.sh

# 上传完整部署脚本
scp E:\EIMS2026\生产环境部署脚本.sh root@39.106.41.239:/root/deploy.sh
```

---

### **步骤 2：在服务器上执行**

**SSH 中执行**：
```bash
# 赋予执行权限
chmod +x /root/setup.sh

# 执行基础环境安装
./setup.sh
```

**等待安装完成**（约 5-10 分钟）

---

### **步骤 3：验证安装**

**SSH 中执行**：
```bash
# 检查 Python
python3 --version

# 检查 Django
django-admin --version

# 检查 Nginx
nginx -v

# 检查 Certbot
certbot --version
```

**预期输出**：
```
Python 3.x.x
Django 5.2.x
nginx 1.x.x
certbot 1.x.x
```

---

## 📊 部署流程对比

| 环境 | 步骤 | 时间 |
|------|------|------|
| **本地开发** | 安装 Werkzeug → 启动 HTTPS | 2 分钟 |
| **服务器部署** | 安装基础环境 → 配置 → 上线 | 30 分钟 |

---

## 🎯 立即行动

### **本地开发（现在）**

```powershell
# 1. 安装 Werkzeug
pip install werkzeug

# 2. 启动 HTTPS 服务器
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8000

# 3. 访问
浏览器打开 → https://localhost:8000
```

**效果**：
- ✅ HTTPS 加密
- ✅ 消除"不安全"警告
- ✅ 开发环境测试

---

### **服务器部署（备案通过后）**

```bash
# 1. 上传脚本（PowerShell）
scp 服务器基础环境安装.sh root@39.106.41.239:/root/setup.sh

# 2. 执行安装（SSH）
./setup.sh

# 3. 配置域名
# 4. 申请证书
# 5. 正式上线
```

---

## ⚠️ 常见问题

### **Q1: pip 安装失败？**

**A**: 检查网络和 Python 环境：
```powershell
# 升级 pip
python -m pip install --upgrade pip

# 使用国内镜像
pip install werkzeug -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

### **Q2: 端口被占用？**

**A**: 更换端口：
```powershell
# 使用其他端口
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8080
```

---

### **Q3: 证书生成失败？**

**A**: 删除旧证书重新生成：
```powershell
# 删除旧证书
del cert.pem key.pem

# 重新启动
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem
```

---

## 📝 已创建的工具

### **本地开发工具**

| 文件 | 功能 |
|------|------|
| `bat\安装 HTTPS 完整依赖.bat` | 安装 Werkzeug + django-extensions |
| `run_https.bat` | 启动 HTTPS 开发服务器 |
| `消除 HTTPS 警告完全指南.md` | 完整使用指南 |

---

### **服务器部署工具**

| 文件 | 功能 |
|------|------|
| `服务器基础环境安装.sh` | 安装 Python、Django、Nginx |
| `生产环境部署脚本.sh` | 完整自动化部署 |
| `生产部署检查清单.md` | 部署检查清单 |
| `域名备案与 HTTPS 部署完全指南.md` | 完整部署指南 |

---

## 🎉 总结

### **本地开发（立即执行）**

```powershell
# 1. 安装 Werkzeug
pip install werkzeug

# 2. 启动 HTTPS
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8000

# 3. 访问
https://localhost:8000
```

**效果**：
- ✅ 消除"不安全"警告
- ✅ HTTPS 加密开发
- ✅ 测试 HTTPS 功能

---

### **服务器部署（备案通过后）**

```bash
# 1. 上传脚本
scp 服务器基础环境安装.sh root@39.106.41.239:/root/setup.sh

# 2. 执行安装
./setup.sh

# 3. 配置域名和证书
# 4. 正式上线
```

**效果**：
- ✅ 正式 HTTPS 网站
- ✅ 浏览器信任证书
- ✅ 用户可安全访问

---

## 📞 快速命令参考

### **本地开发**

```powershell
# 安装依赖
pip install werkzeug django-extensions

# 启动 HTTPS
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8000

# 查看证书
dir cert.pem key.pem
```

---

### **服务器部署**

```bash
# 安装基础环境
./setup.sh

# 验证安装
python3 --version
nginx -v
certbot --version

# 查看服务状态
systemctl status nginx
```

---

**位置**：`E:\EIMS2026\快速部署指南.md`  
**状态**：✅ 立即可用  
**下一步**：安装 Werkzeug，启动 HTTPS 服务器！

---

## 💡 建议

### **现在（备案期间）**

1. ✅ 安装 Werkzeug
2. ✅ 启动本地 HTTPS
3. ✅ 测试所有功能
4. ✅ 准备部署脚本

---

### **备案通过后**

1. ✅ 配置 DNS
2. ✅ 执行部署脚本
3. ✅ 申请证书
4. ✅ 正式上线

---

**立即解决 Werkzeug 问题，开始 HTTPS 开发！** 🚀
