# 会话跳转登录问题 - 最终解决方案

## 🔍 问题描述

在云服务器（39.106.41.239:8000）上，用户登录后点击以下按钮仍然跳转到登录页面：
- 新增产值回款
- 项目人员 → 新增
- 其他需要登录的页面

## 🔧 最新修复内容

### 修改文件：settings.py

添加了以下关键配置：

```python
# Session Cookie 安全设置（HTTP 访问必须为 False）
SESSION_COOKIE_SECURE = False  # 当前使用 HTTP，设为 False

# SameSite 策略（防止 CSRF，允许同站请求）
SESSION_COOKIE_SAMESITE = 'Lax'

# Cookie 路径（确保整个站点可用）
SESSION_COOKIE_PATH = '/'
```

---

## 🚀 服务器部署步骤（必须严格执行）

### 步骤 1：SSH 登录服务器

```bash
ssh root@39.106.41.239
```

### 步骤 2：拉取最新代码

```bash
cd /var/www/eims
git pull gitee master
```

### 步骤 3：验证配置是否生效

```bash
cd /var/www/eims
grep -A 15 "Session 配置" settings.py
```

**应该看到以下输出：**

```python
SESSION_COOKIE_AGE = 1209600
SESSION_SAVE_EVERY_REQUEST = True
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_NAME = 'eims_sessionid'
SESSION_COOKIE_SAMESITE = 'Lax'
SESSION_COOKIE_PATH = '/'
SESSION_COOKIE_SECURE = False  # ← 这个最重要！
```

**如果没看到 `SESSION_COOKIE_SECURE = False`，说明代码未更新成功！**

### 步骤 4：重启服务

```bash
supervisorctl restart eims
```

### 步骤 5：验证服务是否正常运行

```bash
supervisorctl status
```

应该看到：
```
eims                             RUNNING   pid 12345, uptime 0:00:30
```

### 步骤 6：⚠️ 非常重要 - 清除浏览器所有数据

**这是最关键的一步，必须执行！**

#### 方法 1：清除所有 Cookie 和缓存

1. 按 `Ctrl + Shift + Delete`
2. 时间范围：选择"时间不限"
3. 勾选：
   - ✅ Cookie 和其他网站数据
   - ✅ 缓存的图片和文件
   - ✅ 浏览历史记录（可选）
4. 点击"清除数据"

#### 方法 2：使用无痕模式（推荐）

1. 按 `Ctrl + Shift + N`（Chrome）或 `Ctrl + Shift + P`（Firefox）
2. 打开新的无痕窗口
3. 访问：`http://39.106.41.239:8000/login/`
4. 重新登录并测试

---

## 🔍 如果问题仍然存在 - 故障排查

### 排查 1：检查服务器 Session 配置

在服务器上运行：

```bash
cd /var/www/eims
python -c "
import django
import os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings')
django.setup()
from django.conf import settings
print('SESSION_COOKIE_SECURE:', settings.SESSION_COOKIE_SECURE)
print('SESSION_COOKIE_NAME:', settings.SESSION_COOKIE_NAME)
print('SESSION_COOKIE_SAMESITE:', settings.SESSION_COOKIE_SAMESITE)
print('SESSION_COOKIE_PATH:', settings.SESSION_COOKIE_PATH)
print('SESSION_SAVE_EVERY_REQUEST:', settings.SESSION_SAVE_EVERY_REQUEST)
"
```

**预期输出：**
```
SESSION_COOKIE_SECURE: False
SESSION_COOKIE_NAME: eims_sessionid
SESSION_COOKIE_SAMESITE: Lax
SESSION_COOKIE_PATH: /
SESSION_SAVE_EVERY_REQUEST: True
```

### 排查 2：检查浏览器 Cookie

1. 打开浏览器，按 `F12` 打开开发者工具
2. 切换到 **Application**（或"应用程序"）标签
3. 左侧选择 **Storage** → **Cookies** → `http://39.106.41.239:8000`
4. 检查是否存在以下 Cookie：
   - `eims_sessionid` ← 应该存在
   - `csrftoken` ← 应该存在

**如果 Cookie 不存在，说明：**
- 浏览器拒绝了 Cookie
- 或者配置仍然不正确

**如果 `eims_sessionid` 的 Secure 列为 Yes，说明：**
- 配置错误，SESSION_COOKIE_SECURE 应该为 False

### 排查 3：查看服务器日志

```bash
# 查看最近的错误日志
tail -f /var/www/eims/logs/error.log

# 或者查看 Supervisor 日志
supervisorctl tail eims stderr
```

查找以下关键词：
- `SessionMiddleware`
- `AuthenticationMiddleware`
- `CSRF`
- `Forbidden`

### 排查 4：手动测试登录

在浏览器开发者工具 Console 中运行：

```javascript
// 查看当前 Cookie
console.log(document.cookie);

// 测试是否已登录（应该返回 true）
fetch('/api/test-auth/', {
    method: 'GET',
    credentials: 'same-origin'
})
.then(r => r.json())
.then(data => console.log('认证状态:', data));
```

### 排查 5：检查中间件顺序

在服务器上运行：

```bash
cd /var/www/eims
grep -A 10 "^MIDDLEWARE" settings.py
```

**应该看到以下顺序：**

```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',  # ← 第 2 个
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',  # ← 第 5 个
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'eims_app.middleware.login_required.login_required_middleware',
    'eims_app.middleware.monthly_report_reminder.monthly_report_reminder_middleware',
]
```

---

## 📋 问题可能原因总结

### 原因 1：SESSION_COOKIE_SECURE 设置为 True（最可能）

**问题**：通过 HTTP 访问时，如果 `SESSION_COOKIE_SECURE = True`，浏览器会拒绝发送 Session Cookie。

**解决**：确保 `SESSION_COOKIE_SECURE = False`

### 原因 2：浏览器缓存了旧的 Session Cookie

**问题**：旧配置生成的 Cookie 与新配置不兼容。

**解决**：清除浏览器所有 Cookie 和缓存

### 原因 3：Cookie 名称冲突

**问题**：同时存在 `sessionid` 和 `eims_sessionid` 两个 Cookie。

**解决**：清除所有 Cookie，只保留新的 `eims_sessionid`

### 原因 4：SameSite 策略过严

**问题**：如果 `SESSION_COOKIE_SAMESITE = 'Strict'`，某些请求会被阻止。

**解决**：使用 `SESSION_COOKIE_SAMESITE = 'Lax'`

---

## ✅ 验证清单

请按顺序逐项测试：

- [ ] 代码已拉取（`git pull` 显示已是最新）
- [ ] 配置验证通过（`SESSION_COOKIE_SECURE = False`）
- [ ] 服务已重启（`supervisorctl restart`）
- [ ] 服务运行正常（`supervisorctl status` 显示 RUNNING）
- [ ] 浏览器 Cookie 已清除
- [ ] 使用无痕模式测试
- [ ] 登录成功
- [ ] 查看 Cookie，确认 `eims_sessionid` 存在
- [ ] 点击"新增产值回款"，**不跳转**登录页
- [ ] 点击"项目人员"→"新增"，**不跳转**登录页
- [ ] 保持页面 5 分钟，仍然登录状态
- [ ] 刷新页面，仍然登录状态

---

## 🆘 仍然无法解决？

### 收集以下信息并提供：

1. **服务器配置验证结果**
   ```bash
   cd /var/www/eims
   python -c "
   import django
   import os
   os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings')
   django.setup()
   from django.conf import settings
   print('SESSION_COOKIE_SECURE:', settings.SESSION_COOKIE_SECURE)
   print('SESSION_COOKIE_NAME:', settings.SESSION_COOKIE_NAME)
   "
   ```

2. **浏览器 Cookie 截图**
   - 打开 F12 → Application → Cookies
   - 截图显示所有 Cookie

3. **服务器错误日志**
   ```bash
   tail -100 /var/www/eims/logs/error.log
   ```

4. **问题复现步骤**
   - 使用的浏览器及版本
   - 是否使用无痕模式
   - 登录后多久点击按钮
   - 跳转到的具体 URL

5. **网络请求详情**
   - F12 → Network 标签
   - 点击按钮时查看请求头
   - 查看 Cookie 是否被发送

---

## 🎯 最终解决方案

如果以上所有步骤都无法解决问题，可以尝试**完全重置 Session**：

### 方案 1：重启服务器

```bash
# SSH 登录
ssh root@39.106.41.239

# 重启整个服务器
reboot
```

### 方案 2：重新初始化 Django

```bash
ssh root@39.106.41.239
cd /var/www/eims

# 停止服务
supervisorctl stop eims

# 清除旧的 session 数据
python manage.py clearsessions

# 重启服务
supervisorctl start eims
```

### 方案 3：使用不同的浏览器

- 如果 Chrome 有问题，尝试 Firefox 或 Edge
- 确认是否是浏览器特定的问题

---

## 📞 联系支持

如果以上所有方法都无法解决问题，请提供：

1. 上述所有诊断信息
2. 服务器操作系统版本：`cat /etc/os-release`
3. Python 版本：`python --version`
4. Django 版本：`python -m django --version`

---

**最后更新**：2026-03-21  
**版本**：v2.0（最终解决方案）  
**作者**：EIMS 开发团队
