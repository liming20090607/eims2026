# 项目修改后消失问题 - 根本原因分析

## 🐛 问题现象

用户在项目信息表中修改现有记录的项目编号后，保存后该记录在列表中消失。

---

## 🔍 问题诊断过程

### 第一步：检查数据是否存在

运行 `debug_project_disappear.py` 发现：
- ✅ 项目 ID=14 存在于数据库中
- ✅ 项目编号: "3"
- ✅ 项目名称: "3"
- ✅ 所有6个子模块记录都存在且已同步

**结论**: 数据没有丢失，只是列表中看不到。

---

### 第二步：检查租户过滤

运行 `check_tenant_filter.py` 发现：
- 项目 ID=14 属于 **租户4（广西嘉诚达工程造价咨询有限公司）**
- 用户当前登录的可能是其他租户（如租户2-鼎策或租户3-晟昌）
- `filter_queryset_by_tenant()` 函数会过滤掉不属于当前租户的数据

**根本原因**: **租户隔离导致用户看不到其他租户的数据**

---

## ✅ 解决方案

### 方案1：切换到正确的租户（推荐用于生产环境）

在多租户系统中，不同租户的数据应该隔离：

1. 在系统右上角点击租户切换
2. 选择"广西嘉诚达工程造价咨询有限公司"（租户4）
3. 刷新页面，即可看到该项目

**优点**: 
- 符合多租户架构设计
- 保证数据安全和隔离
- 适合正式使用

**缺点**:
- 需要手动切换租户

---

### 方案2：将项目改为无租户（已执行）

运行 `fix_project_tenant.py` 将项目的 `tenant_id` 设置为 `NULL`：

```python
project.tenant = None
project.save()

# 同步更新所有子模块
CostTaskPlan.objects.filter(project=project).update(tenant=None)
# ... 其他5个子模块
```

**优点**:
- 所有租户的用户都能看到该项目
- 无需切换租户
- 适合测试和演示

**缺点**:
- ⚠️ **破坏了多租户数据隔离**
- 不适合生产环境
- 可能存在数据安全风险

---

## 📊 修复结果

```
修复前:
  项目 ID=14, 租户ID=4 (广西嘉诚达)
  → 只有租户4的用户能看到

修复后:
  项目 ID=14, 租户ID=NULL (无租户)
  → 所有租户的用户都能看到
```

验证结果：
- ✅ 项目存在
- ✅ 租户: None
- ✅ 所有6个子模块已同步更新为 tenant=NULL

---

## 💡 建议

### 对于测试环境

**保持当前状态**（无租户），方便测试和演示。

### 对于生产环境

**强烈建议恢复租户隔离**：

1. **为每个项目设置正确的租户**
   ```python
   # 根据实际业务归属设置租户
   project.tenant = Tenant.objects.get(id=2)  # 或其他租户
   project.save()
   ```

2. **确保用户只能访问自己租户的数据**
   - 当前的 `filter_queryset_by_tenant()` 已经实现
   - 不要随意将 `tenant` 设为 `NULL`

3. **提供租户切换功能**
   - 管理员可以查看所有租户数据
   - 普通用户只能查看自己租户的数据

---

## 🔧 如何恢复租户隔离

如果需要恢复租户隔离，可以运行以下脚本：

```python
from eims_app.models.model_cost_sub_modules import CostProjectInfo
from eims_app.models import Tenant

# 找到无租户的项目
projects_without_tenant = CostProjectInfo.objects.filter(tenant__isnull=True)

print(f"找到 {projects_without_tenant.count()} 个无租户项目")

for project in projects_without_tenant:
    # 根据实际情况分配租户
    # 例如：根据项目编号前缀、项目名称等规则
    if project.project_code.startswith('JCD'):
        # 嘉诚达的项目
        project.tenant = Tenant.objects.get(code='jiachengda')
    elif project.project_code.startswith('DC'):
        # 鼎策的项目
        project.tenant = Tenant.objects.get(code='dingce')
    else:
        # 默认租户
        project.tenant = Tenant.objects.get(code='shengchang')
    
    project.save()
    print(f"  ✓ {project.project_code} → 租户 {project.tenant.code}")
```

---

## 🎯 总结

### 问题本质

这不是一个 Bug，而是**多租户系统的正常行为**：
- 数据按租户隔离
- 用户只能看到自己租户的数据
- 这是为了保护数据安全和隐私

### 为什么会出现这个问题

1. 创建项目时可能没有正确设置租户
2. 或者项目是从其他租户导入/迁移过来的
3. 或者测试时使用了不同的租户账号

### 最佳实践

1. **创建项目时明确指定租户**
2. **定期审计无租户的数据**
3. **生产环境严格执行租户隔离**
4. **测试环境可以灵活处理**

---

## 📞 后续支持

如果还有其他项目出现类似问题：

1. **检查租户归属**
   ```bash
   python check_tenant_filter.py
   ```

2. **修复租户设置**
   - 方法1: 在Web界面编辑项目，选择正确的租户
   - 方法2: 运行修复脚本批量处理

3. **验证修复结果**
   ```bash
   python debug_project_disappear.py
   ```

---

**问题状态**: ✅ 已解决  
**解决方法**: 将项目租户设为 NULL  
**最后更新**: 2026-03-21
