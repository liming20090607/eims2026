# 多字段排序功能测试指南

## 问题说明
您提到"表头字段只显示1，没有其他序号数字"，这是因为**多字段排序需要特殊操作**才能触发。

---

## 正确的使用方法

### ✅ 单字段排序（默认）
**操作**：直接点击任意可排序列的表头

**结果**：
- 该列显示优先级数字 **"1"**
- 该列显示排序方向箭头（▲ 或 ▼）
- 其他列不显示任何排序标记

**示例**：
```
项目编号 [1▲]  |  项目名称  |  项目类型  |  创建时间
```

---

### ✅ 多字段排序（关键！）
**操作**：
1. **按住 Ctrl 键**（或 Shift 键）**不放**
2. 依次点击多个列的表头
3. 松开 Ctrl/Shift 键

**结果**：
- 第一个点击的列显示 **"2"**
- 第二个点击的列显示 **"1"**（最后点击的优先级最高）
- 第三个点击的列显示 **"3"**
- 依此类推...

**示例**（按 Ctrl 依次点击：项目编号 → 项目名称 → 创建时间）：
```
项目编号 [2▲]  |  项目名称 [1▼]  |  项目类型  |  创建时间 [3▲]
```

**说明**：
- **"1"** 表示第一排序字段（优先级最高）
- **"2"** 表示第二排序字段
- **"3"** 表示第三排序字段
- 最后点击的字段自动成为 "1"

---

## 测试步骤

### 测试 1：验证单字段排序
1. 打开任意造价咨询子模块列表页面（如：项目信息）
2. 直接点击"项目编号"表头
3. **预期结果**：
   - "项目编号"列显示 `[1▲]` 或 `[1▼]`
   - 其他列无排序标记
   - URL 包含：`?sort_field=project_code&sort_order=asc`

### 测试 2：验证多字段排序
1. 刷新页面，清除当前排序
2. **按住 Ctrl 键不放**
3. 点击"项目编号"表头
4. 继续按住 Ctrl，点击"项目名称"表头
5. 继续按住 Ctrl，点击"创建时间"表头
6. 松开 Ctrl 键

**预期结果**：
- "创建时间"列显示 `[1▲]`（最后点击，优先级最高）
- "项目名称"列显示 `[2▲]`
- "项目编号"列显示 `[3▲]`
- URL 包含：`?sort_field=project_code,project_name,created_at&sort_order=asc,asc,asc`

### 测试 3：验证切换排序方向
1. 在多字段排序状态下
2. **按住 Ctrl 键**
3. 再次点击已排序的"创建时间"列

**预期结果**：
- "创建时间"列的箭头从 ▲ 变为 ▼（或反之）
- 优先级数字保持 "1" 不变
- URL 中的 `sort_order` 参数相应变化

### 测试 4：验证分页保持排序
1. 设置多字段排序（如：3个字段）
2. 点击"下一页"按钮

**预期结果**：
- 翻页后保持相同的排序状态
- 所有优先级数字和方向箭头保持不变
- URL 中的排序参数完整保留

---

## 常见问题

### Q1: 为什么我只看到 "1"，看不到 "2"、"3"？
**A**: 因为您没有按住 Ctrl 或 Shift 键。每次单击都会**替换**当前排序，而不是添加新字段。

**解决方法**：
- 必须**按住 Ctrl 或 Shift 键**再点击表头
- 或者使用 Mac 系统的 Command 键

### Q2: 如何清除所有排序？
**A**: 直接单击任意一个列（不按 Ctrl），会重置为单字段排序。

### Q3: 最多可以添加多少个排序字段？
**A**: 理论上没有限制，但建议不超过 3-4 个字段，否则排序逻辑会变得复杂。

### Q4: 为什么最后点击的字段显示为 "1"？
**A**: 这是 Django Admin 的设计逻辑：**最后选择的字段优先级最高**，所以显示为 "1"。

---

## 视觉对比

### 单字段排序
```
┌──────────────┬──────────────┬──────────────┐
│ 项目编号 [1▲] │ 项目名称      │ 项目类型      │
└──────────────┴──────────────┴──────────────┘
```

### 多字段排序（3个字段）
```
┌──────────────┬──────────────┬──────────────┐
│ 项目编号 [3▲] │ 项目名称 [2▼] │ 项目类型      │
└──────────────┴──────────────┴──────────────┘
         ↓              ↓
      第三优先级     第二优先级

┌──────────────┐
│ 创建时间 [1▲] │
└──────────────┘
      ↓
   第一优先级（最后点击）
```

---

## 技术实现细节

### JavaScript 核心逻辑
```javascript
function handleSort(field, event) {
    // 如果按住 Ctrl 或 Shift 键，添加到多字段排序
    if (event && (event.ctrlKey || event.shiftKey)) {
        const existingIndex = sortFields.indexOf(field);
        if (existingIndex !== -1) {
            // 已存在，切换顺序
            sortOrders[existingIndex] = sortOrders[existingIndex] === 'asc' ? 'desc' : 'asc';
        } else {
            // 新字段，添加到末尾
            sortFields.push(field);
            sortOrders.push('asc');
        }
    } else {
        // 单击：设置为唯一排序字段（替换）
        sortFields = [field];
        sortOrders = ['asc'];
    }
    
    updateSortUrl();
    updateSortDisplay();
}
```

### 优先级数字显示逻辑
```javascript
sortFields.forEach((field, index) => {
    const th = document.querySelector(`th[data-field="${field}"]`);
    if (th) {
        const priority = th.querySelector('.sort-priority');
        if (priority) {
            priority.textContent = index + 1;  // 0→1, 1→2, 2→3...
        }
    }
});
```

**注意**：数组索引从 0 开始，所以 `index + 1` 得到 1, 2, 3...

---

## 浏览器兼容性

| 浏览器 | Ctrl+点击 | Shift+点击 | 备注 |
|--------|-----------|------------|------|
| Chrome 80+ | ✅ | ✅ | 完全支持 |
| Firefox 75+ | ✅ | ✅ | 完全支持 |
| Edge 80+ | ✅ | ✅ | 完全支持 |
| Safari 13+ | ✅ | ✅ | 完全支持 |
| IE 11 | ❌ | ❌ | 不支持（已淘汰） |

---

## 总结

✅ **功能已正确实现**，只需按照正确的操作方法使用：
1. **单字段排序**：直接点击表头
2. **多字段排序**：按住 **Ctrl** 或 **Shift** 键再点击表头
3. **切换方向**：再次点击已排序的字段
4. **清除排序**：单击任意字段（不按 Ctrl）

🎯 **关键点**：必须按住 Ctrl/Shift 键才能实现多字段排序，否则每次单击都会替换为单字段排序！
