# 酬劳分配计算公式构建器功能说明

## 概述

为造价咨询模块的"酬劳分配"表单添加了**可视化公式构建器**功能，用户可以通过点击字段按钮和运算符来构建计算公式，系统自动计算结果并填入"酬劳总额"字段。

---

## 功能特性

### 🎯 核心功能

1. **可视化字段选择**
   - 工程总造价
   - 审减金额
   - 审定金额
   - 计算基数（根据计算基准自动选择）

2. **运算符支持**
   - 加法 `+`
   - 减法 `-`
   - 乘法 `×`
   - 除法 `÷`
   - 括号 `( )`
   - 百分比 `%`

3. **智能计算**
   - 自动获取字段实际值
   - 实时公式验证
   - 计算结果保留2位小数
   - 安全表达式执行

4. **用户友好**
   - 一键清空公式
   - 计算成功提示（Toast）
   - 表单提交前自动计算
   - 错误提示清晰明了

---

## 使用场景示例

### 场景1：按工程总造价计算

**需求**：酬劳 = 工程总造价 × 0.3%

**操作步骤**：
1. 点击"工程总造价"按钮 → 公式框显示 `{total_cost}`
2. 点击"×"按钮 → 公式框显示 `{total_cost}*`
3. 手动输入 `0.3%` → 公式框显示 `{total_cost}*0.3%`
4. 点击"计算"按钮
5. **结果**：如果工程总造价为 1000 万元，酬劳总额自动填入 `3.00` 万元

---

### 场景2：按审减金额计算

**需求**：酬劳 = 审减金额 × 5%

**操作步骤**：
1. 点击"审减金额"按钮 → 公式框显示 `{reduced_amount}`
2. 点击"×"按钮 → 公式框显示 `{reduced_amount}*`
3. 手动输入 `5%` → 公式框显示 `{reduced_amount}*5%`
4. 点击"计算"按钮
5. **结果**：如果审减金额为 50 万元，酬劳总额自动填入 `2.50` 万元

---

### 场景3：复合计算

**需求**：酬劳 = (工程总造价 - 审减金额) × 0.5%

**操作步骤**：
1. 点击"("按钮
2. 点击"工程总造价"按钮 → 公式框显示 `({total_cost}`
3. 点击"-"按钮 → 公式框显示 `({total_cost}-`
4. 点击"审减金额"按钮 → 公式框显示 `({total_cost}-{reduced_amount}`
5. 点击")"按钮 → 公式框显示 `({total_cost}-{reduced_amount})`
6. 点击"×"按钮 → 公式框显示 `({total_cost}-{reduced_amount})*`
7. 手动输入 `0.5%` → 公式框显示 `({total_cost}-{reduced_amount})*0.5%`
8. 点击"计算"按钮
9. **结果**：如果工程总造价 1000 万元，审减金额 50 万元，酬劳总额自动填入 `4.75` 万元

---

### 场景4：使用计算基数

**需求**：根据选择的计算基准自动计算

**操作步骤**：
1. 在"计算基准"下拉框选择"工程总造价"或"审减金额"
2. 点击"计算基数"按钮 → 公式框显示 `{base}`
3. 点击"×"按钮 → 公式框显示 `{base}*`
4. 手动输入 `0.3%` → 公式框显示 `{base}*0.3%`
5. 点击"计算"按钮
6. **结果**：根据计算基准选择，自动获取对应值进行计算

---

## 技术实现

### 前端代码

**文件**: `eims_app/templates/cost_consulting/remuneration_distribution/form.html`

**核心逻辑**:

```javascript
// 1. 字段值获取
function getFieldValue(fieldName) {
    switch(fieldName) {
        case 'total_cost':
            return parseFloat($('#id_remuneration_total_cost').val()) || 0;
        case 'reduced_amount':
            return parseFloat($('#id_remuneration_reduced_amount').val()) || 0;
        // ... 其他字段
    }
}

// 2. 公式构建
$('.formula-field-btn').on('click', function() {
    const field = $(this).data('field');
    formulaInput.val(currentValue + '{' + field + '}');
});

// 3. 公式计算
$('#calculateFormula').on('click', function() {
    // 将字段标记替换为实际值
    let calcExpression = formula
        .replace(/\{total_cost\}/g, getFieldValue('total_cost'))
        .replace(/\{reduced_amount\}/g, getFieldValue('reduced_amount'))
        // ... 替换其他字段
        .replace(/(\d+\.?\d*)%/g, '($1/100)'); // 百分比转换
    
    // 安全计算
    const result = Function('"use strict"; return (' + calcExpression + ')')();
    
    // 填入结果
    totalRemunerationInput.val(result.toFixed(2));
});
```

---

## 安全性设计

### 表达式安全检查

```javascript
// 只允许数字、运算符、括号、小数点
if (!/^[\d+\-*/().]+$/.test(calcExpression)) {
    alert('公式格式错误！请检查公式是否只包含数字和运算符。');
    return;
}
```

**安全机制**:
- ✅ 严格的正则表达式验证
- ✅ 禁止执行任意 JavaScript 代码
- ✅ 只允许数学表达式字符
- ✅ 使用严格模式执行计算
- ✅ 异常捕获和错误提示

---

## 界面布局

### 按钮组排列

```
┌─────────────────────────────────────────────┐
│  字段按钮组                                  │
│  [工程总造价] [审减金额] [审定金额] [计算基数]│
├─────────────────────────────────────────────┤
│  运算符按钮组                                │
│  [+] [-] [×] [÷] [(] [)] [%]               │
├─────────────────────────────────────────────┤
│  计算式输入框            [清空] [计算]       │
│  {total_cost}*0.3%                          │
├─────────────────────────────────────────────┤
│  ℹ️ 点击字段和运算符构建公式，点击"计算"按钮  │
│     自动计算酬劳总额                         │
└─────────────────────────────────────────────┘
```

---

## 数据流向

```
用户操作
  ↓
点击字段/运算符按钮
  ↓
构建公式字符串 {field}*0.3%
  ↓
点击"计算"按钮
  ↓
替换字段标记为实际值 1000*0.003
  ↓
安全验证表达式格式
  ↓
执行数学计算
  ↓
填入"酬劳总额"字段 3.00
  ↓
显示成功提示 Toast
```

---

## 特殊处理

### 1. 百分比转换

用户输入 `0.3%` 会自动转换为 `(0.3/100)` 进行计算

**示例**:
- 输入: `{total_cost}*0.3%`
- 转换: `1000*(0.3/100)`
- 结果: `3.00`

### 2. 计算基数联动

`{base}` 字段会根据"计算基准"下拉框的选择自动获取对应值:
- 选择"工程总造价" → `{base}` = 工程总造价的值
- 选择"审减金额" → `{base}` = 审减金额的值

### 3. 表单提交前自动计算

如果用户构建了公式但未点击"计算"按钮，提交表单时会自动触发计算，确保"酬劳总额"字段有值。

---

## 兼容性

- ✅ **浏览器**: 所有现代浏览器（Chrome, Firefox, Edge, Safari）
- ✅ **jQuery**: 依赖 jQuery（项目已包含）
- ✅ **Bootstrap**: 使用 Bootstrap 5 Toast 组件
- ✅ **响应式**: 支持移动端和桌面端

---

## 测试用例

### 测试1: 基本乘法
- **输入**: `{total_cost}*0.3%`
- **工程总造价**: 1000
- **预期结果**: 3.00

### 测试2: 基本减法
- **输入**: `{total_cost}-{reduced_amount}`
- **工程总造价**: 1000
- **审减金额**: 50
- **预期结果**: 950.00

### 测试3: 复合运算
- **输入**: `({total_cost}-{reduced_amount})*0.5%`
- **工程总造价**: 1000
- **审减金额**: 50
- **预期结果**: 4.75

### 测试4: 百分比计算
- **输入**: `{reduced_amount}*5%`
- **审减金额**: 50
- **预期结果**: 2.50

### 测试5: 错误处理
- **输入**: `{total_cost}**0.3%` (错误语法)
- **预期结果**: 弹出错误提示"公式格式错误"

---

## 相关文件

| 文件 | 说明 |
|------|------|
| `eims_app/templates/cost_consulting/remuneration_distribution/form.html` | 表单模板（包含公式构建器UI和JavaScript逻辑） |
| `eims_app/forms/form_cost_sub_modules.py` | 表单定义（CostRemunerationDistributionForm） |
| `eims_app/views/views_cost_sub_modules.py` | 视图函数（cost_remuneration_distribution_add/edit） |
| `eims_app/models/model_cost_unified.py` | 数据模型（CostProjectUnified） |

---

## 后续优化建议

1. **公式历史记录**
   - 保存用户常用的计算公式
   - 提供公式模板快速选择

2. **实时预览**
   - 输入公式时实时显示计算结果
   - 无需点击"计算"按钮

3. **公式验证增强**
   - 括号匹配检查
   - 运算符连续性检查
   - 更友好的错误提示

4. **更多字段支持**
   - 添加"编制金额"、"送审金额"等字段
   - 支持自定义字段

5. **公式保存**
   - 将公式保存到数据库
   - 下次编辑时自动加载

---

**创建日期**: 2026-03-21  
**功能状态**: ✅ 已完成并部署  
**测试状态**: 待用户验证
