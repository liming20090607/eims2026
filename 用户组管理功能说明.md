# 用户组管理功能说明

**更新日期：** 2026-04-06  
**版本：** v1.0

---

## 📋 功能概述

在**用户账号管理**页面中新增了**用户组（User Group）**字段和管理功能，实现更精细化的权限管理和分类授权。

---

## ✨ 新增功能

### 1. 用户组列显示

在用户管理列表中新增"用户组"列，显示每个用户所属的所有用户组：

- ✅ **已分配用户组**：以蓝色徽章形式显示所有组名
- ⚪ **未分配**：显示灰色"未分配"徽章
- ❌ **无账号**：显示"-"

### 2. 快速管理用户组

为每个有账号的用户添加"管理用户组"按钮，点击后弹出模态框：

- 📝 显示当前用户名和已分配的用户组数量
- ☑️ 多选框列表展示所有可用用户组
- 👥 显示每个用户组的成员数量
- 💾 一键保存用户组变更

### 3. 与 Django Admin 后台互通

- 🔗 使用 Django 内置的 `auth.Group` 模型
- 🔄 在用户管理页面修改用户组后，Django Admin 后台同步更新
- 🎯 在 Django Admin 后台修改用户组后，用户管理页面实时反映

---

## 🎯 使用场景

### 场景 1：批量授权

```
需求：给所有项目经理授予合同审批权限

操作：
1. 创建用户组"项目经理"
2. 为该组分配"合同审批"权限
3. 将所有项目经理加入该组
4. 自动获得相应权限
```

### 场景 2：角色分类管理

```
常见用户组示例：
- 超级管理员：系统最高权限
- 管理员：日常管理权限
- 项目经理：项目管理相关权限
- 财务人员：财务模块权限
- 一般用户：基础查看权限
```

### 场景 3：权限快速调整

```
需求：某员工从普通员工晋升为项目经理

操作：
1. 找到该员工
2. 点击"管理用户组"
3. 勾选"项目经理"组
4. 保存即可，无需逐个分配权限
```

---

## 🔧 技术实现

### 后端修改

**文件：** `eims_app/views/views_user_management.py`

#### 1. 获取用户组信息

```python
# 为员工匹配账号状态时，同时获取用户组
user_groups = []
if user:
    user_groups = list(user.groups.all().values_list('name', flat=True))

employee_account_status.append({
    'employee': emp,
    'has_account': user is not None,
    'user': user,
    'user_groups': user_groups,  # 新增
})
```

#### 2. 处理用户组更新

```python
elif action == 'update_user_groups':
    # 更新用户组
    user_id = request.POST.get('user_id')
    group_ids = request.POST.getlist('group_ids')
    
    try:
        from django.contrib.auth.models import Group
        user = User.objects.get(id=user_id)
        
        # 清空现有组
        user.groups.clear()
        
        # 添加新组
        if group_ids:
            groups = Group.objects.filter(id__in=group_ids)
            user.groups.add(*groups)
        
        user.save()
        messages.success(request, f"用户 {user.username} 的用户组更新成功")
    except Exception as e:
        messages.error(request, f"用户组更新失败：{str(e)}")
```

#### 3. 传递用户组列表到模板

```python
# 获取所有用户组（用于下拉选择）
from django.contrib.auth.models import Group
all_groups = Group.objects.all().order_by('name')

context = {
    ...
    'all_groups': all_groups,  # 新增
}
```

### 前端修改

**文件：** `eims_app/templates/eims_app/user_management.html`

#### 1. 表格新增用户组列

```html
<thead>
    <tr>
        ...
        <th>用户组</th>
        <th>操作</th>
    </tr>
</thead>
```

#### 2. 显示用户组徽章

```html
<td>
    {% if item.has_account and item.user_groups %}
        {% for group in item.user_groups %}
        <span class="badge badge-info mr-1">{{ group }}</span>
        {% endfor %}
    {% elif item.has_account %}
        <span class="badge badge-secondary">未分配</span>
    {% else %}
        <span class="text-muted">-</span>
    {% endif %}
</td>
```

#### 3. 添加管理按钮

```html
<button type="button" class="btn btn-sm btn-warning mb-1" 
        onclick="showUpdateGroupsModal({{ item.user.id }}, '{{ item.user.username }}', {{ item.user.groups.all|length }})">
    <i class="fas fa-users-cog"></i> 管理用户组
</button>
```

#### 4. 用户组管理模态框

包含：
- 用户名显示
- 当前用户组数量
- 多选框列表（所有可用用户组）
- 每个用户组的成员数
- 保存/取消按钮

---

## 📊 数据流程

```
用户点击"管理用户组"
    ↓
弹出模态框，显示所有可用用户组
    ↓
用户勾选/取消勾选用户组
    ↓
提交表单（POST）
    ↓
后端处理：
  1. 清空用户现有组
  2. 添加新选中的组
  3. 保存到数据库
    ↓
返回用户管理页面，显示更新后的用户组
    ↓
Django Admin 后台同步更新
```

---

## 🔐 权限说明

### 访问权限

- **查看用户管理页面**：需要超级管理员权限 (`is_superuser`)
- **修改用户组**：需要超级管理员权限

### 用户组权限

用户组的权限在 **Django Admin 后台**配置：

1. 登录 Django Admin：`http://your-domain/admin/`
2. 进入"认证和授权" → "组"
3. 选择或创建用户组
4. 勾选相应的权限
5. 保存

---

## 🎨 UI 设计

### 用户组徽章样式

- **已分配**：蓝色徽章 (`badge-info`)
- **未分配**：灰色徽章 (`badge-secondary`)
- **间距**：`mr-1`（右侧边距）

### 模态框样式

- **标题栏**：黄色背景 (`bg-warning`)
- **图标**：`fa-users-cog`（用户组管理图标）
- **尺寸**：大号模态框 (`modal-lg`)
- **滚动**：用户组列表可滚动（最大高度 300px）

---

## 📝 使用步骤

### 第一步：创建用户组（在 Django Admin 后台）

1. 访问 `/admin/`
2. 进入"认证和授权" → "组"
3. 点击"增加组"
4. 输入组名（如"项目经理"）
5. 勾选该组需要的权限
6. 保存

### 第二步：为用户分配用户组

1. 访问"用户账号管理"页面
2. 找到目标用户
3. 点击"管理用户组"按钮
4. 在弹出的模态框中勾选相应的用户组
5. 点击"保存用户组"
6. 系统提示"用户组更新成功"

### 第三步：验证权限

1. 使用该用户账号登录
2. 检查是否具有相应用户组的权限
3. 如有问题，回到用户管理页面调整

---

## ⚠️ 注意事项

### 1. 用户组与权限的关系

- **用户组**是权限的集合
- 用户加入用户组后，自动获得该组的所有权限
- 一个用户可以属于多个用户组
- 用户的最终权限是所有用户组权限的并集

### 2. 权限冲突处理

如果用户属于多个用户组，且这些组有不同的权限：
- Django 会自动合并所有权限
- 不会发生冲突
- 用户拥有所有组权限的总和

### 3. 移除用户组的影响

- 从用户组移除用户后，该用户立即失去该组的权限
- 如果用户还属于其他组，保留其他组的权限
- 建议先确认用户不再需要该组权限后再移除

### 4. 默认用户组

系统预置的用户组（从备份数据中看到）：
- **超级管理员**：所有权限
- **管理员**：大部分管理权限
- **一般用户**：基础查看权限

---

## 🔍 常见问题

### Q1: 为什么看不到某些用户组？

**A:** 确保已在 Django Admin 后台创建了该用户组。如果没有，请先创建。

### Q2: 用户组修改后不生效？

**A:** 
1. 检查是否成功保存（看提示信息）
2. 让用户重新登录
3. 清除浏览器缓存

### Q3: 如何批量修改多个用户的用户组？

**A:** 目前需要逐个修改。如需批量操作，可以在 Django Admin 后台：
1. 进入"用户"列表
2. 勾选多个用户
3. 选择动作"将用户添加到组"
4. 执行

### Q4: 用户组和角色的区别？

**A:**
- **用户组（Group）**：Django 内置，基于权限集合
- **角色（Role）**：业务概念，可能包含更多业务逻辑
- 本系统使用 Django 内置的用户组进行权限管理

---

## 🚀 未来优化方向

1. **批量操作用户组**：支持一次为多个用户分配相同的用户组
2. **用户组模板**：预设常用的用户组配置，快速应用
3. **用户组继承**：支持用户组之间的继承关系
4. **权限预览**：在分配用户组前，预览该组的具体权限
5. **操作日志**：记录用户组的变更历史

---

## 📞 技术支持

如遇问题，请检查：
1. Django Admin 后台是否正确配置了用户组
2. 用户是否有正确的账号
3. 浏览器控制台是否有 JavaScript 错误
4. 服务器日志是否有错误信息

**祝您使用愉快！** 🎉
