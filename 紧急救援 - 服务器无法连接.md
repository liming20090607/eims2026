# 紧急救援 - 服务器无法连接

## 🚨 当前状况

**症状：**
- 浏览器访问：`http://39.106.41.239:8000/` → ERR_CONNECTION_RESET
- 阿里云 Workbench 登录失败
- SSH 可能也无法连接

**可能原因：**
1. 服务器宕机或重启中
2. 网络服务异常
3. 防火墙/安全组配置变更
4. 服务器过载

---

## 🚑 紧急处理步骤

### 方案一：使用阿里云救援登录（推荐）

#### 步骤 1：登录阿里云控制台

1. 访问：https://www.aliyun.com/
2. 登录您的账号
3. 进入 **控制台**

#### 步骤 2：找到 ECS 实例

1. 点击 **云服务器 ECS**
2. 找到实例：`39.106.41.239`
3. 点击实例 ID

#### 步骤 3：使用 VNC 登录

1. 点击 **远程连接**
2. 选择 **VNC 登录**（不是 Workbench）
3. 如果提示输入 VNC 密码，点击 **发送验证码** 或 **重置 VNC 密码**

#### 步骤 4：检查服务器状态

VNC 登录后（类似直接操作服务器屏幕）：

```bash
# 检查服务器是否运行
uptime

# 检查网络
ping -c 4 www.baidu.com

# 检查 SSH 服务
systemctl status sshd

# 检查 Gunicorn
ps aux | grep gunicorn

# 重启网络服务
systemctl restart network
```

---

### 方案二：重启服务器

如果 VNC 也无法登录，使用阿里云控制台重启：

#### 步骤 1：在实例详情页

1. 点击 **重启** 按钮
2. 确认重启
3. 等待 2-3 分钟

#### 步骤 2：重启后测试

```bash
# 等待 3 分钟后
# 测试 SSH
ssh admin@39.106.41.239

# 测试网页
http://39.106.41.239:8000/
```

---

### 方案三：使用阿里云命令助手

如果服务器安装了云助手：

#### 步骤 1：找到云助手

1. ECS 控制台 → 实例详情
2. 点击 **云助手**
3. 点击 **创建命令**

#### 步骤 2：执行诊断命令

```bash
#!/bin/bash
# 检查服务状态
systemctl status sshd
systemctl status supervisord
ps aux | grep gunicorn
netstat -tlnp | grep 8000

# 重启必要服务
systemctl restart sshd
systemctl restart supervisord
```

#### 步骤 3：查看执行结果

在云助手查看命令输出。

---

## 🔍 网络连通性测试

### 在本地 PowerShell 测试

```powershell
# 测试服务器连通性
Test-NetConnection 39.106.41.239 -Port 22

# 测试 8000 端口
Test-NetConnection 39.106.41.239 -Port 8000

# Ping 测试
ping 39.106.41.239
```

**结果分析：**

| 测试 | 结果 | 说明 |
|------|------|------|
| Ping 失败 | ❌ | 服务器关机或网络问题 |
| 端口 22 失败 | ❌ | SSH 服务未运行 |
| 端口 8000 失败 | ❌ | Gunicorn 未运行 |

---

## 🛠️ VNC 登录后的操作

如果成功通过 VNC 登录：

### 1. 检查基本状态

```bash
# 系统运行时间
uptime

# 内存使用
free -h

# 磁盘空间
df -h

# CPU 负载
top
```

### 2. 检查网络配置

```bash
# 查看 IP 地址
ip addr show

# 查看路由
ip route show

# 测试外网
ping -c 4 8.8.8.8
```

### 3. 重启关键服务

```bash
# 重启网络
systemctl restart network

# 重启 SSH
systemctl restart sshd

# 重启 Supervisor
systemctl restart supervisord

# 重启 Gunicorn
supervisorctl restart all
```

### 4. 查看日志

```bash
# 系统日志
journalctl -xe | tail -n 50

# SSH 日志
tail -n 50 /var/log/secure

# Gunicorn 日志
tail -n 50 /var/log/eims/error.log
```

### 5. 检查防火墙

```bash
# 查看防火墙状态
firewall-cmd --state

# 查看开放端口
firewall-cmd --list-ports

# 临时关闭防火墙（仅测试用）
systemctl stop firewalld
```

---

## 📊 可能的问题及解决方案

### 问题 1：服务器关机

**症状：**
- Ping 不通
- 所有端口连接失败
- 阿里云显示 "已停止" 状态

**解决：**
1. 阿里云控制台 → 实例
2. 点击 **启动**
3. 等待 2-3 分钟

---

### 问题 2：网络服务异常

**症状：**
- 服务器运行但无法连接
- VNC 看到网络错误

**解决：**
```bash
# 重启网络服务
systemctl restart network

# 重启 NetworkManager
systemctl restart NetworkManager

# 检查网络配置
cat /etc/sysconfig/network-scripts/ifcfg-eth0
```

---

### 问题 3：SSH 服务停止

**症状：**
- 端口 22 连接失败
- 其他端口正常

**解决：**
```bash
# 启动 SSH
systemctl start sshd

# 查看 SSH 状态
systemctl status sshd

# 重启 SSH
systemctl restart sshd
```

---

### 问题 4：防火墙问题

**症状：**
- 服务运行但无法连接
- 本地可以访问，远程不行

**解决：**
```bash
# 查看防火墙
firewall-cmd --state

# 临时关闭测试
systemctl stop firewalld

# 如果关闭后可以访问，需要配置规则
systemctl start firewalld
firewall-cmd --permanent --add-port=22/tcp
firewall-cmd --permanent --add-port=8000/tcp
firewall-cmd --reload
```

---

### 问题 5：安全组配置丢失

**症状：**
- 服务器正常
- 防火墙已关闭
- 仍然无法连接

**解决：**
1. 阿里云控制台 → 安全组
2. 检查入方向规则
3. 确保有以下规则：
   - 22/TCP, 0.0.0.0/0
   - 8000/TCP, 0.0.0.0/0

---

## 🚀 一键救援脚本

### 创建本地脚本

文件：[`bat\紧急救援 - 重启服务器.bat`](file://e:\EIMS2026\bat\紧急救援 - 重启服务器.bat)

```batch
@echo off
chcp 65001 >nul
title 紧急救援 - 重启服务器
echo ========================================
echo   紧急救援 - 重启服务器
echo ========================================
echo.
echo 服务器：39.106.41.239
echo.
echo 操作步骤:
echo   1. 打开浏览器
echo   2. 访问：https://ecs.console.aliyun.com/
echo   3. 找到实例 39.106.41.239
echo   4. 点击"重启"按钮
echo   5. 等待 2-3 分钟
echo   6. 测试访问
echo.
echo ========================================
pause
echo.
echo 正在打开阿里云控制台...
start https://ecs.console.aliyun.com/
echo.
echo 请在阿里云控制台重启服务器！
echo 重启后等待 3 分钟，然后测试访问
echo.
pause
```

---

## ✅ 重启后验证

### 步骤 1：等待服务器完全启动

```
重启后至少等待 3 分钟
```

### 步骤 2：测试连通性

```powershell
# PowerShell 测试
ping 39.106.41.239
Test-NetConnection 39.106.41.239 -Port 22
Test-NetConnection 39.106.41.239 -Port 8000
```

### 步骤 3：SSH 登录

```bash
ssh admin@39.106.41.239
```

### 步骤 4：检查服务

```bash
sudo su -

# 检查 Supervisor
systemctl status supervisord

# 检查 Gunicorn
supervisorctl status eims

# 启动服务（如果需要）
systemctl start supervisord
supervisorctl start eims
```

### 步骤 5：测试网页

```bash
# 本地测试
curl http://localhost:8000/

# 远程测试
# 浏览器访问：http://39.106.41.239:8000/
```

---

## 📞 立即执行建议

### 推荐顺序：

**1️⃣ 首先尝试 VNC 登录**
- 使用阿里云 VNC（不是 Workbench）
- 检查服务器状态
- 重启必要服务

**2️⃣ 如果 VNC 不行，重启服务器**
- 阿里云控制台 → 重启
- 等待 3 分钟
- 测试连接

**3️⃣ 重启后仍不行，检查配置**
- 安全组规则
- 防火墙配置
- 服务状态

---

## 📝 快速操作清单

- [ ] 尝试阿里云 VNC 登录
- [ ] 检查服务器运行状态
- [ ] 检查网络连通性
- [ ] 重启必要服务
- [ ] 如果不行，重启服务器
- [ ] 重启后等待 3 分钟
- [ ] 测试 SSH 连接
- [ ] 测试网页访问
- [ ] 检查安全组配置
- [ ] 启动所有服务

---

**紧急程度：** 🔴 高  
**建议操作：** 立即使用阿里云 VNC 登录或重启服务器  
**预计恢复时间：** 5-10 分钟
