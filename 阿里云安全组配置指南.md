# 阿里云安全组配置指南

## 🎯 从您的截图看

**好消息：**
- ✅ Gunicorn 已经启动
- ✅ 端口 8000 正在监听
- ✅ 能访问到服务器（Internal Server Error 说明服务器有响应）

**问题：**
- Internal Server Error 可能是 Django 配置问题
- 但也可能是安全组配置不完整

---

## 🛡️ 安全组位置（根据您的截图）

### 方法 1：轻量应用服务器控制台

**从您的截图看，您使用的是"轻量应用服务器"：**

1. **当前页面：** 服务器概览页
2. **左侧菜单：** 找到 **"防火墙"** 或 **"安全防护"**
3. **点击进入：** 配置防火墙规则

---

### 方法 2：传统 ECS 控制台

**如果是 ECS 实例：**

1. **访问：** https://ecs.console.aliyun.com/
2. **左侧菜单：** 网络和安全组 → 安全组
3. **找到您的实例：** 39.106.41.239
4. **配置规则**

---

## ✅ 配置步骤（轻量应用服务器）

### 步骤 1：进入防火墙页面

**在您的截图中：**
- 左侧菜单有 **"防火墙"** 选项
- 点击它

### 步骤 2：添加入站规则

**添加以下规则：**

| 规则名称 | 协议 | 端口 | 授权对象 |
|---------|------|------|---------|
| Django | TCP | 8000 | 0.0.0.0/0 |

**操作：**
1. 点击 **"添加规则"** 或 **"创建规则"**
2. 填写：
   - 规则名称：Django 或 EIMS
   - 协议：TCP
   - 端口：8000
   - 授权对象：0.0.0.0/0（允许所有 IP）
3. 点击 **"确定"** 或 **"保存"**

---

## 🔍 但更重要的是...

### Internal Server Error 的真正原因

**看到 Internal Server Error，说明：**
1. ✅ 网络连接正常（能访问到服务器）
2. ✅ Gunicorn 正常运行
3. ❌ Django 应用内部有错误

**需要查看错误日志！**

---

## 📋 立即操作

### 步骤 1：上传错误检查脚本

**双击：**
```
E:\EIMS2026\bat\upload-check-django-error.bat
```

---

### 步骤 2：SSH 登录

**输入：**
```bash
ssh root@39.106.41.239
```

---

### 步骤 3：检查错误日志

**输入：**
```bash
bash /tmp/check-django-error.sh
```

**这个脚本会显示：**
- ✅ Gunicorn 错误日志
- ✅ Supervisor 日志
- ✅ 当前进程状态
- ✅ 端口监听状态

---

### 步骤 4：复制输出给我

**执行后：**
1. 选中所有输出
2. Ctrl+C 复制
3. 粘贴给我

**我会根据错误日志提供解决方案！**

---

## 🎯 可能的错误原因

### 1. Django Settings 配置错误

```python
# settings.py 可能有问题
ALLOWED_HOSTS = ['*']  # 或者需要添加 IP
DEBUG = False  # 生产环境应该 False
```

### 2. 数据库连接失败

```python
# settings.py 数据库配置
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'HOST': 'localhost',
        'NAME': 'eims',
        'USER': 'root',
        'PASSWORD': 'xxx',
    }
}
```

### 3. 缺少其他依赖

```bash
# 可能需要安装其他包
pip install -r requirements.txt
```

### 4. 静态文件问题

```bash
# 收集静态文件
python manage.py collectstatic
```

---

## 📊 安全组配置检查清单

**检查以下端口是否开放：**

| 端口 | 用途 | 状态 |
|------|------|------|
| 22 | SSH | ✅ 已开放 |
| 8000 | Django | ❓ 需要开放 |
| 80 | HTTP | ❓ 可选 |
| 443 | HTTPS | ❓ 可选 |

---

## 💡 立即操作建议

**优先级：**

1. **第一步：** 查看错误日志（最重要！）
   - 执行：`bash /tmp/check-django-error.sh`
   - 复制输出给我

2. **第二步：** 配置安全组
   - 左侧菜单 → 防火墙
   - 添加端口 8000

3. **第三步：** 测试访问
   - 浏览器访问：http://39.106.41.239:8000/admin/
   - 按 Ctrl+F5

---

## 📁 我已创建的工具

### 检查工具
- ✅ [`bat\check-django-error.sh`](file://e:\EIMS2026\bat\check-django-error.sh) - 检查 Django 错误
- ✅ [`bat\upload-check-django-error.bat`](file://e:\EIMS2026\bat\upload-check-django-error.bat) - 自动上传

### 说明文档
- **阿里云安全组配置指南.md** - 本文档

---

**现在先执行错误检查脚本，看看 Django 为什么报错！**

**然后配置防火墙，添加端口 8000！**

---

**预计时间：** 2-3 分钟  
**难度：** ⭐⭐（需要查看错误日志）

---

**最后更新：** 2026-04-03  
**服务器：** 39.106.41.239
