# 表头右键菜单功能说明

## 功能概述

为造价咨询模块的所有列表页面添加了表头右键菜单功能，提供三个便捷操作选项。

---

## 右键菜单选项

### 1. ❌ 取消本字段排序

**功能**：从当前排序条件中移除该字段

**使用场景**：
- 多字段排序时，想移除某个字段的排序
- 误点了某个字段的排序，想取消

**操作效果**：
- 该字段从排序列表中移除
- 其他字段的排序保持不变
- 优先级数字自动重新调整
- 页面自动刷新显示新排序结果

**示例**：
```
当前排序：项目名称(1) → 项目编号(2) → 项目状态(3)
右键点击"项目编号" → 选择"取消本字段排序"
结果排序：项目名称(1) → 项目状态(2)
```

---

### 2. 🔍 按此字段筛选

**功能**：打开筛选对话框，为该字段添加筛选条件

**使用场景**：
- 只想查看特定委托单位的项目
- 筛选特定状态的项目
- 查找包含关键词的项目名称

**操作步骤**：
1. 右键点击表头字段（如"委托单位"）
2. 选择"按此字段筛选"
3. 在弹出的对话框中：
   - 选择筛选条件（包含/等于/开头是/结尾是/不包含）
   - 输入筛选值
4. 点击"应用筛选"

**支持的筛选条件**：
- **包含**：字段中包含指定文本（不区分大小写）
- **等于**：字段完全等于指定值
- **开头是**：字段以指定文本开头
- **结尾是**：字段以指定文本结尾
- **不包含**：字段中不包含指定文本

**示例**：
```
字段：委托单位
条件：包含
值：科技
结果：显示所有委托单位名称中包含"科技"的项目
```

---

### 3. 🗑️ 取消此字段筛选

**功能**：移除该字段的筛选条件

**使用场景**：
- 之前设置了筛选，现在想查看所有数据
- 筛选条件不再需要

**操作效果**：
- 该字段的筛选条件被清除
- 其他字段的筛选条件保持不变
- 页面自动刷新显示更多数据

---

## 技术实现

### 前端实现

**文件**：`eims_app/templates/cost_consulting/project_info/list.html`

**关键功能**：

1. **右键事件监听**
```javascript
th.addEventListener('contextmenu', function(e) {
    const field = this.dataset.field;
    if (field) {
        showContextMenu(e, field, this);
    }
});
```

2. **菜单显示逻辑**
- 智能定位：确保菜单不超出视口边界
- 动画效果：淡入动画提升用户体验
- 自动关闭：点击其他地方或按ESC键关闭

3. **筛选对话框**
- Bootstrap Modal 组件
- 动态生成字段名称
- 支持多种筛选操作符

### 后端实现

**文件**：`eims_app/views/views_cost_sub_modules.py`

**动态筛选处理**：
```python
# 遍历所有 GET 参数，查找 filter_ 开头的参数
for param_key in request.GET.keys():
    if param_key.startswith('filter_') and not param_key.endswith('_op'):
        field_name = param_key.replace('filter_', '', 1)
        operator = request.GET.get(f'{param_key}_op', 'contains')
        filter_value = request.GET.get(param_key, '').strip()
        
        # 根据操作符构建查询
        if operator == 'contains':
            queryset = queryset.filter(**{f'{field_name}__icontains': filter_value})
        # ... 其他操作符
```

**URL 参数格式**：
```
?filter_project_name=科技&filter_project_name_op=contains
```

---

## 用户界面

### 右键菜单样式

```
┌─────────────────────┐
│ ❌ 取消本字段排序    │
├─────────────────────┤
│ 🔍 按此字段筛选      │
│ 🗑️ 取消此字段筛选    │
└─────────────────────┘
```

**特点**：
- 白色背景，圆角边框
- 阴影效果突出显示
- 悬停时高亮（蓝色文字）
- 第一项下方有分隔线
- 淡入动画效果

### 筛选对话框

```
┌──────────────────────────┐
│ 筛选: 委托单位       [×] │
├──────────────────────────┤
│                          │
│ 筛选条件                 │
│ ┌──────────────────────┐ │
│ │ 包含              ▼  │ │
│ └──────────────────────┘ │
│                          │
│ 筛选值                   │
│ ┌──────────────────────┐ │
│ │ 输入筛选值...        │ │
│ └──────────────────────┘ │
│                          │
├──────────────────────────┤
│          [取消] [应用筛选]│
└──────────────────────────┘
```

---

## 使用示例

### 示例 1：多字段排序管理

**场景**：当前按5个字段排序，想移除第3个字段

**操作**：
1. 看到表头显示：项目状态(3)
2. 右键点击"项目状态"表头
3. 选择"❌ 取消本字段排序"
4. 页面刷新，项目状态从排序中移除
5. 其他字段优先级自动调整：(1)(2)(4)(5) → (1)(2)(3)(4)

---

### 示例 2：快速筛选委托单位

**场景**：只想查看"某某科技公司"的项目

**操作**：
1. 右键点击"委托单位"表头
2. 选择"🔍 按此字段筛选"
3. 筛选条件选择"包含"
4. 输入值："某某科技"
5. 点击"应用筛选"
6. 列表只显示委托单位包含"某某科技"的项目

---

### 示例 3：组合使用排序和筛选

**场景**：查看"科技公司"的项目，按金额降序排列

**操作**：
1. 右键点击"委托单位" → "按此字段筛选" → 输入"科技" → 应用
2. 左键点击"审定金额"表头（第一次点击升序）
3. 再次点击"审定金额"表头（切换为降序）
4. 结果：显示科技公司项目，按金额从高到低排序

---

### 示例 4：清除筛选

**场景**：之前筛选了项目类型，现在想查看所有类型

**操作**：
1. 右键点击"项目类型"表头
2. 选择"🗑️ 取消此字段筛选"
3. 页面刷新，显示所有项目类型的数据

---

## 键盘快捷键

- **ESC 键**：关闭右键菜单
- **点击空白处**：关闭右键菜单
- **点击菜单项**：执行对应操作并关闭菜单

---

## 注意事项

### 1. 浏览器兼容性

- ✅ Chrome/Edge：完全支持
- ✅ Firefox：完全支持
- ✅ Safari：完全支持
- ⚠️ IE11：不支持（已停止支持）

### 2. 触摸设备

在平板或手机上：
- 长按表头可触发右键菜单
- 部分设备可能需要双击

### 3. 性能考虑

- 筛选条件过多可能影响查询性能
- 建议同时使用的筛选条件不超过5个
- 大数据量时使用"等于"比"包含"更快

### 4. 字段限制

以下字段类型支持筛选：
- ✅ 文本字段（CharField）
- ✅ 日期字段（DateField）
- ✅ 数字字段（DecimalField, IntegerField）
- ✅ 选择字段（choices）

---

## 已应用的模板

以下6个造价咨询模块的列表页面都已添加右键菜单功能：

1. ✅ `project_info/list.html` - 项目信息
2. ✅ `task_implementation/list.html` - 任务实施
3. ✅ `review_result/list.html` - 审核结果
4. ✅ `payment_status/list.html` - 付款状态
5. ✅ `project_archive/list.html` - 项目归档
6. ✅ `remuneration_distribution/list.html` - 酬劳分配

---

## 故障排除

### 问题 1：右键菜单不显示

**可能原因**：
- 浏览器缓存旧版本 JavaScript
- JavaScript 错误

**解决方法**：
1. 硬刷新页面（Ctrl+F5）
2. 打开浏览器控制台（F12）检查错误
3. 尝试无痕模式

---

### 问题 2：筛选后没有结果

**可能原因**：
- 筛选值输入错误
- 筛选条件太严格
- 确实没有匹配的数据

**解决方法**：
1. 检查筛选值是否正确
2. 尝试更宽松的条件（如"包含"代替"等于"）
3. 取消筛选查看所有数据

---

### 问题 3：取消排序后顺序不对

**可能原因**：
- 还有其他字段在排序
- 默认排序生效

**解决方法**：
1. 检查其他表头的优先级数字
2. 理解多字段排序逻辑
3. 如需重置，刷新页面（恢复默认排序）

---

## 未来扩展

可能的增强功能：

1. **批量操作**
   - 全选/反选筛选条件
   - 一键清除所有筛选

2. **高级筛选**
   - 范围筛选（金额区间、日期区间）
   - 多值筛选（同时筛选多个委托单位）
   - 正则表达式筛选

3. **保存筛选**
   - 保存常用筛选条件
   - 快速切换不同筛选方案

4. **筛选历史**
   - 记录最近使用的筛选
   - 快速恢复之前的筛选

---

*功能添加日期：2026年3月21日*
*Django 版本：4.2.7*
*Python 版本：3.14*
