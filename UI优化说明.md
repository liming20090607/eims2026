# 人员花名册页面 UI 优化说明

## 修改时间
2026-03-21

## 修改文件
`eims_app/templates/personnel/list.html`

## 优化内容

### 1. ✅ 移除顶部统计卡片
- **删除内容**：移除了"总人数"和"在岗人数"两个统计卡片
- **影响**：节省了约 80px 的垂直空间，使页面更紧凑

### 2. ✅ 移除搜索框标签
- **删除内容**：
  - 移除了"关键词："标签
  - 移除了"部门："标签
- **保留内容**：输入框和下拉框的 placeholder 文本仍然保留
- **效果**：筛选表单更加简洁，节省水平空间

### 3. ✅ 调整按钮位置
- **原布局**：操作按钮在卡片头部（card-header），与筛选表单分离
- **新布局**：所有操作按钮移到筛选表单内，与搜索框在同一行
- **按钮顺序**：
  1. 可视化分配
  2. 导入
  3. 导出
  4. 打印
  5. 关键词搜索框
  6. 部门下拉框
  7. 搜索按钮（带"搜索"文字）
  8. 重置按钮

### 4. ✅ 优化间距，最大化视口显示
- **容器 padding**：从 `py-3` 改为 `py-2`（减少垂直内边距）
- **固定顶部区域 padding-bottom**：从 `1rem` 改为 `0.5rem`
- **筛选卡片 margin**：
  - `margin-top`：从 `0.125rem` 改为 `0`（紧贴顶部）
  - `margin-bottom`：从 `0.125rem` 改为 `0.25rem`
- **表格容器 padding**：从 `0.5rem` 改为 `0.25rem`（最小化内边距）
- **数据表格最大高度**：从 `calc(100vh - 320px)` 改为 `calc(100vh - 230px)`
  - 增加了约 90px 的表格显示空间

### 5. ✅ 简化卡片结构
- **删除 card-header**：移除了包含"人员信息"标题和操作按钮的卡片头部
- **直接使用 card-body**：筛选表单直接放在 card-body 中，减少嵌套层级
- **减小 card-body padding**：使用 `py-2` 类，进一步压缩垂直空间

## 视觉效果对比

### 修改前
```
┌─────────────────────────────────────┐
│ [总人数: XX] [在岗人数: XX]         │ ← 统计卡片（已删除）
├─────────────────────────────────────┤
│ 人员信息              [按钮组...]   │ ← 卡片头部（已删除）
├─────────────────────────────────────┤
│ 关键词: [___] 部门: [___] [搜索]    │ ← 带标签的筛选表单
├─────────────────────────────────────┤
│                                     │
│         数据表格区域                 │
│      （较小显示空间）                │
│                                     │
└─────────────────────────────────────┘
```

### 修改后
```
┌─────────────────────────────────────┐
│ [可视化][导入][导出][打印]           │ ← 按钮与筛选框同行
│ [关键词___] [部门___] [搜索] [重置]  │ ← 无标签，更紧凑
├─────────────────────────────────────┤
│                                     │
│         数据表格区域                 │
│    （增加约 90px 显示空间）          │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

## 技术细节

### CSS 修改
1. `.fixed-top-section` padding-bottom: `1rem` → `0.5rem`
2. `.filter-card` margin-top: `0.125rem` → `0`
3. `.filter-card` margin-bottom: `0.125rem` → `0.25rem`
4. `.data-table-card` max-height: `calc(100vh - 320px)` → `calc(100vh - 230px)`
5. `.table-responsive` padding: `0.5rem` → `0.25rem`

### HTML 结构修改
1. 删除整个统计卡片区域（stats-card-container）
2. 删除卡片头部（card-header）及其中的标题和按钮组
3. 将按钮组移动到筛选表单内部
4. 移除 label 元素，只保留 input 和 select
5. 为搜索按钮添加"搜索"文字
6. 添加重置按钮

## 用户体验提升

1. **更多数据显示**：表格区域增加约 90px 高度，可多显示 3-4 行数据
2. **更简洁界面**：去除冗余标签和统计卡片，界面更清爽
3. **操作更高效**：所有筛选和操作用具在一行，减少鼠标移动距离
4. **响应式友好**：在小屏幕上也能更好地利用有限空间

## 注意事项

- 统计功能仍然在后端计算，只是不在前端显示
- 如需恢复统计卡片，可以取消注释相关 HTML 代码
- 所有按钮功能保持不变，只是位置调整
- 移动端响应式布局仍然有效
