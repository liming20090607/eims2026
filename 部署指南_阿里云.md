# Django 项目阿里云部署完整指南

## 📋 部署前准备

### 1. 阿里云服务器配置
- **操作系统**：推荐 Ubuntu 22.04 LTS 或 CentOS 7.9
- **最低配置**：1 核 2GB（建议 2 核 4GB）
- **存储**：40GB 起
- **网络**：按量付费或包年包月

### 2. 安全组配置（重要！）
在阿里云控制台配置安全组规则：
- **80 端口**：HTTP 访问
- **443 端口**：HTTPS 访问（可选）
- **22 端口**：SSH 连接
- **8000 端口**：Django 开发服务器（仅测试用，生产环境不推荐）

---

## 🚀 部署步骤

### 第一步：连接服务器

```bash
# Windows PowerShell 或 CMD
ssh root@你的服务器公网 IP

# 输入密码登录
```

### 第二步：安装基础软件

#### Ubuntu/Debian 系统：
```bash
# 更新软件包
sudo apt update
sudo apt upgrade -y

# 安装 Python3、pip3、Git、Nginx、MySQL
sudo apt install -y python3 python3-pip python3-venv git nginx mysql-server

# 安装 MySQL 开发库
sudo apt install -y libmysqlclient-dev
```

#### CentOS/RHEL 系统：
```bash
# 安装 EPEL 仓库
sudo yum install -y epel-release

# 安装 Python3、pip3、Git、Nginx、MySQL
sudo yum install -y python3 python3-pip git nginx mysql mysql-devel

# 安装 MySQL 开发库
sudo yum install -y mysql-devel gcc
```

### 第三步：上传项目代码

#### 方法 1：使用 Git（推荐）
```bash
# 在服务器上创建项目目录
sudo mkdir -p /var/www/eims
sudo chown -R $USER:$USER /var/www/eims
cd /var/www/eims

# 克隆代码（如果有 Git 仓库）
git clone <你的 Git 仓库地址> .
```

#### 方法 2：使用 SCP 上传
```bash
# 在本地 PowerShell 执行
scp -r e:\EIMS2026\* root@你的服务器 IP:/var/www/eims/
```

#### 方法 3：使用 FTP 工具
- 使用 FileZilla 或 WinSCP 上传整个项目到 `/var/www/eims/`

### 第四步：配置 Python 虚拟环境

```bash
cd /var/www/eims

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt

# 额外安装生产环境依赖
pip install gunicorn mysqlclient
```

### 第五步：配置数据库

#### 选项 A：使用 MySQL（推荐）
```bash
# 登录 MySQL
sudo mysql -u root -p

# 创建数据库和用户
mysql> CREATE DATABASE eims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
mysql> CREATE USER 'eims_user'@'localhost' IDENTIFIED BY '你的强密码';
mysql> GRANT ALL PRIVILEGES ON eims.* TO 'eims_user'@'localhost';
mysql> FLUSH PRIVILEGES;
mysql> EXIT;
```

修改 `settings.py`：
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'eims',
        'USER': 'eims_user',
        'PASSWORD': '你的强密码',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
        },
    }
}
```

#### 选项 B：继续使用 SQLite（不推荐生产环境）
```bash
# 确保数据库文件权限正确
sudo chown -R www-data:www-data /var/www/eims/db.sqlite3
sudo chmod 640 /var/www/eims/db.sqlite3
```

### 第六步：配置环境变量

创建 `.env` 文件：
```bash
cd /var/www/eims
nano .env
```

添加以下内容：
```env
# 生产环境配置
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=你的超长随机密钥（至少 50 字符）
DJANGO_ALLOWED_HOSTS=你的域名，服务器 IP

# 数据库配置（如果使用 MySQL）
DB_NAME=eims
DB_USER=eims_user
DB_PASSWORD=你的强密码
DB_HOST=localhost
DB_PORT=3306

# 媒体文件配置
MEDIA_ROOT=/var/www/eims/media
STATIC_ROOT=/var/www/eims/staticfiles
```

生成随机密钥：
```bash
python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### 第七步：数据库迁移

```bash
cd /var/www/eims
source venv/bin/activate

# 执行迁移
python manage.py makemigrations
python manage.py migrate

# 收集静态文件
python manage.py collectstatic --noinput

# 创建超级管理员
python manage.py createsuperuser
```

### 第八步：配置 Gunicorn

创建 Gunicorn 配置文件：
```bash
nano /var/www/eims/gunicorn.conf.py
```

添加配置：
```python
bind = "127.0.0.1:8000"
workers = 3
accesslog = "/var/log/gunicorn/access.log"
errorlog = "/var/log/gunicorn/error.log"
loglevel = "info"
```

创建日志目录：
```bash
sudo mkdir -p /var/log/gunicorn
sudo chown -R $USER:$USER /var/log/gunicorn
```

### 第九步：配置 Systemd 服务

创建服务文件：
```bash
sudo nano /etc/systemd/system/eims.service
```

添加以下内容：
```ini
[Unit]
Description=EIMS Django Application
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/var/www/eims
ExecStart=/var/www/eims/venv/bin/gunicorn --config gunicorn.conf.py wsgi:application

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
# 重载 systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start eims
sudo systemctl enable eims

# 查看状态
sudo systemctl status eims
```

### 第十步：配置 Nginx

创建 Nginx 配置文件：
```bash
sudo nano /etc/nginx/sites-available/eims
```

添加配置：
```nginx
server {
    listen 80;
    server_name 你的域名 或 服务器 IP;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /var/www/eims/staticfiles/;
    }

    location /media/ {
        alias /var/www/eims/media/;
    }

    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
    }
}
```

启用站点：
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/eims /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

### 第十一步：配置防火墙

#### Ubuntu (UFW)：
```bash
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
```

#### CentOS (firewalld)：
```bash
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --reload
```

### 第十二步：配置 SSL 证书（推荐）

使用 Let's Encrypt 免费证书：
```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx  # Ubuntu
sudo yum install -y certbot python3-certbot-nginx  # CentOS

# 获取证书
sudo certbot --nginx -d 你的域名

# 自动续期测试
sudo certbot renew --dry-run
```

---

## 🔧 常见问题解决

### 1. 权限问题
```bash
# 设置正确的文件权限
sudo chown -R www-data:www-data /var/www/eims
sudo chmod -R 755 /var/www/eims
sudo chmod 644 /var/www/eims/.env
```

### 2. 静态文件 404
```bash
# 重新收集静态文件
source venv/bin/activate
python manage.py collectstatic --clear --noinput
sudo systemctl restart nginx
```

### 3. 数据库连接失败
```bash
# 检查 MySQL 状态
sudo systemctl status mysql

# 检查数据库配置
python manage.py dbshell
```

### 4. Gunicorn 启动失败
```bash
# 查看日志
sudo tail -f /var/log/gunicorn/error.log

# 检查端口占用
sudo netstat -tulpn | grep 8000
```

### 5. Nginx 502 错误
```bash
# 检查 Gunicorn 是否运行
sudo systemctl status eims

# 重启服务
sudo systemctl restart eims
sudo systemctl restart nginx
```

---

## 📊 部署后检查清单

- [ ] 网站可以通过 HTTP 访问
- [ ] 静态文件（CSS/JS）正常加载
- [ ] 媒体文件（上传的文件）正常访问
- [ ] 数据库连接正常
- [ ] 管理员后台可以登录
- [ ] HTTPS 证书配置成功
- [ ] 防火墙配置正确
- [ ] 服务开机自启动
- [ ] 日志文件正常记录
- [ ] 数据库定期备份

---

## 🔐 安全建议

1. **修改 ALLOWED_HOSTS**：
   ```python
   ALLOWED_HOSTS = ['你的域名', '服务器 IP']
   ```

2. **设置强密码**：
   - 数据库密码至少 16 位
   - Django SECRET_KEY 至少 50 位

3. **关闭 DEBUG**：
   ```python
   DEBUG = False
   ```

4. **定期备份数据库**：
   ```bash
   mysqldump -u eims_user -p eims > backup_$(date +%Y%m%d).sql
   ```

5. **更新系统**：
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

6. **配置日志轮转**：
   ```bash
   sudo nano /etc/logrotate.d/gunicorn
   ```

---

## 📞 监控和维护

### 查看日志
```bash
# Django 应用日志
sudo tail -f /var/log/gunicorn/error.log

# Nginx 日志
sudo tail -f /var/log/nginx/error.log

# 系统日志
sudo journalctl -u eims -f
```

### 性能监控
```bash
# 安装监控工具
sudo apt install -y htop iotop nethogs

# 查看资源使用
htop
```

### 数据库备份脚本
```bash
#!/bin/bash
# backup.sh
BACKUP_DIR="/var/backups/eims"
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u eims_user -p'密码' eims > $BACKUP_DIR/backup_$DATE.sql
# 保留最近 30 天的备份
find $BACKUP_DIR -name "backup_*.sql" -mtime +30 -delete
```

---

## 🎉 部署完成！

访问：http://你的域名 或 http://服务器 IP

管理员后台：http://你的域名/admin

---

**需要帮助？**
- Django 文档：https://docs.djangoproject.com/
- Gunicorn 文档：https://docs.gunicorn.org/
- Nginx 文档：https://nginx.org/
