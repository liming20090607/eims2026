# EIMS 阿里云服务器部署指南

## 📋 部署方式

### 方式 1：从 Gitee 克隆（推荐）

**优点**：
- 国内访问速度快
- 操作简单
- 易于更新

---

## 🚀 部署步骤

### 第 1 步：准备服务器

**SSH 登录服务器**：
```bash
ssh root@你的服务器 IP
```

**或**

**使用阿里云控制台**：
1. 登录阿里云控制台
2. ECS 云服务器 → 实例
3. 点击"远程连接"
4. 输入密码登录

---

### 第 2 步：安装必要软件

**1. 安装 Python 3.14**

```bash
# 安装依赖
sudo yum update -y
sudo yum install -y gcc openssl-devel bzip2-devel libffi-devel zlib-devel

# 下载 Python 3.14
cd /usr/local/src
wget https://www.python.org/ftp/python/3.14.3/Python-3.14.3.tgz

# 解压编译
tar -xzf Python-3.14.3.tgz
cd Python-3.14.3
./configure --enable-optimizations
make -j$(nproc)
sudo make altinstall

# 验证
python3.14 --version
```

**2. 安装 Git**

```bash
sudo yum install -y git
```

**3. 安装 Nginx**

```bash
sudo yum install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

**4. 安装 Supervisor**

```bash
sudo yum install -y epel-release
sudo yum install -y supervisor
sudo systemctl start supervisor
sudo systemctl enable supervisor
```

---

### 第 3 步：克隆项目代码

```bash
# 创建应用目录
mkdir -p /var/www/eims
cd /var/www/eims

# 从 Gitee 克隆
git clone https://gitee.com/liming20090607/eims2026.git .

# 或从 GitHub 克隆
# git clone https://github.com/liming20090607/eims2026.git .
```

---

### 第 4 步：配置 Python 虚拟环境

```bash
# 创建虚拟环境
python3.14 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

---

### 第 5 步：配置数据库

**1. 安装 MySQL**

```bash
# 下载 MySQL YUM 源
wget https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm
sudo yum localinstall -y mysql80-community-release-el7-3.noarch.rpm

# 安装 MySQL
sudo yum install -y mysql-community-server

# 启动 MySQL
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 获取临时密码
sudo grep 'temporary password' /var/log/mysqld.log
```

**2. 配置 MySQL**

```bash
# 登录 MySQL
mysql -u root -p

# 修改密码（替换 YourPassword）
ALTER USER 'root'@'localhost' IDENTIFIED BY 'YourPassword';

# 创建数据库
CREATE DATABASE eims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建用户
CREATE USER 'eims'@'localhost' IDENTIFIED BY 'EimsPassword2026';

# 授权
GRANT ALL PRIVILEGES ON eims.* TO 'eims'@'localhost';
FLUSH PRIVILEGES;

# 退出
EXIT;
```

**3. 修改项目配置**

编辑 `settings.py`：

```python
# 数据库配置
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'eims',
        'USER': 'eims',
        'PASSWORD': 'EimsPassword2026',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
            'charset': 'utf8mb4',
        },
    }
}

# 允许的主机
ALLOWED_HOSTS = ['你的服务器 IP', 'localhost', '127.0.0.1']

# 静态文件
STATIC_ROOT = '/var/www/eims/staticfiles/'

# 媒体文件
MEDIA_ROOT = '/var/www/eims/media/'

# 安全设置
DEBUG = False  # 生产环境必须关闭
```

---

### 第 6 步：数据库迁移

```bash
# 激活虚拟环境
source venv/bin/activate

# 进入项目目录
cd /var/www/eims

# 执行迁移
python manage.py makemigrations
python manage.py migrate

# 创建超级用户
python manage.py createsuperuser

# 收集静态文件
python manage.py collectstatic --noinput
```

---

### 第 7 步：配置 Gunicorn

**安装 Gunicorn**：
```bash
pip install gunicorn
```

**创建 Gunicorn 配置文件** `/var/www/eims/gunicorn.conf.py`：

```python
bind = "127.0.0.1:8000"
workers = 3
worker_class = "sync"
timeout = 120
accesslog = "/var/log/gunicorn/access.log"
errorlog = "/var/log/gunicorn/error.log"
loglevel = "info"
```

**创建日志目录**：
```bash
sudo mkdir -p /var/log/gunicorn
sudo chown -R $USER:$USER /var/log/gunicorn
```

---

### 第 8 步：配置 Supervisor

**创建 Supervisor 配置文件** `/etc/supervisor.d/eims.conf`：

```ini
[program:eims]
command=/var/www/eims/venv/bin/gunicorn -c /var/www/eims/gunicorn.conf.py wsgi:application
directory=/var/www/eims
user=root
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/gunicorn/supervisor.log
```

**启动服务**：
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start eims
sudo supervisorctl status eims
```

---

### 第 9 步：配置 Nginx

**创建 Nginx 配置文件** `/etc/nginx/conf.d/eims.conf`：

```nginx
server {
    listen 80;
    server_name 你的服务器 IP;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /var/www/eims/staticfiles/;
    }

    location /media/ {
        alias /var/www/eims/media/;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**测试并重载 Nginx**：
```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

### 第 10 步：配置防火墙

**阿里云安全组配置**：

1. 登录阿里云控制台
2. 云服务器 ECS → 安全组
3. 添加入方向规则：
   - 端口：80（HTTP）
   - 端口：443（HTTPS，可选）
   - 授权对象：0.0.0.0/0

**系统防火墙**：
```bash
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

---

### 第 11 步：验证部署

**1. 访问网站**

浏览器访问：`http://你的服务器 IP`

**2. 访问管理后台**

浏览器访问：`http://你的服务器 IP/admin`

使用创建的超级用户登录。

---

## 🔄 更新部署

**从 Gitee 更新代码**：

```bash
cd /var/www/eims

# 停止服务
sudo supervisorctl stop eims

# 拉取最新代码
git pull origin master

# 激活虚拟环境
source venv/bin/activate

# 安装新依赖
pip install -r requirements.txt

# 数据库迁移
python manage.py makemigrations
python manage.py migrate

# 收集静态文件
python manage.py collectstatic --noinput

# 重启服务
sudo supervisorctl start eims
```

---

## 📝 常见问题

### 1. Python 版本问题

**错误**：`python3.14: command not found`

**解决**：
```bash
# 检查安装
ls -la /usr/local/bin/python3.14

# 创建软链接
sudo ln -s /usr/local/bin/python3.14 /usr/bin/python3.14
```

### 2. 数据库连接失败

**错误**：`Can't connect to MySQL server`

**解决**：
```bash
# 检查 MySQL 状态
sudo systemctl status mysqld

# 重启 MySQL
sudo systemctl restart mysqld
```

### 3. 静态文件 404

**解决**：
```bash
# 重新收集静态文件
python manage.py collectstatic --noinput

# 检查 Nginx 配置
sudo nginx -t
sudo systemctl reload nginx
```

### 4. Gunicorn 启动失败

**查看日志**：
```bash
tail -f /var/log/gunicorn/error.log
```

**检查端口占用**：
```bash
netstat -tlnp | grep 8000
```

---

## 🔒 安全建议

### 1. 配置 HTTPS

**使用 Let's Encrypt 免费证书**：

```bash
# 安装 Certbot
sudo yum install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d 你的域名

# 自动续期
sudo certbot renew --dry-run
```

### 2. 配置防火墙

```bash
# 只开放必要端口
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 3. 定期备份

**数据库备份**：
```bash
# 创建备份脚本
cat > /var/www/backup_db.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/var/backups/eims"
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u eims -p'EimsPassword2026' eims > $BACKUP_DIR/eims_$DATE.sql
find $BACKUP_DIR -name "*.sql" -mtime +7 -delete
EOF

# 设置权限
chmod +x /var/www/backup_db.sh

# 添加定时任务
crontab -e
# 每天凌晨 2 点备份
0 2 * * * /var/www/backup_db.sh
```

---

## 📊 监控和维护

### 1. 查看日志

**应用日志**：
```bash
tail -f /var/log/gunicorn/error.log
tail -f /var/log/gunicorn/access.log
```

**Nginx 日志**：
```bash
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/access.log
```

### 2. 监控服务状态

```bash
# 查看 Supervisor 状态
sudo supervisorctl status

# 查看 Nginx 状态
sudo systemctl status nginx

# 查看 MySQL 状态
sudo systemctl status mysqld
```

### 3. 性能优化

**优化 Gunicorn**：
```python
# gunicorn.conf.py
workers = 4  # CPU 核心数 * 2 + 1
worker_connections = 1000
threads = 2
```

**优化 Nginx**：
```nginx
# /etc/nginx/nginx.conf
worker_processes auto;
worker_connections 10240;
```

---

## 🎯 快速部署脚本

**创建一键部署脚本** `/var/www/deploy.sh`：

```bash
#!/bin/bash

echo "======================================"
echo "  EIMS 一键部署脚本"
echo "======================================"

# 检查是否以 root 运行
if [ "$EUID" -ne 0 ]; then
  echo "请使用 root 用户运行"
  exit 1
fi

# 安装依赖
echo "正在安装依赖..."
yum update -y
yum install -y gcc openssl-devel bzip2-devel libffi-devel zlib-devel git nginx epel-release supervisor

# 下载编译 Python
echo "正在安装 Python 3.14..."
cd /usr/local/src
if [ ! -f "Python-3.14.3.tgz" ]; then
  wget https://www.python.org/ftp/python/3.14.3/Python-3.14.3.tgz
fi
tar -xzf Python-3.14.3.tgz
cd Python-3.14.3
./configure --enable-optimizations
make -j$(nproc)
make altinstall

# 创建项目目录
echo "正在创建项目目录..."
mkdir -p /var/www/eims
cd /var/www/eims

# 克隆代码
echo "正在克隆代码..."
git clone https://gitee.com/liming20090607/eims2026.git .

# 创建虚拟环境
echo "正在创建虚拟环境..."
/usr/local/bin/python3.14 -m venv venv
source venv/bin/activate

# 安装依赖
echo "正在安装项目依赖..."
pip install --upgrade pip
pip install -r requirements.txt
pip install gunicorn

# 创建日志目录
mkdir -p /var/log/gunicorn

echo "======================================"
echo "  基础安装完成！"
echo "======================================"
echo ""
echo "下一步："
echo "1. 配置 MySQL 数据库"
echo "2. 修改 settings.py"
echo "3. 运行数据库迁移"
echo "4. 配置 Nginx 和 Supervisor"
echo ""
echo "详细说明请查看：/var/www/eims/阿里云部署指南.md"
```

**运行部署**：
```bash
chmod +x /var/www/deploy.sh
bash /var/www/deploy.sh
```

---

## 📞 技术支持

### 阿里云文档
- [ECS 使用指南](https://help.aliyun.com/product/25362.html)
- [宝塔面板教程](https://help.aliyun.com/document_detail/52396.html)

### Django 文档
- [Django 部署文档](https://docs.djangoproject.com/en/5.2/howto/deployment/)
- [Gunicorn 文档](https://docs.gunicorn.org/en/stable/)

### 项目问题
- Gitee: https://gitee.com/liming20090607/eims2026
- GitHub: https://github.com/liming20090607/eims2026

---

**祝您部署成功！** 🎉
