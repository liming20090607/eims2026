# 右键菜单功能 - 快速测试指南

## 🎯 测试目标

验证表头右键菜单的三个功能选项是否正常工作。

---

## 📋 测试步骤

### 第一步：清除浏览器缓存

**必须执行**，否则看不到新功能！

- **方法1（推荐）**：按 `Ctrl + F5` 硬刷新
- **方法2**：打开无痕/隐私窗口
- **方法3**：手动清除浏览器缓存

---

### 第二步：访问测试页面

打开浏览器，访问：
```
http://127.0.0.1:8000/cost-consulting/project-info/
```

---

### 第三步：测试右键菜单显示

1. 将鼠标移到任意可排序的表头上（如"项目编号"）
2. **右键点击**表头
3. **预期结果**：
   - ✅ 弹出白色菜单框
   - ✅ 显示三个选项：
     - ❌ 取消本字段排序
     - 🔍 按此字段筛选
     - 🗑️ 取消此字段筛选
   - ✅ 菜单有淡入动画效果
   - ✅ 鼠标悬停时选项高亮（蓝色）

4. **点击空白处**或按 **ESC 键**
5. **预期结果**：菜单关闭

---

### 第四步：测试"取消本字段排序"

**准备**：
1. 左键点击"项目编号"表头（添加排序）
2. 左键点击"项目名称"表头（添加第二个排序）
3. 确认看到优先级数字：项目名称(1)、项目编号(2)

**测试**：
1. 右键点击"项目编号"表头
2. 选择"❌ 取消本字段排序"
3. **预期结果**：
   - ✅ 页面自动刷新
   - ✅ "项目编号"的优先级数字消失
   - ✅ "项目名称"仍显示优先级(1)
   - ✅ URL 中 `sort_field` 参数只包含 `project_name`

---

### 第五步：测试"按此字段筛选"

**测试1：文本筛选**
1. 右键点击"委托单位"表头
2. 选择"🔍 按此字段筛选"
3. **预期结果**：
   - ✅ 弹出筛选对话框
   - ✅ 标题显示"筛选: 委托单位"
   - ✅ 有"筛选条件"下拉框
   - ✅ 有"筛选值"输入框
   - ✅ 有"取消"和"应用筛选"按钮

4. 筛选条件选择"包含"
5. 筛选值输入一个存在的单位名称（如"科技"）
6. 点击"应用筛选"
7. **预期结果**：
   - ✅ 页面刷新
   - ✅ 只显示委托单位包含"科技"的项目
   - ✅ URL 中包含 `filter_entrusting_unit=科技&filter_entrusting_unit_op=contains`

**测试2：不同筛选条件**
1. 右键点击"项目状态"表头
2. 选择"🔍 按此字段筛选"
3. 筛选条件选择"等于"
4. 筛选值输入"进行中"
5. 点击"应用筛选"
6. **预期结果**：只显示状态为"进行中"的项目

---

### 第六步：测试"取消此字段筛选"

**准备**：
1. 先设置一个筛选（如委托单位包含"科技"）
2. 确认列表已过滤

**测试**：
1. 右键点击"委托单位"表头
2. 选择"🗑️ 取消此字段筛选"
3. **预期结果**：
   - ✅ 页面刷新
   - ✅ 显示所有项目（不再过滤）
   - ✅ URL 中 `filter_entrusting_unit` 参数消失

---

### 第七步：测试组合功能

**场景**：多字段排序 + 筛选

1. 左键点击"项目类型"表头（排序1）
2. 左键点击"创建时间"表头（排序2）
3. 右键点击"委托单位" → 筛选 → 输入"科技" → 应用
4. **预期结果**：
   - ✅ 显示委托单位包含"科技"的项目
   - ✅ 按项目类型和创建时间排序
   - ✅ 表头显示优先级数字
   - ✅ URL 包含 sort_field、sort_order 和 filter 参数

5. 右键点击"项目类型" → 取消本字段排序
6. **预期结果**：
   - ✅ 筛选仍然生效
   - ✅ 只剩"创建时间"排序
   - ✅ 优先级数字调整为(1)

---

## ✅ 检查清单

完成以下所有检查项：

- [ ] 右键菜单能正常显示
- [ ] 菜单有三个选项，带图标
- [ ] 菜单有关闭动画
- [ ] ESC键能关闭菜单
- [ ] 点击空白处能关闭菜单
- [ ] "取消本字段排序"功能正常
- [ ] "按此字段筛选"对话框正常弹出
- [ ] 筛选条件下拉框有5个选项
- [ ] 筛选值输入框能输入文本
- [ ] "应用筛选"按钮能提交
- [ ] 筛选后数据正确过滤
- [ ] "取消此字段筛选"能清除筛选
- [ ] URL参数正确反映操作
- [ ] 多字段排序和筛选可以组合使用
- [ ] 页面刷新后状态保持

---

## 🐛 常见问题

### Q1: 右键菜单不显示

**解决方法**：
1. 确认已硬刷新（Ctrl+F5）
2. 按F12打开控制台，查看是否有JavaScript错误
3. 尝试无痕模式
4. 检查浏览器是否阻止了右键菜单

---

### Q2: 筛选对话框不弹出

**解决方法**：
1. 检查Bootstrap是否正确加载
2. 查看控制台是否有 `bootstrap is not defined` 错误
3. 确认使用的是现代浏览器

---

### Q3: 筛选后没有数据

**可能原因**：
- 筛选值输入错误
- 确实没有匹配的数据

**解决方法**：
1. 检查筛选值是否正确
2. 尝试更宽松的条件（如"包含"代替"等于"）
3. 取消筛选查看所有数据

---

### Q4: 取消排序后顺序混乱

**说明**：这是正常的，因为还有其他字段在排序

**解决方法**：
1. 查看其他表头的优先级数字
2. 理解多字段排序逻辑
3. 如需完全重置，刷新页面

---

## 📊 终端调试信息

操作时观察Django终端输出，应该看到类似：

```
DEBUG SORT - sort_field: project_name,project_code, sort_order: asc,asc
DEBUG SORT - Multi-field order: ['project_name', 'project_code']
[21/Mar/2026 14:00:00] "GET /cost-consulting/project-info/?filter_entrusting_unit=科技&filter_entrusting_unit_op=contains HTTP/1.1" 200 12345
```

如果看到这些日志，说明后端正确接收了参数。

---

## 🎉 测试成功标志

当你看到以下现象时，说明功能完全正常：

1. ✅ 右键点击表头，弹出美观的菜单
2. ✅ 点击菜单项，执行对应操作
3. ✅ 页面刷新后，数据按预期排序/筛选
4. ✅ URL参数正确反映当前状态
5. ✅ 可以组合使用多个功能
6. ✅ 没有任何JavaScript错误

---

## 📝 测试报告模板

如果发现问题，请提供：

```
测试日期：2026年3月21日
浏览器：Chrome/Edge/Firefox + 版本号
操作步骤：
1. ...
2. ...
3. ...

预期结果：...
实际结果：...
截图：[附上截图]
控制台错误：[复制错误信息]
```

---

*测试指南版本：1.0*
*创建日期：2026年3月21日*
