# 🔄 EIMS 持续集成与自动部署方案

## 📋 问题分析

**当前方式**：
- ❌ 每次修改都要重新上传整个项目
- ❌ 手动替换文件容易出错
- ❌ 需要手动执行迁移和重启服务
- ❌ 无法追踪代码变更历史

**推荐方案**：
- ✅ 使用 Git 版本控制
- ✅ 只上传变更的文件
- ✅ 自动执行迁移和重启
- ✅ 保留完整的修改历史

---

## 🎯 方案一：Git 版本控制（强烈推荐⭐⭐⭐⭐⭐）

### 优势
- ✅ 只推送变更的文件
- ✅ 保留完整的修改历史
- ✅ 可以轻松回滚到任意版本
- ✅ 支持多人协作开发
- ✅ 可以配置自动部署

### 实施步骤

#### 第 1 步：在本地创建 Git 仓库
```bash
# 在本地项目根目录（e:\EIMS2026）
cd e:\EIMS2026
git init
git add .
git commit -m "Initial commit - EIMS project"
```

#### 第 2 步：创建远程仓库（3 个选择）

**选择 A：GitHub（免费，推荐）**
1. 访问 https://github.com
2. 创建新仓库 `eims2026`
3. 选择 Private（私有）或 Public（公开）

**选择 B：Gitee（国内，速度快）**
1. 访问 https://gitee.com
2. 创建新仓库 `eims2026`

**选择 C：阿里云 Code（付费，企业级）**
1. 访问 https://code.aliyun.com
2. 创建新仓库

#### 第 3 步：关联远程仓库
```bash
# 添加远程仓库（以 GitHub 为例）
git remote add origin https://github.com/你的用户名/eims2026.git

# 推送到远程
git push -u origin main
```

#### 第 4 步：在服务器上克隆项目
```bash
# 在阿里云服务器上
cd /var/www
git clone https://github.com/你的用户名/eims2026.git eims
cd eims

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

#### 第 5 步：配置自动部署脚本
创建 `deploy.sh`（在服务器项目根目录）：
```bash
#!/bin/bash
# 自动部署脚本

echo "🚀 开始部署..."

# 进入项目目录
cd /var/www/eims

# 拉取最新代码
git pull origin main

# 激活虚拟环境
source venv/bin/activate

# 安装新依赖（如果有）
pip install -r requirements.txt

# 数据库迁移
python manage.py migrate

# 收集静态文件
python manage.py collectstatic --noinput

# 重启服务
sudo systemctl restart eims

echo "✅ 部署完成！"
```

赋予执行权限：
```bash
chmod +x deploy.sh
```

#### 第 6 步：日常开发流程
```bash
# 在本地修改代码后
cd e:\EIMS2026

# 查看变更
git status

# 添加变更
git add .

# 提交变更
git commit -m "修复了部门角色配置的布局问题"

# 推送到远程
git push origin main

# 在服务器上执行部署
ssh root@服务器 IP "cd /var/www/eims && ./deploy.sh"
```

---

## 🚀 方案二：使用 rsync 同步（推荐⭐⭐⭐⭐）

### 优势
- ✅ 只传输变更的文件
- ✅ 传输速度快
- ✅ 可以排除不必要的文件
- ✅ 适合小团队或个人开发

### 实施步骤

#### 第 1 步：创建同步脚本（本地）
创建 `sync_to_server.bat`（Windows）：
```batch
@echo off
echo 🔄 同步到阿里云服务器...

REM 配置服务器信息
set SERVER_IP=你的服务器 IP
set SERVER_USER=root
set PROJECT_PATH=/var/www/eims

REM 排除不需要上传的文件
set EXCLUDE=--exclude=".git/" --exclude="*.pyc" --exclude="__pycache__/" --exclude="db.sqlite3" --exclude="media/" --exclude="staticfiles/"

REM 使用 rsync 同步
rsync -avz --delete %EXCLUDE% e:\EIMS2026\ %SERVER_USER%@%SERVER_IP%:%PROJECT_PATH%

echo ✅ 同步完成！
pause
```

#### 第 2 步：安装 rsync（Windows）
下载并安装 Git Bash 或 Cygwin，它们包含 rsync 工具

或者使用 PowerShell 版本：
创建 `sync_to_server.ps1`：
```powershell
# 配置服务器信息
$ServerIP = "你的服务器 IP"
$ServerUser = "root"
$ProjectPath = "/var/www/eims"

# 排除的文件和目录
$Exclude = @(".git/", "*.pyc", "__pycache__/", "db.sqlite3", "media/", "staticfiles/")

# 构建 rsync 命令
$ExcludeArgs = $Exclude | ForEach-Object { "--exclude='$_'" }

Write-Host "🔄 开始同步到服务器..." -ForegroundColor Green

# 执行 rsync
rsync -avz --delete $ExcludeArgs e:/EIMS2026/ ${ServerUser}@${ServerIP}:${ProjectPath}

Write-Host "✅ 同步完成！" -ForegroundColor Green
```

#### 第 3 步：在服务器上创建部署脚本
创建 `/var/www/eims/deploy.sh`：
```bash
#!/bin/bash
echo "🚀 开始部署..."

cd /var/www/eims

# 激活虚拟环境
source venv/bin/activate

# 数据库迁移
python manage.py migrate

# 收集静态文件
python manage.py collectstatic --noinput

# 重启服务
sudo systemctl restart eims

echo "✅ 部署完成！"
```

#### 第 4 步：日常使用
```bash
# 在本地修改代码后
# 运行同步脚本
.\sync_to_server.bat

# 或 PowerShell 版本
.\sync_to_server.ps1

# SSH 到服务器执行部署
ssh root@服务器 IP "cd /var/www/eims && ./deploy.sh"
```

---

## ⚡ 方案三：使用 Fabric 自动化部署（推荐⭐⭐⭐⭐）

### 优势
- ✅ 一键部署到服务器
- ✅ 可以管理多台服务器
- ✅ 完整的部署流程控制
- ✅ Python 编写，易于维护

### 实施步骤

#### 第 1 步：安装 Fabric（本地）
```bash
cd e:\EIMS2026
pip install fabric
```

#### 第 2 步：创建部署脚本 `fabfile.py`
```python
from fabric import task
import os

# 服务器配置
HOSTS = ['root@你的服务器 IP']
PROJECT_PATH = '/var/www/eims'

@task
def deploy(c):
    """部署到服务器"""
    print("🚀 开始部署...")
    
    # 进入项目目录
    with c.cd(PROJECT_PATH):
        # 拉取最新代码（如果使用 Git）
        # c.run('git pull origin main')
        
        # 激活虚拟环境
        with c.prefix('source venv/bin/activate'):
            # 数据库迁移
            c.run('python manage.py migrate')
            
            # 收集静态文件
            c.run('python manage.py collectstatic --noinput')
        
        # 重启服务
        c.sudo('systemctl restart eims')
    
    print("✅ 部署完成！")

@task
def upload(c):
    """上传本地代码到服务器"""
    print("📤 上传代码...")
    
    # 排除的文件和目录
    exclude = [
        '.git',
        '*.pyc',
        '__pycache__',
        'db.sqlite3',
        'media',
        'staticfiles',
        'venv',
        '.env'
    ]
    
    # 构建 rsync 命令
    exclude_opts = ' '.join([f"--exclude='{e}'" for e in exclude])
    c.local(f'rsync -avz --delete {exclude_opts} e:/EIMS2026/ {HOSTS[0].split("@")[1]}:{PROJECT_PATH}')
    
    print("✅ 上传完成！")

@task
def restart(c):
    """仅重启服务"""
    print("🔄 重启服务...")
    c.sudo('systemctl restart eims')
    print("✅ 重启完成！")

@task
def logs(c):
    """查看日志"""
    print("📋 查看最新日志...")
    c.run('tail -n 50 /var/log/gunicorn/error.log')
```

#### 第 3 步：日常使用
```bash
# 上传并部署
fab -H root@服务器 IP deploy

# 仅上传代码
fab -H root@服务器 IP upload

# 仅重启服务
fab -H root@服务器 IP restart

# 查看日志
fab -H root@服务器 IP logs
```

---

##  方案四：使用 GitHub Actions 自动部署（推荐⭐⭐⭐⭐⭐）

### 优势
- ✅ 完全自动化
- ✅ 提交代码后自动部署
- ✅ 无需手动操作
- ✅ 支持 CI/CD 流水线

### 实施步骤

#### 第 1 步：在 GitHub 仓库创建 Actions 配置
创建 `.github/workflows/deploy.yml`：
```yaml
name: 🚀 自动部署到阿里云

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v3
    
    - name: 🔑 配置 SSH 密钥
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
    
    - name: 🚀 部署到服务器
      run: |
        ssh -o StrictHostKeyChecking=no root@你的服务器 IP << 'ENDSSH'
          cd /var/www/eims
          git pull origin main
          source venv/bin/activate
          pip install -r requirements.txt
          python manage.py migrate
          python manage.py collectstatic --noinput
          systemctl restart eims
          echo "✅ 部署完成！"
        ENDSSH
```

#### 第 2 步：配置 GitHub Secrets
1. 在 GitHub 仓库 → Settings → Secrets and variables → Actions
2. 添加 `SSH_PRIVATE_KEY`：你的 SSH 私钥

#### 第 3 步：日常使用
```bash
# 在本地修改代码后
git add .
git commit -m "新增功能：部门角色配置优化"
git push origin main

# 自动触发部署，无需其他操作！
```

---

## 📊 方案对比

| 方案 | 难度 | 自动化程度 | 适用场景 | 推荐指数 |
|------|------|-----------|---------|---------|
| **Git + 手动部署** | ⭐⭐ | ⭐⭐ | 个人开发、小项目 | ⭐⭐⭐⭐⭐ |
| **rsync 同步** | ⭐⭐ | ⭐⭐ | 快速同步文件 | ⭐⭐⭐⭐ |
| **Fabric 自动化** | ⭐⭐⭐ | ⭐⭐⭐⭐ | 多服务器管理 | ⭐⭐⭐⭐ |
| **GitHub Actions** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 团队协作、CI/CD | ⭐⭐⭐⭐⭐ |

---

## 🎯 最佳实践建议

### 个人开发者（推荐）
**Git + 简单部署脚本**
```bash
# 本地提交
git add .
git commit -m "修改说明"
git push

# 服务器部署
ssh root@服务器 IP "cd /var/www/eims && ./deploy.sh"
```

### 小团队（3-5 人）
**Git + GitHub Actions**
- 代码推送到 GitHub
- 自动触发部署
- 代码审查后合并

### 企业级（多人协作）
**Git + CI/CD 流水线**
- 使用 GitLab CI 或 Jenkins
- 多环境（开发、测试、生产）
- 自动化测试
- 灰度发布

---

## 🔧 具体实施方案（立即开始）

### 方案 A：快速上手（10 分钟）

#### 第 1 步：本地创建 Git 仓库
```bash
cd e:\EIMS2026
git init
git add .
git commit -m "Initial commit"
```

#### 第 2 步：创建 GitHub 仓库
1. 访问 https://github.com/new
2. 仓库名：`eims2026`
3. 选择 Private

#### 第 3 步：推送代码
```bash
git remote add origin https://github.com/你的用户名/eims2026.git
git push -u origin main
```

#### 第 4 步：在服务器上克隆
```bash
ssh root@服务器 IP
cd /var/www
git clone https://github.com/你的用户名/eims2026.git eims
cd eims
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 第 5 步：创建部署脚本
```bash
nano deploy.sh
```

内容：
```bash
#!/bin/bash
cd /var/www/eims
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py collectstatic --noinput
systemctl restart eims
echo "✅ 部署完成！"
```

#### 第 6 步：以后每次修改
```bash
# 在本地
git add .
git commit -m "修改说明"
git push

# 在服务器上
ssh root@服务器 IP "cd /var/www/eims && ./deploy.sh"
```

---

## 💡 常见问题

### Q1：数据库会丢失吗？
**A**：不会！数据库是独立的，代码变更不会影响数据。但要注意：
- ✅ 不要上传 `db.sqlite3`
- ✅ 使用 MySQL 等独立数据库
- ✅ 每次部署后执行 `python manage.py migrate`

### Q2：上传的文件会丢失吗？
**A**：不会！媒体文件应该：
- ✅ 存放在独立目录（如 `/var/www/media`）
- ✅ 不要通过 Git 管理媒体文件
- ✅ 使用对象存储（如阿里云 OSS）

### Q3：如何回滚到之前的版本？
**A**：使用 Git 的回滚功能：
```bash
# 查看历史
git log

# 回滚到指定版本
git checkout <commit_id>

# 推送到服务器
git push origin main --force
```

### Q4：多人开发会冲突吗？
**A**：合理使用 Git 分支：
```bash
# 创建功能分支
git checkout -b feature/new-feature

# 开发完成后合并
git checkout main
git merge feature/new-feature

# 解决冲突（如果有）
git mergetool
```

---

## 🎉 总结

**立即开始的最佳方案**：
1. ✅ 使用 Git 管理代码
2. ✅ 推送到 GitHub/Gitee
3. ✅ 在服务器上克隆
4. ✅ 创建简单的部署脚本
5. ✅ 每次修改后：`git push` + `ssh 部署`

**未来升级方向**：
- 自动测试
- CI/CD 流水线
- 容器化部署（Docker）
-  Kubernetes 编排

**现在就开始吧！** 🚀
