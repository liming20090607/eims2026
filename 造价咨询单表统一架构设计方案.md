# 造价咨询模块 - 单表统一架构设计方案

## 🎯 设计目标

将当前的7个独立表合并为**1个大表**，通过不同的视图（View）展示不同的业务模块。

---

## 📊 当前架构 vs 新架构对比

### 当前架构（7个表）

```
CostProjectInfo (项目信息) - 主表
    ↓ 外键关联
CostTaskPlan (任务计划)
CostTaskImplementation (任务实施)
CostReviewResult (审核成果)
CostPaymentStatus (收费情况)
CostProjectArchive (项目存档)
CostRemunerationDistribution (酬劳分配)
    ↓ 一对多
CostRemunerationItem (酬劳明细)
```

**问题**：
- ❌ 需要同步冗余字段
- ❌ 外键关联复杂
- ❌ 查询需要多次JOIN
- ❌ 数据一致性难保证

---

### 新架构（1个表 + 7个视图）

```
CostProjectUnified (统一项目表)
    ├── 视图1: 项目信息视口
    ├── 视图2: 任务计划视口
    ├── 视图3: 任务实施视口
    ├── 视图4: 审核成果视口
    ├── 视图5: 收费情况视口
    ├── 视图6: 项目存档视口
    └── 视图7: 酬劳分配视口
```

**优势**：
- ✅ 单一数据源，无同步问题
- ✅ 无需外键关联
- ✅ 查询简单高效
- ✅ 数据天然一致

---

## 🔧 实施方案

### 步骤1: 创建统一模型

将所有字段合并到一个模型中，按业务模块分组：

```python
class CostProjectUnified(models.Model):
    """造价咨询统一项目表"""
    
    # ===== 租户字段 =====
    tenant = models.ForeignKey('Tenant', ...)
    
    # ===== 基础信息（来自原CostProjectInfo）=====
    project_code = CharField(...)
    project_name = CharField(...)
    project_type = CharField(...)
    # ... 约40个字段
    
    # ===== 任务计划字段（来自原CostTaskPlan）=====
    compiler = CharField(...)
    first_reviewer = CharField(...)
    # ... 约20个字段
    
    # ===== 任务实施字段（来自原CostTaskImplementation）=====
    actual_compiler = CharField(...)
    # ... 约15个字段
    
    # ===== 审核成果字段（来自原CostReviewResult）=====
    first_submission = DecimalField(...)
    # ... 约20个字段
    
    # ===== 收费情况字段（来自原CostPaymentStatus）=====
    invoice_amount = DecimalField(...)
    # ... 约15个字段
    
    # ===== 项目存档字段（来自原CostProjectArchive）=====
    archive_status = CharField(...)
    # ... 约10个字段
    
    # ===== 酬劳分配字段（来自原CostRemunerationDistribution）=====
    total_remuneration = DecimalField(...)
    # ... 约15个字段
    
    # ===== 系统字段 =====
    created_at = DateTimeField(...)
    update_time = DateTimeField(...)
```

**预估字段数**: 约 **135个字段**

---

### 步骤2: 创建数据库视图（可选）

为每个业务模块创建数据库视图，简化查询：

```sql
-- 项目信息视图
CREATE VIEW v_cost_project_info AS
SELECT id, project_code, project_name, project_type, ...
FROM cost_project_unified;

-- 任务计划视图
CREATE VIEW v_cost_task_plan AS
SELECT id, project_code, project_name, compiler, first_reviewer, ...
FROM cost_project_unified;

-- 其他视图类似...
```

---

### 步骤3: 创建Django Manager视图

在模型中定义不同的Manager，模拟原来的子模块：

```python
class CostProjectUnified(models.Model):
    # ... 字段定义 ...
    
    # 项目管理器
    objects = models.Manager()
    
    # 各业务模块管理器
    project_info_manager = ProjectInfoManager()
    task_plan_manager = TaskPlanManager()
    task_impl_manager = TaskImplManager()
    review_result_manager = ReviewResultManager()
    payment_manager = PaymentManager()
    archive_manager = ArchiveManager()
    remuneration_manager = RemunerationManager()
```

---

### 步骤4: 重构视图层

修改所有视图函数，使用新的统一模型：

**修改前**:
```python
from eims_app.models import CostTaskPlan

def cost_task_plan_list(request):
    queryset = CostTaskPlan.objects.select_related('project').all()
    # ...
```

**修改后**:
```python
from eims_app.models import CostProjectUnified

def cost_task_plan_list(request):
    # 只查询任务计划相关的字段
    queryset = CostProjectUnified.objects.only(
        'id', 'project_code', 'project_name', 
        'compiler', 'first_reviewer', ...
    ).all()
    # ...
```

---

### 步骤5: 更新模板

模板基本不需要大改，因为字段名保持不变。

---

## 📈 迁移策略

### 阶段1: 准备阶段

1. ✅ 统计所有字段
2. ✅ 设计新模型结构
3. ✅ 创建数据迁移脚本

### 阶段2: 并行运行

1. 创建新表 `cost_project_unified`
2. 编写数据迁移脚本，将7个表的数据合并到新表
3. 新旧模型同时存在，逐步迁移视图

### 阶段3: 切换

1. 所有视图切换到新模型
2. 测试所有功能
3. 删除旧表和旧模型

### 阶段4: 清理

1. 删除旧的7个模型类
2. 删除相关的外键关系
3. 优化数据库索引

---

## ⚠️ 注意事项

### 1. 字段命名冲突

不同子模块可能有相同名称的字段，需要重命名：

```python
# 原CostTaskPlan
compiler = CharField("编制人", ...)

# 原CostTaskImplementation  
compiler = CharField("实际编制人", ...)

# 合并后
task_plan_compiler = CharField("计划-编制人", ...)
task_impl_compiler = CharField("实施-实际编制人", ...)
```

### 2. 一对多关系处理

`CostRemunerationItem` 是酬劳分配的明细表（一对多），需要特殊处理：

**方案A**: 保留为独立表，通过外键关联
**方案B**: 改为JSON字段存储在统一表中
**方案C**: 使用前缀区分多条记录（item1_person, item2_person...）

**推荐**: 方案A - 保留独立表，因为明细数量不固定

### 3. 数据库性能

135个字段的表可能影响性能：

**优化措施**:
- 添加合适的索引
- 使用 `only()` 和 `defer()` 按需加载字段
- 考虑垂直分表（热点字段 vs 冷字段）

### 4. 向后兼容

为确保平滑过渡：
- 保留旧模型的只读访问
- 提供数据同步工具
- 逐步迁移，不要一次性切换

---

## 🎯 建议

### 是否值得这样做？

**适合的场景**：
- ✅ 项目数量不多（< 10万条）
- ✅ 字段访问模式相对固定
- ✅ 希望简化架构
- ✅ 团队规模小，维护成本低

**不适合的场景**：
- ❌ 项目数量巨大（> 100万条）
- ❌ 不同模块访问频率差异大
- ❌ 需要严格的模块隔离
- ❌ 团队规模大，需要模块化开发

### 我的建议

考虑到您当前的情况：
1. **如果项目数量不大**（几千到几万条）：**推荐合并**
2. **如果追求架构简洁**：**推荐合并**
3. **如果可以接受重构工作量**：**推荐合并**

**预计工作量**：
- 模型设计: 2-4小时
- 数据迁移: 4-8小时
- 视图重构: 8-16小时
- 模板调整: 4-8小时
- 测试验证: 8-16小时
- **总计**: 约 2-3个工作日

---

## 📝 下一步

如果您决定采用这个方案，我可以帮您：

1. ✅ 创建完整的统一模型（包含所有字段）
2. ✅ 编写数据迁移脚本
3. ✅ 重构所有视图函数
4. ✅ 更新URL配置
5. ✅ 提供测试脚本

**请确认是否继续？**
