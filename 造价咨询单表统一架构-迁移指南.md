# 造价咨询模块 - 单表统一架构迁移指南

## 📋 概述

本指南将帮助您将当前的7表外键架构迁移到1表统一架构。

---

## ✅ 已完成的工作

### 1. 创建统一模型

**文件**: `eims_app/models/model_cost_unified.py`

**包含内容**:
- ✅ `CostProjectUnified` - 统一项目表（约135个字段）
- ✅ `CostRemunerationItem` - 酬劳分配明细表（保留独立表）

**字段分组**:
```
- 系统字段 (4个)
- 审批流字段 (9个)
- 模块1: 项目信息 (约40个字段)
- 模块2: 任务计划 (约30个字段)
- 模块3: 任务实施 (约35个字段)
- 模块4: 审核成果 (约20个字段)
- 模块5: 收费情况 (约15个字段)
- 模块6: 项目存档 (约8个字段)
- 模块7: 酬劳分配 (约15个字段)
```

**总计**: 约 **176个字段**

---

## 🚀 迁移步骤

### 步骤1: 注册新模型

编辑 `eims_app/models/__init__.py`，添加新模型：

```python
from .model_cost_unified import CostProjectUnified, CostRemunerationItem
```

---

### 步骤2: 创建数据库迁移

```bash
# 生成迁移文件
python manage.py makemigrations eims_app --name create_cost_unified_table

# 查看迁移SQL（可选）
python manage.py sqlmigrate eims_app <migration_number>

# 执行迁移
python manage.py migrate
```

---

### 步骤3: 数据迁移脚本

创建数据迁移脚本，将旧表数据合并到新表：

**文件**: `migrate_to_unified.py`

```python
#!/usr/bin/env python
"""
将7个旧表的数据迁移到统一表
"""

import os
import sys
import django

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings')
django.setup()

from eims_app.models.model_cost_sub_modules import (
    CostProjectInfo,
    CostTaskPlan,
    CostTaskImplementation,
    CostReviewResult,
    CostPaymentStatus,
    CostProjectArchive,
    CostRemunerationDistribution,
    CostRemunerationItem as OldRemunerationItem,
)
from eims_app.models.model_cost_unified import (
    CostProjectUnified,
    CostRemunerationItem as NewRemunerationItem,
)


def migrate_data():
    """执行数据迁移"""
    print("="*60)
    print("开始迁移造价咨询数据到统一表")
    print("="*60)
    
    # 获取所有项目
    projects = CostProjectInfo.objects.all()
    print(f"\n找到 {projects.count()} 个项目")
    
    migrated_count = 0
    error_count = 0
    
    for project in projects:
        try:
            # 创建统一记录
            unified = CostProjectUnified.objects.create(
                tenant=project.tenant,
                created_at=project.created_at,
                update_time=project.update_time,
                operator=project.operator,
                approval_status=project.approval_status,
                
                # 模块1: 项目信息
                project_code=project.project_code,
                project_name=project.project_name,
                project_type=project.project_type,
                compilation_category=project.compilation_category,
                review_category=project.review_category,
                project_status=project.project_status,
                client_unit=project.client_unit,
                entrusting_unit=project.entrusting_unit,
                contact_person=project.contact_person,
                contact_phone=project.contact_phone,
                submission_time=project.submission_time,
                start_time=project.start_time,
                planned_duration=project.planned_duration,
                planned_completion_time=project.planned_completion_time,
                compilation_amount=project.compilation_amount,
                submission_amount=project.submission_amount,
                approved_amount=project.approved_amount,
                reduced_amount=project.reduced_amount,
                report_time=project.report_time,
                result_confirm=project.result_confirm,
                total_fee=project.total_fee,
                received_fee=project.received_fee,
                pending_fee=project.pending_fee,
                fee_settlement=project.fee_settlement,
                remark=project.remark,
            )
            
            # 模块2: 任务计划
            task_plan = CostTaskPlan.objects.filter(project=project).first()
            if task_plan:
                unified.plan_compiler = task_plan.compiler
                unified.plan_compilation_amount = task_plan.compilation_amount
                unified.plan_first_reviewer = task_plan.first_reviewer
                unified.plan_first_review_start_time = task_plan.first_review_start_time
                unified.plan_first_review_planned_duration = task_plan.first_review_planned_duration
                unified.plan_first_review_planned_completion = task_plan.first_review_planned_completion
                # ... 其他字段
            
            # 模块3: 任务实施
            task_impl = CostTaskImplementation.objects.filter(project=project).first()
            if task_impl:
                unified.impl_compiler = task_impl.compiler
                unified.impl_compilation_amount = task_impl.compilation_amount
                # ... 其他字段
            
            # 模块4: 审核成果
            review = CostReviewResult.objects.filter(project=project).first()
            if review:
                unified.review_compiler = review.compiler
                unified.review_compilation_amount = review.compilation_amount
                unified.review_first_submission = review.first_submission
                unified.review_final_approved_amount = review.final_approved_amount
                # ... 其他字段
            
            # 模块5: 收费情况
            payment = CostPaymentStatus.objects.filter(project=project).first()
            if payment:
                unified.payment_invoice_amount = payment.invoice_amount
                unified.payment_is_invoiced = payment.is_invoiced
                unified.payment_owner_payable = payment.owner_payable
                unified.payment_owner_paid = payment.owner_paid
                unified.payment_owner_pending = payment.owner_pending
                # ... 其他字段
            
            # 模块6: 项目存档
            archive = CostProjectArchive.objects.filter(project=project).first()
            if archive:
                unified.archive_status = archive.archive_status
                unified.archive_electronic = archive.electronic_archive
                unified.archive_paper = archive.paper_archive
                unified.archive_complete = archive.archive_complete
                unified.archive_location = archive.archive_location
                unified.archive_date = archive.archive_date
                # ... 其他字段
            
            # 模块7: 酬劳分配
            remuneration = CostRemunerationDistribution.objects.filter(project=project).first()
            if remuneration:
                unified.remuneration_calculation_type = remuneration.calculation_type
                unified.remuneration_calculation_base = remuneration.calculation_base
                unified.remuneration_total_cost = remuneration.total_cost
                unified.remuneration_reduced_amount = remuneration.reduced_amount
                unified.remuneration_total_remuneration = remuneration.total_remuneration
                unified.remuneration_calculation_formula = remuneration.calculation_formula
                unified.remuneration_distribution_status = remuneration.distribution_status
                # ... 其他字段
                
                # 迁移酬劳明细
                old_items = OldRemunerationItem.objects.filter(distribution=remuneration)
                for old_item in old_items:
                    NewRemunerationItem.objects.create(
                        project=unified,
                        person_name=old_item.person_name,
                        person_id_card=old_item.person_id_card,
                        role=old_item.role,
                        ratio=old_item.ratio,
                        amount=old_item.amount,
                    )
            
            unified.save()
            migrated_count += 1
            print(f"  ✓ 迁移项目: {project.project_code}")
            
        except Exception as e:
            error_count += 1
            print(f"  ✗ 迁移失败 {project.project_code}: {e}")
    
    print("\n" + "="*60)
    print(f"迁移完成!")
    print(f"  成功: {migrated_count} 个项目")
    print(f"  失败: {error_count} 个项目")
    print("="*60)


if __name__ == '__main__':
    migrate_data()
```

运行迁移脚本：
```bash
python migrate_to_unified.py
```

---

### 步骤4: 更新视图层

需要修改所有视图函数，使用新的统一模型。

**示例：项目信息列表**

修改前：
```python
from eims_app.models.model_cost_sub_modules import CostProjectInfo

def cost_project_info_list(request):
    queryset = CostProjectInfo.objects.all()
    # ...
```

修改后：
```python
from eims_app.models.model_cost_unified import CostProjectUnified

def cost_project_info_list(request):
    queryset = CostProjectUnified.objects.only(
        'id', 'project_code', 'project_name', 'project_type',
        'project_status', 'compilation_amount', 'submission_amount',
        'approved_amount', 'reduced_amount', 'total_fee',
        'received_fee', 'pending_fee', 'created_at'
    ).all()
    # ...
```

**需要修改的视图文件**:
- `eims_app/views/views_cost_sub_modules.py` - 所有7个子模块的视图

---

### 步骤5: 更新URL配置

URL配置基本不需要修改，因为视图函数名称保持不变。

---

### 步骤6: 更新模板

模板中的字段引用需要更新：

**修改前**:
```html
{{ item.project_code }}
{{ item.task_plan.compiler }}
```

**修改后**:
```html
{{ item.project_code }}
{{ item.plan_compiler }}
```

**需要修改的模板**:
- `eims_app/templates/cost_consulting/project_info/list.html`
- `eims_app/templates/cost_consulting/task_plan/list.html`
- `eims_app/templates/cost_consulting/task_implementation/list.html`
- `eims_app/templates/cost_consulting/review_result/list.html`
- `eims_app/templates/cost_consulting/payment_status/list.html`
- `eims_app/templates/cost_consulting/project_archive/list.html`
- `eims_app/templates/cost_consulting/remuneration_distribution/list.html`

以及对应的form.html模板。

---

### 步骤7: 测试验证

1. **功能测试**:
   - 新增项目
   - 编辑项目
   - 删除项目
   - 搜索和筛选
   - 导出Excel

2. **数据完整性测试**:
   - 确认所有字段都正确迁移
   - 确认外键关系正确
   - 确认酬劳明细正确关联

3. **性能测试**:
   - 列表页面加载速度
   - 搜索响应时间
   - 大数据量下的表现

---

### 步骤8: 清理旧代码

确认新系统运行稳定后：

1. 删除旧模型文件：
   ```bash
   # 备份后删除
   mv eims_app/models/model_cost_sub_modules.py eims_app/models/model_cost_sub_modules.py.bak
   ```

2. 从 `__init__.py` 中移除旧模型的导入

3. 删除旧的数据库表（谨慎操作！）：
   ```bash
   python manage.py dbshell
   DROP TABLE eims_app_costtaskplan;
   DROP TABLE eims_app_costtaskimplementation;
   # ... 其他表
   ```

---

## ⚠️ 注意事项

### 1. 字段命名规范

统一模型使用**前缀**区分不同模块的字段：

```
plan_*     - 任务计划字段
impl_*     - 任务实施字段
review_*   - 审核成果字段
payment_*  - 收费情况字段
archive_*  - 项目存档字段
remuneration_* - 酬劳分配字段
```

### 2. 外键处理

原模型中的外键（如 Personnel）在统一模型中：
- 添加了前缀以避免冲突
- related_name 也相应修改

### 3. 唯一约束

`project_code` 仍然是唯一约束，确保项目编号不重复。

### 4. 性能优化

对于135+字段的表，建议：
- 使用 `only()` 按需加载字段
- 添加合适的数据库索引
- 考虑读写分离

### 5. 回滚方案

如果迁移出现问题，可以：
1. 保留旧表和旧模型
2. 新旧系统并行运行
3. 逐步切换，随时回滚

---

## 📊 预期效果

### 优势

1. **简化架构**: 从7个表减少到1个主表+1个明细表
2. **消除同步**: 不再有冗余字段同步问题
3. **提升性能**: 减少JOIN操作，查询更快
4. **易于维护**: 单一数据源，逻辑清晰
5. **扩展性好**: 添加新字段更简单

### 可能的挑战

1. **表字段多**: 176个字段可能影响可读性
2. **迁移工作量大**: 需要修改大量视图和模板
3. **学习成本**: 团队需要适应新的字段命名
4. **垂直扩展**: 未来可能需要分表

---

## 🎯 建议

### 是否立即迁移？

**建议迁移的情况**:
- ✅ 当前同步问题频繁出现
- ✅ 项目数量不大（< 10万）
- ✅ 有充足时间进行测试
- ✅ 团队同意架构变更

**暂缓迁移的情况**:
- ❌ 当前系统运行稳定
- ❌ 项目数量巨大（> 100万）
- ❌ 近期有重要发布
- ❌ 团队资源紧张

### 渐进式迁移策略

如果决定迁移，建议采用**渐进式**：

1. **第1周**: 创建新模型，执行数据迁移
2. **第2周**: 迁移1-2个视图，充分测试
3. **第3周**: 迁移剩余视图
4. **第4周**: 全面测试，修复bug
5. **第5周**: 上线新系统，观察运行
6. **第6周**: 清理旧代码

---

## 📞 技术支持

如果在迁移过程中遇到问题：

1. 检查迁移日志
2. 验证数据完整性
3. 对比新旧系统的查询结果
4. 查看Django错误日志

---

**准备好了吗？让我们开始迁移吧！** 🚀
