# 会话过期跳转登录页面问题修复指南

## 🔍 问题描述

在云服务器协同办公系统中，用户登录后点击以下按钮时会自动跳转到登录页面：
- ✅ 新增产值回款
- ✅ 项目人员
- ✅ 其他需要登录的页面

**根本原因**：Django session 配置不当，导致会话过期或丢失。

---

## 🔧 问题原因分析

### 1. **缺少 Session 配置**
`settings.py` 中没有配置以下关键参数：
- `SESSION_COOKIE_AGE` - Session 过期时间（默认 2 周）
- `SESSION_SAVE_EVERY_REQUEST` - 每次请求刷新 session
- `SESSION_COOKIE_HTTPONLY` - 防止 XSS 攻击

### 2. **Session Cookie 未正确设置**
- 缺少 `SESSION_COOKIE_NAME` 自定义名称
- 生产环境没有配置 domain 和 secure 选项

---

## ✅ 修复方案

### 已添加的配置（settings.py）

```python
# -------------------------- Session 配置 --------------------------
SESSION_COOKIE_AGE = 1209600  # Session 过期时间：2 周（14 天）
SESSION_SAVE_EVERY_REQUEST = True  # 每次请求都保存 session，延长过期时间
SESSION_COOKIE_HTTPONLY = True  # 防止 XSS 攻击
SESSION_COOKIE_NAME = 'eims_sessionid'  # 自定义 session cookie 名称

# 生产环境配置（通过 IP 访问时使用）
# 如果使用域名，取消注释并修改为实际域名
# SESSION_COOKIE_DOMAIN = 'xietongai.com.cn'
# SESSION_COOKIE_SECURE = True  # 仅 HTTPS 时启用
```

---

## 🚀 部署步骤

### 第 1 步：SSH 登录服务器

```bash
ssh root@39.106.41.239
```

### 第 2 步：进入项目目录并拉取代码

```bash
cd /var/www/eims
git pull gitee master
```

### 第 3 步：验证配置

检查 `settings.py` 是否包含 Session 配置：

```bash
cd /var/www/eims
grep -A 5 "SESSION_COOKIE_AGE" settings.py
```

应该显示：
```python
SESSION_COOKIE_AGE = 1209600
SESSION_SAVE_EVERY_REQUEST = True
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_NAME = 'eims_sessionid'
```

### 第 4 步：重启服务

**方法 1：使用 Supervisor 重启（推荐）**

```bash
supervisorctl restart eims
```

**方法 2：如果使用 Gunicorn**

```bash
supervisorctl restart gunicorn
```

**方法 3：如果使用 runserver（开发环境）**

```bash
# 查找进程
ps aux | grep python

# 杀掉旧进程（替换 <PID> 为实际进程号）
kill -9 <PID>

# 重启
nohup python manage.py runserver 0.0.0.0:8000 > /var/www/eims/logs/runserver.log 2>&1 &
```

### 第 5 步：清除浏览器旧 Session

**非常重要**：由于 session cookie 名称从默认的 `sessionid` 改为 `eims_sessionid`，必须清除旧的 session cookie。

**方法 1：清除浏览器所有缓存和 Cookie**

1. 按 `Ctrl + Shift + Delete`
2. 选择"Cookie 和其他站点数据"
3. 点击"清除数据"

**方法 2：使用无痕模式（快速）**

1. 按 `Ctrl + Shift + N`（Chrome）或 `Ctrl + Shift + P`（Firefox）
2. 打开新的无痕窗口
3. 访问系统并登录

**方法 3：手动清除特定 Cookie**

1. 打开浏览器开发者工具（F12）
2. 切换到 **Application** 标签
3. 左侧选择 **Cookies** → `http://39.106.41.239:8000`
4. 右键删除所有 cookie
5. 刷新页面

### 第 6 步：验证修复

1. **登录系统**
   ```
   http://39.106.41.239:8000/login/
   ```

2. **测试导航**
   - 进入项目月报页面
   - 点击"新增产值回款"
   - 应该正常打开页面，不再跳转到登录页

3. **测试项目人员**
   - 在项目详情页点击"项目人员"→"新增"
   - 应该正常打开页面

4. **保持登录状态**
   - 登录后保持页面打开 5-10 分钟
   - 点击不同菜单项
   - 确认不会自动跳转到登录页

---

## 🔍 故障排查

### 如果问题仍然存在

#### 检查 1：确认 Session 配置生效

```bash
cd /var/www/eims
python -c "from EIMS2026 import settings; print('SESSION_COOKIE_AGE:', settings.SESSION_COOKIE_AGE)"
```

应该输出：`SESSION_COOKIE_AGE: 1209600`

#### 检查 2：查看浏览器 Cookie

1. 打开开发者工具（F12）
2. Application → Cookies → `http://39.106.41.239:8000`
3. 确认存在 `eims_sessionid` cookie（不是 `sessionid`）
4. 检查 cookie 的过期时间（应该约 2 周后）

#### 检查 3：查看 Django 日志

```bash
# 查看最近的错误日志
tail -f /var/www/eims/logs/error.log

# 或者查看 Supervisor 日志
supervisorctl tail eims stderr
```

#### 检查 4：确认中间件顺序

确保 `settings.py` 中的中间件顺序正确：

```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',  # ← 必须有
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',  # ← 必须有
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'eims_app.middleware.login_required.login_required_middleware',
    'eims_app.middleware.monthly_report_reminder.monthly_report_reminder_middleware',
]
```

#### 检查 5：测试 Session 是否工作

在浏览器开发者工具 Console 中运行：

```javascript
// 查看当前所有 cookie
console.log(document.cookie);

// 应该看到类似：eims_sessionid=abc123...; csrftoken=xyz789...
```

---

## 📊 配置参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `SESSION_COOKIE_AGE` | 1209600 | Session 过期时间（秒），1209600 = 2 周 |
| `SESSION_SAVE_EVERY_REQUEST` | True | 每次请求刷新 session 过期时间 |
| `SESSION_COOKIE_HTTPONLY` | True | 防止 JavaScript 读取 cookie（安全） |
| `SESSION_COOKIE_NAME` | eims_sessionid | 自定义 cookie 名称 |
| `SESSION_COOKIE_DOMAIN` | 留空 | IP 访问时留空，域名访问时填写 |
| `SESSION_COOKIE_SECURE` | False | HTTP 时设为 False，HTTPS 时设为 True |

---

## 🔐 后续优化建议

### 1. 使用域名后更新配置

当使用 `xietongai.com.cn` 域名时，修改：

```python
SESSION_COOKIE_DOMAIN = 'xietongai.com.cn'
SESSION_COOKIE_SECURE = True  # 启用 HTTPS 后
```

### 2. 数据库 Session 后端（可选）

如果用户量大，建议将 session 存储到数据库：

```python
# settings.py
SESSION_ENGINE = 'django.contrib.sessions.backends.db'
```

### 3. 缓存 Session 后端（高性能）

使用 Redis 存储 session（需要先安装 Redis）：

```python
# settings.py
SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.redis.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
    }
}
```

### 4. Session 清理定时任务

定期清理过期 session：

```bash
# 添加到 crontab（每天凌晨 3 点）
0 3 * * * cd /var/www/eims && python manage.py clearsessions
```

---

## ✅ 验证清单

部署完成后，请逐项确认：

- [ ] 代码已拉取到服务器（`git pull`）
- [ ] `settings.py` 包含 Session 配置
- [ ] 服务已重启（`supervisorctl restart`）
- [ ] 浏览器旧 Cookie 已清除
- [ ] 登录后能看到 `eims_sessionid` cookie
- [ ] 点击"新增产值回款"不跳转登录页
- [ ] 点击"项目人员"不跳转登录页
- [ ] 5 分钟后仍然保持登录状态
- [ ] 刷新页面不会要求重新登录

---

## 📝 已知问题

### Q: 为什么旧 session 仍然会导致问题？

**原因**：浏览器中可能同时存在 `sessionid` 和 `eims_sessionid` 两个 cookie，导致混淆。

**解决**：必须清除所有旧 cookie，只保留新的 `eims_sessionid`。

### Q: 为什么有时候仍然会跳到登录页？

可能原因：
1. Session 过期（2 周无操作）
2. 浏览器禁用了 cookie
3. 使用了无痕模式且关闭后重新打开
4. 服务器重启（如果 session 存储在内存中）

### Q: 如何延长 session 过期时间？

修改 `settings.py`：

```python
SESSION_COOKIE_AGE = 2592000  # 30 天（30 * 24 * 60 * 60）
```

---

## 📞 获取帮助

如果按照以上步骤仍然无法解决问题，请提供：

1. 浏览器 Cookie 截图（F12 → Application → Cookies）
2. 错误日志内容（`tail -100 /var/www/eims/logs/error.log`）
3. 问题复现步骤
4. 浏览器名称和版本

---

**修复日期**：2026-03-21  
**版本**：v1.0  
**作者**：EIMS 开发团队
