# 部门与角色数据迁移 - 快速指南

**目标**：5 分钟内完成本地到服务器的数据迁移  
**适用**：本地有部门、角色数据，需要导入到服务器

---

## 🚀 最快方法（推荐）

### **方式 1：一键自动迁移** ⭐⭐⭐⭐⭐

#### **步骤 1：双击运行脚本**

**在本地**：
```
双击 → bat\导出部门角色数据.bat
```

**脚本会自动**：
1. ✅ 导出部门数据
2. ✅ 导出角色数据
3. ✅ 上传到服务器
4. ✅ SSH 登录并导入
5. ✅ 验证结果

---

#### **步骤 2：等待完成**

**全程自动化**，约 2-3 分钟完成！

**预期输出**：
```
✅ 部门数据导出成功
✅ 角色数据导出成功
✅ 部门数据上传成功
✅ 角色数据上传成功
✅ 导入完成 - 部门：10 条，角色：7 条
```

---

### **方式 2：手动执行** ⭐⭐⭐⭐

#### **步骤 1：本地导出**

**PowerShell 执行**：
```powershell
cd E:\EIMS2026

# 导出部门
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json

# 导出角色
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json
```

---

#### **步骤 2：上传到服务器**

```powershell
scp department_data.json root@39.106.41.239:/root/
scp role_data.json root@39.106.41.239:/root/
```

---

#### **步骤 3：SSH 导入**

```bash
# SSH 登录
ssh root@39.106.41.239

# 进入项目目录
cd /var/www/eims

# 激活虚拟环境
source venv/bin/activate

# 导入数据
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json

# 验证
python manage.py shell -c "from eims_app.models import Department, Role; print(f'部门:{Department.objects.count()}, 角色:{Role.objects.count()}')"
```

---

## 📊 迁移流程对比

| 步骤 | 自动方式 | 手动方式 |
|------|----------|----------|
| **导出** | ✅ 自动 | ⚠️ 手动执行命令 |
| **上传** | ✅ 自动 | ⚠️ 手动 SCP |
| **导入** | ✅ 自动 | ⚠️ 手动 SSH |
| **验证** | ✅ 自动 | ⚠️ 手动检查 |
| **时间** | 2-3 分钟 | 5-10 分钟 |

---

## 🎯 推荐方案选择

### **场景 1：快速迁移（推荐）**

```
双击 → bat\导出部门角色数据.bat
按提示操作
等待完成
```

**优点**：
- ✅ 全自动
- ✅ 一键完成
- ✅ 无需记忆命令

---

### **场景 2：学习理解**

```
1. 手动导出
2. 手动上传
3. 手动导入
4. 手动验证
```

**优点**：
- ✅ 理解原理
- ✅ 灵活控制
- ✅ 适合学习

---

## ⚠️ 常见问题

### **问题 1：SCP 无法使用**

**解决**：
```powershell
# 检查 SSH 连接
ssh root@39.106.41.239

# 如果 SSH 正常但 SCP 失败，使用 FTP 工具
WinSCP / FileZilla 上传文件
```

---

### **问题 2：ID 冲突**

**错误**：
```
UNIQUE constraint failed
```

**解决**：
```bash
# SSH 登录服务器
ssh root@39.106.41.239

# 清除现有数据
cd /var/www/eims
source venv/bin/activate
python manage.py shell << EOF
from eims_app.models import Department, Role
Department.objects.all().delete()
Role.objects.all().delete()
EOF

# 重新导入
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json
```

---

### **问题 3：数据量不匹配**

**检查**：
```powershell
# 本地统计
python manage.py shell -c "from eims_app.models import Department; print(f'本地：{Department.objects.count()}')"

# 服务器统计
ssh root@39.106.41.239 "cd /var/www/eims && python manage.py shell -c \"from eims_app.models import Department; print(f'服务器：{Department.objects.count()}')\""
```

---

##  验证清单

### **导入后检查**

- [ ] ✅ 部门数量一致
- [ ] ✅ 角色数量一致
- [ ] ✅ 部门名称正确
- [ ] ✅ 角色权限正常
- [ ] ✅ 网站显示正常
- [ ] ✅ 后台管理正常

---

## 📞 快速命令参考

### **自动迁移（推荐）**

```
双击 → bat\导出部门角色数据.bat
```

---

### **手动迁移**

```powershell
# 本地导出
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json

# 上传
scp *.json root@39.106.41.239:/root/

# 导入
ssh root@39.106.41.239
cd /var/www/eims
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json
```

---

## 📦 已创建的工具

| 文件 | 功能 | 使用方式 |
|------|------|----------|
| **部门角色数据迁移指南.md** | 完整指南（726 行） | 阅读参考 |
| **bat\导出部门角色数据.bat** | 自动导出导入脚本 | 双击执行 |
| **bat\导入部门角色数据.sh** | 服务器导入脚本 | SSH 执行 |

---

## 🎯 总结

### **最快方法**

```
1. 双击 → bat\导出部门角色数据.bat
2. 等待完成（2-3 分钟）
3. ✅ 完成！
```

---

### **效果**

✅ **部门数据完整迁移**  
✅ **角色数据完整迁移**  
✅ **关联关系保持**  
✅ **权限配置保持**  

---

**位置**：`E:\EIMS2026\部门角色数据迁移快速指南.md`  
**状态**：✅ 立即可用  
**下一步**：双击脚本，等待完成！

---

**使用自动化脚本，2 分钟完成数据迁移！** 🚀📊
