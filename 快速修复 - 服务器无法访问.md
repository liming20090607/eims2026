# 快速修复 - 服务器无法访问

## 问题说明

**症状：** 创建 admin 用户后网页打不开，看到端口 22

**原因：** 
- 端口 22 是 SSH 端口（用于登录服务器）
- Web 服务应该使用端口 8000
- Gunicorn 服务可能没有运行

---

## 🚀 快速修复步骤

### 方案一：一键自动修复（推荐）

#### 步骤 1：SSH 登录服务器

**使用工具：**
- Windows PowerShell: `ssh admin@39.106.41.239`
- 或使用脚本：双击 `E:\EIMS2026\bat\连接服务器排查.bat`

#### 步骤 2：上传修复脚本

在本地 PowerShell 执行：
```powershell
scp E:\EIMS2026\bat\修复服务器访问.sh admin@39.106.41.239:/tmp/
```

#### 步骤 3：在服务器上执行修复

SSH 登录后执行：
```bash
sudo su -
bash /tmp/修复服务器访问.sh
```

**脚本会自动：**
- ✅ 检查并启动 Gunicorn
- ✅ 检查并启动 Supervisor
- ✅ 检查端口 8000
- ✅ 配置防火墙
- ✅ 测试本地访问
- ✅ 显示错误日志

---

### 方案二：手动快速修复

#### 如果 Gunicorn 未运行

```bash
# 1. 启动 Supervisor
sudo systemctl start supervisord

# 2. 启动 EIMS 服务
sudo supervisorctl start eims

# 3. 检查状态
sudo supervisorctl status eims
```

#### 如果 Supervisor 未运行

```bash
# 1. 启动 Supervisor
sudo systemctl start supervisord

# 2. 设置开机自启
sudo systemctl enable supervisord

# 3. 检查状态
sudo systemctl status supervisord
```

#### 如果安全组未配置

**登录阿里云控制台：**
1. 访问：https://ecs.console.aliyun.com/
2. 找到实例 `39.106.41.239`
3. 安全组 → 配置规则 → 入方向
4. 添加规则：
   - 端口：8000/8000
   - 协议：TCP
   - 授权对象：0.0.0.0/0
5. 保存

---

## ✅ 验证修复

### 测试 1：浏览器访问

打开浏览器访问：
```
http://39.106.41.239:8000/admin/
```

**预期结果：** 看到 Django Admin 登录页面

### 测试 2：端口测试

在本地 PowerShell 执行：
```powershell
Test-NetConnection 39.106.41.239 -Port 8000
```

**预期结果：** `TcpTestSucceeded : True`

### 测试 3：curl 测试

SSH 登录后执行：
```bash
curl http://localhost:8000/admin/
```

**预期结果：** 返回 HTML 代码

---

## 🔍 常见问题

### Q1: 为什么端口显示 22？

**答：** 端口 22 是 SSH 端口，用于远程登录服务器。Web 服务使用端口 8000。

### Q2: 创建 admin 用户会影响服务吗？

**答：** 不会。创建用户本身不会影响 Gunicorn 服务。可能是巧合服务停止了。

### Q3: 如何防止服务停止？

**答：** 使用 Supervisor 管理 Gunicorn，设置自动重启：
```ini
[program:eims]
command=/var/www/eims/venv/bin/gunicorn --bind 0.0.0.0:8000 eims.wsgi:application
autostart=true
autorestart=true
```

### Q4: 安全组配置后多久生效？

**答：** 立即生效，无需重启。

---

## 📊 诊断流程图

```
开始
  ↓
SSH 登录服务器
  ↓
检查 Gunicorn 进程
  ├─ 运行中 → 检查端口 8000
  └─ 未运行 → 启动 Supervisor
                ↓
              启动 Gunicorn
                ↓
              检查状态
  ↓
检查 Supervisor 状态
  ├─ 运行中 → 检查服务状态
  └─ 未运行 → systemctl start supervisord
  ↓
检查端口 8000
  ├─ 已监听 → 检查防火墙
  └─ 未监听 → 查看错误日志
  ↓
检查防火墙
  ├─ 已开放 → 检查安全组
  └─ 未开放 → firewall-cmd --add-port=8000/tcp
  ↓
检查阿里云安全组
  ├─ 已配置 → 测试访问
  └─ 未配置 → 添加安全组规则
  ↓
测试访问
  ├─ 成功 → ✅ 完成
  └─ 失败 → 查看详细日志
```

---

## 📝 检查清单

- [ ] SSH 可以登录服务器
- [ ] Gunicorn 进程正在运行
- [ ] Supervisor 服务正常
- [ ] 端口 8000 正在监听
- [ ] 防火墙开放 8000 端口
- [ ] 阿里云安全组配置了 8000 端口
- [ ] 本地可以 curl 访问
- [ ] 浏览器可以访问

---

##  紧急联系

如果以上方法都无法解决，请提供：

1. **执行修复脚本的完整输出**
2. **Gunicorn 进程列表：** `ps aux | grep gunicorn`
3. **Supervisor 状态：** `supervisorctl status eims`
4. **错误日志：** `tail -20 /var/log/eims/error.log`
5. **端口监听状态：** `netstat -tlnp | grep 8000`
6. **安全组配置截图**

---

**最后更新：** 2026-04-03  
**适用版本：** Django 4.2.7, Gunicorn, Supervisor  
**服务器：** Alibaba Cloud CentOS
