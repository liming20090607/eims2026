# 项目月报模糊搜索功能说明

## 功能概述

在协同办公系统的项目月报列表页面添加了**模糊搜索功能**，帮助用户快速从大量项目中找到目标项目。

## 实现细节

### 1. 后端修改

**文件**: `eims_app/views/views_monthly_report.py`

#### 主要变更：

1. **接收搜索参数**（第 282 行）
   ```python
   search_query = request.GET.get('search', '').strip()
   ```

2. **执行模糊搜索**（第 287-292 行）
   ```python
   if search_query:
       projects = projects.filter(
           Q(project_code__icontains=search_query) |
           Q(project_name__icontains=search_query)
       )
   ```
   
   - 支持按**项目编号**模糊搜索
   - 支持按**项目名称**模糊搜索
   - 使用 `icontains` 实现不区分大小写的模糊匹配

3. **传递搜索关键词到模板**（第 344 行）
   ```python
   'search_query': search_query,
   ```

### 2. 前端修改

**文件**: `eims_app/templates/monthly_report/dashboard.html`

#### 主要变更：

1. **添加搜索框**（第 289-320 行）
   - 搜索输入框，带占位符提示
   - 搜索按钮
   - 清除按钮（仅在有搜索内容时显示）
   - 搜索结果计数显示

2. **表格 ID 标识**（第 323 行）
   ```html
   <table class="table table-hover table-sm all-projects-table" id="projectsTable">
   ```

3. **JavaScript 实时搜索增强**（第 435-473 行）
   - **防抖处理**：输入后延迟 500ms 自动提交，避免频繁请求
   - **Enter 键搜索**：按 Enter 键立即提交搜索
   - **空值处理**：输入为空时不自动提交

## 功能特性

### ✅ 核心功能

1. **模糊搜索**
   - 支持项目编号部分匹配
   - 支持项目名称部分匹配
   - 不区分大小写

2. **用户友好界面**
   - 直观的搜索框设计
   - 实时显示搜索结果数量
   - 一键清除搜索条件

3. **智能交互**
   - 输入时自动搜索（延迟 500ms）
   - 支持手动点击搜索按钮
   - 支持 Enter 键快速搜索

### 🎯 使用场景

- **场景 1**：用户知道项目编号的一部分
  - 输入：`2024` 
  - 结果：显示所有包含 "2024" 的项目编号

- **场景 2**：用户记得项目名称的关键词
  - 输入：`地铁`
  - 结果：显示所有名称中包含 "地铁" 的项目

- **场景 3**：用户想快速定位特定项目
  - 输入：完整或部分项目名称
  - 结果：精确或模糊匹配的项目列表

## 技术优势

1. **性能优化**
   - 使用数据库级别的模糊查询（`icontains`）
   - 防抖机制减少不必要的请求
   - 仅在必要时执行搜索

2. **用户体验**
   - 实时反馈搜索结果
   - 保留搜索状态（刷新页面后仍显示搜索词）
   - 清晰的视觉提示

3. **代码质量**
   - 遵循 Django 最佳实践
   - 使用 Q 对象构建复杂查询
   - 良好的代码注释和文档

## 部署说明

### 本地测试

1. 启动开发服务器
   ```bash
   python manage.py runserver
   ```

2. 访问月度填报提醒页面
   ```
   http://localhost:8000/eims/monthly-report/dashboard/
   ```

3. 测试搜索功能
   - 在搜索框中输入项目编号或名称
   - 观察搜索结果实时更新
   - 点击清除按钮重置搜索

### 云服务器部署

1. 同步代码到服务器
   ```bash
   ssh root@39.106.41.239
   cd /var/www/eims
   git pull origin main
   ```

2. 重启服务
   ```bash
   supervisorctl restart eims
   ```

3. 验证功能
   - 访问云服务器的月度填报提醒页面
   - 测试搜索功能是否正常工作

## 注意事项

1. **搜索范围**
   - 当前仅搜索"所有项目"列表
   - 不影响"待填报项目"、"草稿箱"、"已逾期报告"等其他区域

2. **性能考虑**
   - 对于超大数据集（>10000 个项目），建议添加索引
   - 当前实现适用于中小规模项目库

3. **扩展性**
   - 如需增加更多搜索字段，只需在视图的 Q 对象中添加条件
   - 例如：`Q(project_manager__icontains=search_query)`

## 未来优化方向

1. **高级搜索**
   - 添加筛选条件（合同类别、项目状态等）
   - 组合搜索（多条件 AND/OR）

2. **搜索结果高亮**
   - 在结果中高亮显示匹配的关键词
   - 提升用户识别效率

3. **搜索历史**
   - 记录用户常用搜索词
   - 提供搜索建议

4. **AJAX 异步搜索**
   - 无刷新更新搜索结果
   - 更流畅的用户体验

---

**更新日期**: 2026-03-21  
**版本**: v1.0  
**作者**: EIMS 开发团队
