# 服务器无法访问问题排查与修复

## 问题描述

**症状：**
- 创建 admin 用户后，网页打不开了
- 看到的端口是 22（SSH 端口）
- 无法访问 Django 应用

---

## 🔍 问题分析

### 端口说明

| 端口 | 用途 | 说明 |
|------|------|------|
| **22** | SSH | 用于远程登录服务器 |
| **8000** | Django/Gunicorn | Web 应用端口 |
| **80** | Nginx（可选） | HTTP 反向代理 |

**重要：** 端口 22 是 SSH 登录端口，**不是** Web 服务端口！

---

## 📋 排查步骤

### 方案一：使用自动排查脚本

#### 步骤 1：SSH 登录服务器

**方法 A：使用脚本**
```
双击：E:\EIMS2026\bat\连接服务器排查.bat
```

**方法 B：手动连接**
```bash
ssh admin@39.106.41.239
```

#### 步骤 2：上传排查脚本

```bash
# 在本地 PowerShell 执行
scp E:\EIMS2026\bat\服务器问题排查.py admin@39.106.41.239:/tmp/
```

#### 步骤 3：在服务器上执行排查

```bash
# SSH 登录后
sudo python /tmp/服务器问题排查.py
```

---

### 方案二：手动排查

#### Step 1: 检查 Gunicorn 是否运行

```bash
# 检查进程
ps aux | grep gunicorn

# 检查端口
sudo netstat -tlnp | grep 8000
```

**预期结果：**
```
root     1234  0.0  0.5  xxxxx  gunicorn: master
admin    1235  0.1  1.0  xxxxx  gunicorn: worker
admin    1236  0.1  1.0  xxxxx  gunicorn: worker
admin    1237  0.1  1.0  xxxxx  gunicorn: worker
tcp        0      0 0.0.0.0:8000    0.0.0.0:*    LISTEN      1234/python
```

**如果没有 Gunicorn 进程：**
```bash
# 启动 Supervisor
sudo systemctl start supervisord

# 或启动 Gunicorn
cd /var/www/eims
source venv/bin/activate
gunicorn --bind 0.0.0.0:8000 eims.wsgi:application
```

---

#### Step 2: 检查 Supervisor 状态

```bash
# 查看 Supervisor 状态
sudo systemctl status supervisord

# 查看 EIMS 服务状态
sudo supervisorctl status eims

# 重启 EIMS 服务
sudo supervisorctl restart eims
```

**正常状态应显示：**
```
eims                             RUNNING   PID 1234
```

**如果显示 STOPPED 或 FATAL：**
```bash
# 查看日志
sudo tail -f /var/log/supervisor/eims.err.log

# 启动服务
sudo supervisorctl start eims
```

---

#### Step 3: 检查阿里云安全组

**登录阿里云控制台：**

1. 访问：https://ecs.console.aliyun.com/
2. 找到您的实例：`39.106.41.239`
3. 点击 **安全组** → **配置规则**
4. 检查 **入方向** 规则

**需要添加的规则：**

| 端口范围 | 协议 | 授权对象 | 说明 |
|---------|------|---------|------|
| 8000/8000 | TCP | 0.0.0.0/0 | Django 应用 |
| 80/80 | TCP | 0.0.0.0/0 | HTTP（可选） |
| 443/443 | TCP | 0.0.0.0/0 | HTTPS（可选） |

**添加安全组规则步骤：**
1. 点击 **手动添加**
2. 优先级：1
3. 策略：允许
4. 协议类型：TCP
5. 端口范围：8000/8000
6. 授权对象：0.0.0.0/0
7. 描述：Django 应用
8. 点击 **保存**

---

#### Step 4: 检查防火墙

```bash
# 查看防火墙状态
sudo firewall-cmd --state

# 如果防火墙开启，添加端口
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload

# 验证
sudo firewall-cmd --list-ports
```

---

#### Step 5: 检查 Django 配置

```bash
# 编辑 settings.py
sudo nano /var/www/eims/settings.py
```

**确保 ALLOWED_HOSTS 配置正确：**

```python
# 生产环境配置
ALLOWED_HOSTS = [
    'localhost',
    '127.0.0.1',
    '39.106.41.239',  # 服务器公网 IP
    'your-domain.com',  # 如果有域名
]
```

---

#### Step 6: 查看日志

```bash
# Gunicorn 错误日志
sudo tail -f /var/log/eims/error.log

# Supervisor 错误日志
sudo tail -f /var/log/supervisor/eims.err.log

# Django 日志
sudo tail -f /var/log/eims/django.log

# Nginx 日志（如果使用）
sudo tail -f /var/log/nginx/error.log
```

---

## 🔧 快速修复命令

### 场景 1：Gunicorn 未运行

```bash
sudo systemctl start supervisord
sudo supervisorctl start eims
```

### 场景 2：Supervisor 配置丢失

```bash
# 重新创建 Supervisor 配置
sudo nano /etc/supervisord.d/eims.ini
```

内容：
```ini
[program:eims]
command=/var/www/eims/venv/bin/gunicorn --workers 3 --bind 0.0.0.0:8000 eims.wsgi:application
directory=/var/www/eims
user=admin
autostart=true
autorestart=true
stderr_logfile=/var/log/eims/error.log
stdout_logfile=/var/log/eims/access.log
```

然后：
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start eims
```

### 场景 3：端口被占用

```bash
# 查看谁在使用 8000 端口
sudo lsof -i :8000

# 杀死进程
sudo kill -9 <PID>

# 重启 Gunicorn
sudo supervisorctl restart eims
```

### 场景 4：虚拟环境问题

```bash
cd /var/www/eims
source venv/bin/activate

# 测试 Django 是否能运行
python manage.py runserver 0.0.0.0:8000

# 如果正常，Ctrl+C 停止，然后用 Supervisor 启动
sudo supervisorctl restart eims
```

---

## ✅ 验证修复

### 测试 1：本地访问

在服务器上使用 curl 测试：
```bash
curl http://localhost:8000/admin/
```

**预期：** 返回 HTML 内容

### 测试 2：远程访问

在浏览器访问：
```
http://39.106.41.239:8000/admin/
```

**预期：** 看到 Django Admin 登录页面

### 测试 3：端口连通性

在本地 PowerShell 测试：
```powershell
Test-NetConnection 39.106.41.239 -Port 8000
```

**预期：** `TcpTestSucceeded : True`

---

## 📊 常见问题速查表

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| 连接超时 | 安全组未开放 | 添加安全组规则 |
| 连接被拒绝 | Gunicorn 未运行 | 启动 Supervisor |
| 502 Bad Gateway | Gunicorn 崩溃 | 查看日志，修复错误 |
| 404 Not Found | Django 配置错误 | 检查 ALLOWED_HOSTS |
| 权限错误 | 文件权限问题 | `sudo chown -R admin:admin /var/www/eims` |

---

##  紧急恢复流程

如果以上方法都无效：

### 1. 重启服务器

```bash
sudo reboot
```

### 2. 重新启动所有服务

```bash
sudo systemctl start supervisord
sudo supervisorctl start all
```

### 3. 手动启动 Django（临时）

```bash
cd /var/www/eims
source venv/bin/activate
nohup python manage.py runserver 0.0.0.0:8000 > /tmp/django.log 2>&1 &
```

---

## 📝 预防措施

1. **定期检查服务状态**
   ```bash
   sudo supervisorctl status
   sudo systemctl status supervisord
   ```

2. **设置日志轮转**
   ```bash
   sudo logrotate -f /etc/logrotate.d/eims
   ```

3. **监控端口**
   ```bash
   sudo netstat -tlnp | grep 8000
   ```

4. **定期备份配置**
   ```bash
   sudo cp /etc/supervisord.d/eims.ini /backup/
   sudo cp /var/www/eims/settings.py /backup/
   ```

---

## 📞 需要帮助？

提供以下信息以便进一步诊断：

1. **排查脚本输出**
2. **Gunicorn 进程列表**
3. **Supervisor 状态**
4. **错误日志内容**
5. **安全组配置截图**

---

**最后更新：** 2026-04-03  
**适用环境：** Django 4.2.7, Gunicorn, Supervisor, Alibaba Cloud
