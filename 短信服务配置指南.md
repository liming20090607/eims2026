# 📱 阿里云短信服务配置指南

## ✅ 已完成配置

以下配置已完成，无需修改：

- ✅ 阿里云 SDK 已安装（aliyun-python-sdk-core + aliyun-python-sdk-dysmsapi）
- ✅ settings.py 已添加短信配置项
- ✅ sms_service.py 已集成阿里云短信 API
- ✅ 短信模板已创建并审核通过（SMS_333830134）
- ✅ 短信签名已设置（协同AI办公系统）

## 🔑 下一步：获取 AccessKey

### 第 1 步：访问 AccessKey 管理页面

打开浏览器，访问：
```
https://ram.console.aliyun.com/manage/ak
```

### 第 2 步：创建 AccessKey

1. 如果提示需要创建 RAM 用户，点击"创建 AccessKey"
2. 选择"开始使用子用户 AccessKey"（推荐，更安全）
3. 创建 RAM 用户：
   - 登录名称：eims-sms-user
   - 显示名称：EIMS短信服务用户
4. 为该用户添加权限：
   - 搜索并添加：`AliyunDysmsFullAccess`（短信服务完全访问权限）
5. 点击"确定"创建
6. **重要：立即复制并保存 AccessKey ID 和 AccessKey Secret**
   - AccessKey ID：以 `LTAI` 开头
   - AccessKey Secret：一串随机字符
   - ⚠️ Secret 只显示一次，务必保存！

### 第 3 步：配置到系统

1. 打开文件：`e:\EIMS2026\.env`

2. 找到这两行：
```ini
ALIYUN_ACCESS_KEY_ID=your_access_key_id_here
ALIYUN_ACCESS_KEY_SECRET=your_access_key_secret_here
```

3. 替换为您的实际 AccessKey：
```ini
ALIYUN_ACCESS_KEY_ID=LTAI5tXXXXXXXXXXXXXXXX
ALIYUN_ACCESS_KEY_SECRET=your_actual_secret_key_here
```

4. 保存文件

## 🧪 测试短信发送

### 方法 1：通过 Web 界面测试

1. 访问：http://127.0.0.1:8000/my-account/
2. 输入您的手机号
3. 点击"获取验证码"
4. 如果配置正确，您将收到真实的短信验证码
5. 如果配置有误，服务器会回退到开发模式，验证码将打印在控制台

### 方法 2：通过 Python 命令行测试

打开 PowerShell，执行：

```powershell
cd e:\EIMS2026
python manage.py shell
```

然后在 Python 交互式命令行中输入：

```python
from eims_app.sms_service import SMSService

# 替换为您的手机号
success, message = SMSService.send_sms('13800138000', 'login')
print(f'发送结果: {success}')
print(f'消息: {message}')

exit()
```

## 📊 查看短信发送记录

阿里云控制台查看：
```
https://dysms.console.aliyun.com/dysms.htm#/domestic
```

1. 左侧菜单：国内消息 → 发送记录查询
2. 可以看到所有发送记录、状态、费用等

## ⚠️ 常见问题

### Q1: 短信发送失败怎么办？

检查以下几点：
1. AccessKey ID 和 Secret 是否正确
2. RAM 用户是否有 `AliyunDysmsFullAccess` 权限
3. 短信签名是否审核通过
4. 短信模板是否审核通过
5. 手机号格式是否正确（中国大陆：1开头，11位数字）

### Q2: 如何查看短信费用？

阿里云控制台：
```
https://usercenter2.aliyun.com/expense-bill/overview
```

### Q3: 开发环境和生产环境配置一样吗？

是的，配置方式相同。只需将 `.env` 文件复制到生产服务器即可。

## 📝 配置检查清单

在测试前，请确认：

- [ ] 已购买短信套餐包（500条 ¥10）
- [ ] 短信签名已审核通过
- [ ] 短信模板已审核通过（SMS_333830134）
- [ ] 已创建 AccessKey
- [ ] 已在 .env 中配置 AccessKey ID 和 Secret
- [ ] RAM 用户有短信服务权限
- [ ] 阿里云 SDK 已安装（已完成 ✅）

## 🎯 配置完成后

配置完成后，您的 EIMS 系统将能够：

✅ 发送真实的短信验证码  
✅ 支持短信登录  
✅ 支持忘记密码功能  
✅ 支持手机号绑定/修改  
✅ 自动记录发送日志  
✅ 防止频繁发送（60秒间隔）  
✅ 每日发送次数限制（10次/天）  

---

## 💡 需要帮助？

如果您在配置过程中遇到任何问题，请随时告诉我！
