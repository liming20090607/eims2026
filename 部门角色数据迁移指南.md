# 部门与角色数据迁移完全指南

**目标**：将本地部门管理、角色分配数据导入到服务器  
**适用**：本地数据已建立，需要迁移到生产环境

---

## 📋 迁移方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **方案 1**：Django dumpdata | ✅ 官方工具<br>✅ 完整数据<br>✅ 简单快速 | ⚠️ 需要环境一致 | ⭐⭐⭐⭐⭐ |
| **方案 2**：Excel 导入导出 | ✅ 可视化管理<br>✅ 可编辑<br>✅ 易理解 | ⚠️ 需要导入功能 | ⭐⭐⭐⭐ |
| **方案 3**：SQL 直接导入 | ✅ 快速<br>✅ 灵活 | ⚠️ 风险高<br>⚠️ 需专业知识 | ⭐⭐⭐ |

---

## 🎯 方案 1：Django 数据导出导入（推荐）

### **步骤 1：本地导出数据**

#### **1.1 导出部门数据**

**在本地执行**：
```powershell
cd E:\EIMS2026

# 导出部门数据
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json
```

**生成的文件**：
```json
[
  {
    "model": "eims_app.department",
    "pk": 1,
    "fields": {
      "department_code": "DEV001",
      "department_name": "研发部",
      "department_type": "functional",
      "manager_name": "张三",
      "contact_phone": "13800138000",
      "contact_email": "dev@company.com",
      "description": "负责产品研发",
      "status": "active",
      "order": 1
    }
  }
]
```

---

#### **1.2 导出角色数据**

**在本地执行**：
```powershell
# 导出角色数据
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json
```

---

#### **1.3 导出所有相关数据**

**一次性导出所有数据**：
```powershell
# 导出部门、角色、人员数据
python manage.py dumpdata eims_app.Department eims_app.Role eims_app.Personnel --indent 2 > all_data.json
```

---

### **步骤 2：上传数据到服务器**

#### **方式 1：使用 SCP（推荐）**

**PowerShell 执行**：
```powershell
# 上传数据文件
scp department_data.json root@39.106.41.239:/root/
scp role_data.json root@39.106.41.239:/root/
```

---

#### **方式 2：使用 FTP 工具**

**使用 WinSCP / FileZilla**：
1. 连接服务器：`39.106.41.239`
2. 用户：`root`
3. 密码：您的密码
4. 上传文件到：`/root/`

---

### **步骤 3：服务器导入数据**

#### **3.1 SSH 登录服务器**

```bash
ssh root@39.106.41.239
```

---

#### **3.2 进入项目目录**

```bash
cd /var/www/eims
```

---

#### **3.3 导入部门数据**

```bash
# 导入部门数据
python manage.py loaddata /root/department_data.json
```

**预期输出**：
```
Installed 10 object(s) from 1 fixture(s)
```

---

#### **3.4 导入角色数据**

```bash
# 导入角色数据
python manage.py loaddata /root/role_data.json
```

**预期输出**：
```
Installed 7 object(s) from 1 fixture(s)
```

---

### **步骤 4：验证导入**

#### **4.1 查看部门数据**

```bash
# 进入 Django shell
python manage.py shell

# 执行 Python 代码
from eims_app.models import Department
print(f"部门总数：{Department.objects.count()}")

# 查看所有部门
for dept in Department.objects.all()[:5]:
    print(f"{dept.department_code} - {dept.department_name}")
```

---

#### **4.2 查看角色数据**

```bash
from eims_app.models import Role
print(f"角色总数：{Role.objects.count()}")

for role in Role.objects.all():
    print(f"{role.name} - {role.get_role_display()}")
```

---

## 🎯 方案 2：Excel 导入导出

### **步骤 1：本地导出为 Excel**

#### **创建导出脚本** `export_to_excel.py`

```python
import os
import django
import pandas as pd

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings')
django.setup()

from eims_app.models import Department, Role

# 导出部门
departments = Department.objects.values()
df_dept = pd.DataFrame.from_records(departments)
df_dept.to_excel('部门数据.xlsx', index=False)
print(f"✅ 导出 {len(df_dept)} 个部门")

# 导出角色
roles = Role.objects.values()
df_role = pd.DataFrame.from_records(roles)
df_role.to_excel('角色数据.xlsx', index=False)
print(f"✅ 导出 {len(df_role)} 个角色")

print("🎉 导出完成！")
```

---

#### **执行导出**

```powershell
# 安装依赖
pip install pandas openpyxl

# 执行导出
python export_to_excel.py
```

---

### **步骤 2：上传 Excel 到服务器**

```powershell
scp 部门数据.xlsx root@39.106.41.239:/root/
scp 角色数据.xlsx root@39.106.41.239:/root/
```

---

### **步骤 3：服务器导入 Excel**

#### **创建导入脚本** `/root/import_from_excel.py`

```python
#!/usr/bin/env python
import os
import sys
import django
import pandas as pd

# 配置 Django 环境
sys.path.append('/var/www/eims')
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings')
django.setup()

from eims_app.models import Department, Role

def import_departments(excel_file):
    """导入部门数据"""
    print(f"正在导入部门数据：{excel_file}")
    
    df = pd.read_excel(excel_file)
    count = 0
    
    for index, row in df.iterrows():
        try:
            dept, created = Department.objects.update_or_create(
                department_code=row['department_code'],
                defaults={
                    'department_name': row['department_name'],
                    'department_type': row.get('department_type', 'functional'),
                    'manager_name': row.get('manager_name', ''),
                    'contact_phone': row.get('contact_phone', ''),
                    'contact_email': row.get('contact_email', ''),
                    'description': row.get('description', ''),
                    'status': row.get('status', 'active'),
                    'order': row.get('order', 0),
                }
            )
            count += 1
            print(f"  ✅ {dept.department_code} - {dept.department_name}")
        except Exception as e:
            print(f"  ❌ 导入失败：{e}")
    
    print(f"✅ 部门导入完成：{count}/{len(df)}")
    return count

def import_roles(excel_file):
    """导入角色数据"""
    print(f"正在导入角色数据：{excel_file}")
    
    df = pd.read_excel(excel_file)
    count = 0
    
    for index, row in df.iterrows():
        try:
            role, created = Role.objects.update_or_create(
                name=row['name'],
                defaults={
                    'description': row.get('description', ''),
                    'permissions': row.get('permissions', ''),
                }
            )
            count += 1
            print(f"  ✅ {role.get_role_display()}")
        except Exception as e:
            print(f"  ❌ 导入失败：{e}")
    
    print(f"✅ 角色导入完成：{count}/{len(df)}")
    return count

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print("用法：python import_from_excel.py <excel 文件>")
        print("示例：python import_from_excel.py 部门数据.xlsx")
        sys.exit(1)
    
    excel_file = sys.argv[1]
    
    if '部门' in excel_file:
        import_departments(excel_file)
    elif '角色' in excel_file:
        import_roles(excel_file)
    else:
        print("❌ 无法识别文件类型，请确保文件名包含'部门'或'角色'")
```

---

#### **执行导入**

```bash
# SSH 登录
ssh root@39.106.41.239

# 进入目录
cd /var/www/eims

# 安装依赖
pip install pandas openpyxl

# 导入部门
python /root/import_from_excel.py /root/部门数据.xlsx

# 导入角色
python /root/import_from_excel.py /root/角色数据.xlsx
```

---

## 🎯 方案 3：SQL 直接导入

### **步骤 1：本地导出 SQL**

```powershell
# 使用 Django 导出 SQL
python manage.py dumpdata eims_app.Department --indent 2 > department.sql
```

---

### **步骤 2：上传到服务器**

```powershell
scp department.sql root@39.106.41.239:/root/
```

---

### **步骤 3：服务器导入**

```bash
# 登录数据库（如果是 MySQL）
mysql -u root -p

# 使用数据库
use eims;

# 导入 SQL
source /root/department.sql;
```

---

## 🚀 快速部署脚本

### **数据迁移自动化脚本**

#### **本地脚本** `migrate_data.bat`

```batch
@echo off
chcp 65001 >nul
echo ======================================
echo EIMS 数据迁移工具 - 导出
echo ======================================
echo.

echo [1/3] 导出部门数据...
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json
if %ERRORLEVEL% EQU 0 (
    echo   ✅ 部门数据导出成功
) else (
    echo   ❌ 部门数据导出失败
)

echo.
echo [2/3] 导出角色数据...
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json
if %ERRORLEVEL% EQU 0 (
    echo   ✅ 角色数据导出成功
) else (
    echo   ❌ 角色数据导出失败
)

echo.
echo [3/3] 上传到服务器...
scp department_data.json root@39.106.41.239:/root/
scp role_data.json root@39.106.41.239:/root/

if %ERRORLEVEL% EQU 0 (
    echo   ✅ 数据上传成功
) else (
    echo   ❌ 数据上传失败
)

echo.
echo ======================================
echo 导出完成！
echo ======================================
echo.
echo 下一步：
echo   1. SSH 登录服务器：ssh root@39.106.41.239
echo   2. 执行导入命令：
echo      cd /var/www/eims
echo      python manage.py loaddata /root/department_data.json
echo      python manage.py loaddata /root/role_data.json
echo.

pause
```

---

#### **服务器脚本** `/root/import_data.sh`

```bash
#!/bin/bash

echo "======================================"
echo "EIMS 数据导入工具
echo "======================================"
echo ""

# 检查文件是否存在
if [ ! -f "/root/department_data.json" ]; then
    echo "❌ 部门数据文件不存在"
    exit 1
fi

if [ ! -f "/root/role_data.json" ]; then
    echo "❌ 角色数据文件不存在"
    exit 1
fi

# 进入项目目录
cd /var/www/eims

# 激活虚拟环境
source venv/bin/activate

echo "[1/2] 导入部门数据..."
python manage.py loaddata /root/department_data.json

if [ $? -eq 0 ]; then
    echo "✅ 部门数据导入成功"
else
    echo "❌ 部门数据导入失败"
    exit 1
fi

echo ""
echo "[2/2] 导入角色数据..."
python manage.py loaddata /root/role_data.json

if [ $? -eq 0 ]; then
    echo "✅ 角色数据导入成功"
else
    echo "❌ 角色数据导入失败"
    exit 1
fi

echo ""
echo "======================================"
echo "✅ 数据导入完成！
echo "======================================"
echo ""

# 验证
echo "验证数据："
python manage.py shell << EOF
from eims_app.models import Department, Role
print(f"部门总数：{Department.objects.count()}")
print(f"角色总数：{Role.objects.count()}")
EOF

echo ""
```

---

## 📊 完整迁移流程

### **推荐流程（使用 Django dumpdata）**

#### **阶段 1：本地准备**

```powershell
# 1. 导出数据
cd E:\EIMS2026
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json

# 2. 验证导出
dir *.json
```

---

#### **阶段 2：上传数据**

```powershell
# 上传到服务器
scp department_data.json root@39.106.41.239:/root/
scp role_data.json root@39.106.41.239:/root/
```

---

#### **阶段 3：服务器导入**

```bash
# SSH 登录
ssh root@39.106.41.239

# 进入项目目录
cd /var/www/eims

# 激活虚拟环境
source venv/bin/activate

# 导入数据
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json

# 验证
python manage.py shell -c "from eims_app.models import Department; print(f'部门：{Department.objects.count()}')"
```

---

## ⚠️ 常见问题

### **问题 1：ID 冲突**

**错误**：
```
Problem installing fixture: Could not load Department(pk=1): UNIQUE constraint failed
```

**解决**：
```bash
# 清除现有数据后导入
python manage.py shell << EOF
from eims_app.models import Department, Role
Department.objects.all().delete()
Role.objects.all().delete()
EOF

# 重新导入
python manage.py loaddata department_data.json
```

---

### **问题 2：外键依赖**

**错误**：
```
Problem installing fixture: Could not load Personnel(pk=1): Foreign Key constraint
```

**解决**：
```bash
# 按顺序导入：先部门，后人员
python manage.py loaddata department_data.json
python manage.py loaddata role_data.json
# 最后导入人员（如果需要）
python manage.py loaddata personnel_data.json
```

---

### **问题 3：数据格式不一致**

**错误**：
```
ValueError: invalid literal for int() with base 10
```

**解决**：
```bash
# 检查 JSON 文件格式
cat department_data.json | python -m json.tool

# 确保本地和服务器 Django 版本一致
python manage.py version
```

---

## 🎉 验证清单

### **导入后验证**

- [ ] ✅ 部门数量正确
- [ ] ✅ 角色数量正确
- [ ] ✅ 部门名称显示正常
- [ ] ✅ 角色权限正常
- [ ] ✅ 人员部门关联正常
- [ ] ✅ 审批流程正常
- [ ] ✅ 前端显示正常

---

### **功能测试**

```bash
# 1. 查看部门列表
python manage.py shell << EOF
from eims_app.models import Department
for dept in Department.objects.all()[:5]:
    print(f"{dept.department_code} - {dept.department_name}")
EOF

# 2. 查看角色列表
python manage.py shell << EOF
from eims_app.models import Role
for role in Role.objects.all():
    print(f"{role.name} - {role.get_role_display()}")
EOF

# 3. 访问网站测试
浏览器访问：https://yourdomain.com/admin/
```

---

## 📞 快速命令参考

### **本地导出**

```powershell
# 导出部门
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json

# 导出角色
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json

# 上传
scp *.json root@39.106.41.239:/root/
```

---

### **服务器导入**

```bash
# 导入部门
python manage.py loaddata /root/department_data.json

# 导入角色
python manage.py loaddata /root/role_data.json

# 验证
python manage.py shell -c "from eims_app.models import Department, Role; print(f'部门:{Department.objects.count()}, 角色:{Role.objects.count()}')"
```

---

## 🎯 总结

### **推荐方案**

**方案 1：Django dumpdata/loaddata** ⭐⭐⭐⭐⭐

**优点**：
- ✅ Django 官方工具
- ✅ 数据完整可靠
- ✅ 操作简单快速
- ✅ 自动处理关联

**步骤**：
```powershell
# 本地导出
python manage.py dumpdata eims_app.Department --indent 2 > department_data.json
python manage.py dumpdata eims_app.Role --indent 2 > role_data.json

# 上传
scp *.json root@39.106.41.239:/root/

# 服务器导入
ssh root@39.106.41.239
cd /var/www/eims
python manage.py loaddata /root/department_data.json
python manage.py loaddata /root/role_data.json
```

---

### **效果**

✅ **部门数据完整迁移**  
✅ **角色数据完整迁移**  
✅ **关联关系保持**  
✅ **权限配置保持**  

---

**位置**：`E:\EIMS2026\部门角色数据迁移指南.md`  
**状态**：✅ 立即可用  
**下一步**：导出数据 → 上传 → 导入 → 验证！

---

**使用 Django 数据迁移工具，安全快速完成数据导入！** 🚀📊
