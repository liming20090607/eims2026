# EIMS 本地系统同步到云服务器完整指南

## 📋 服务器与仓库信息

### 云服务器配置
- **云服务器 IP**: 39.106.41.239
- **SSH 用户名**: root
- **代码路径**: /var/www/eims
- **MySQL 数据库**: eims
- **MySQL 用户名**: root
- **MySQL 密码**: root123（按本指南设置）

### Gitee 仓库配置
- **仓库地址**: https://gitee.com/liming20090607/eims2026.git
- **仓库用户名**: liming20090607
- **分支**: main（或 master）
- **访问方式**: HTTPS（需要 Gitee 账号密码或 Access Token）

---

## 🚀 完整同步流程

### 第一阶段：设置 MySQL root 密码

**目的**：在云服务器上创建 MySQL root 用户并设置简单密码

#### 步骤 1：上传密码设置脚本到服务器

在本地 Git Bash 中执行：

```bash
scp setup_mysql_root_password.sh root@39.106.41.239:/root/
```

#### 步骤 2：SSH 登录服务器

```bash
ssh root@39.106.41.239
```

#### 步骤 3：执行密码设置脚本

```bash
cd /root
bash setup_mysql_root_password.sh
```

**脚本将自动完成**：
- ✅ 检查 MySQL 服务状态
- ✅ 设置 root 密码为 `root123`
- ✅ 创建 eims 数据库
- ✅ 验证密码是否正确

**如果遇到密码重置问题**，手动执行：

```bash
# 1. 停止 MySQL
systemctl stop mysqld

# 2. 跳过权限表启动
mysqld_safe --skip-grant-tables &

# 3. 登录并重置密码
mysql -u root
FLUSH PRIVILEGES;
ALTER USER 'root'@'localhost' IDENTIFIED BY 'root123';
FLUSH PRIVILEGES;
exit;

# 4. 重启 MySQL
systemctl restart mysqld

# 5. 测试
mysql -u root -proot123
```

#### 步骤 4：验证 MySQL 连接

```bash
mysql -u root -proot123 -e "SHOW DATABASES;"
```

应该看到 `eims` 数据库在列表中。

---

### 第二阶段：配置 Django 使用 MySQL

**目的**：修改 Django 配置从 SQLite 切换到 MySQL

#### 步骤 5：在服务器上创建 production settings

SSH 到服务器后执行：

```bash
cd /var/www/eims
nano settings_production.py
```

粘贴以下内容：

```python
"""EIMS 生产环境配置"""
from .settings import *

# 关闭调试模式
DEBUG = False

# 允许的主机
ALLOWED_HOSTS = ['39.106.41.239', 'localhost', '127.0.0.1']

# MySQL 数据库配置
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'eims',
        'USER': 'root',
        'PASSWORD': 'root123',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
            'charset': 'utf8mb4',
        },
    }
}

# 静态文件配置
STATIC_ROOT = '/var/www/eims/staticfiles'

# 安全设置
SESSION_COOKIE_SECURE = False  # 如果使用 HTTP
CSRF_COOKIE_SECURE = False

# 日志配置
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file': {
            'level': 'ERROR',
            'class': 'logging.FileHandler',
            'filename': '/var/www/eims/logs/django_error.log',
        },
    },
    'root': {
        'handlers': ['file'],
        'level': 'ERROR',
    },
}
```

保存并退出（Ctrl+O, Enter, Ctrl+X）

---

### 第三阶段：同步代码和数据

#### 步骤 6：推送本地代码到 Gitee

#### 6.1 配置 Gitee 凭证（首次使用需要）

**方法 A：使用 HTTPS 账号密码（简单）**

每次推送时会提示输入 Gitee 账号和密码：
```bash
# 用户名：liming20090607
# 密码：您的 Gitee 登录密码
```

**方法 B：使用 Gitee Access Token（推荐，更安全）**

1. 登录 Gitee，进入：设置 → 私人令牌 → 生成新令牌
2. 勾选权限：`projects`（项目）、`push_code`（推送代码）
3. 复制生成的 Token（格式：`xxxxxxxxxxxxxxxxxxxx`）
4. 在本地配置：

```bash
# 方式 1：在 Git URL 中嵌入 Token（不推荐，会暴露 Token）
git remote set-url origin https://<您的Token>@gitee.com/liming20090607/eims2026.git

# 方式 2：使用 Git Credential Helper（推荐）
git config --global credential.helper store
# 第一次推送时会保存凭据到 ~/.git-credentials
```

#### 6.2 推送代码

```bash
# 如果已配置 Git 仓库
git add .
git commit -m "Sync before cloud deployment - $(date +%Y-%m-%d)"
git push origin main

# 如果是首次推送
git init
git remote add origin https://gitee.com/liming20090607/eims2026.git
git add .
git commit -m "Initial commit - EIMS2026"
git push -u origin main
```

#### 6.3 验证推送成功

访问 Gitee 仓库页面：https://gitee.com/liming20090607/eims2026
确认最新代码已上传。

#### 步骤 7：上传 MySQL 密码设置脚本

```bash
scp setup_mysql_root_password.sh root@39.106.41.239:/root/
```

#### 步骤 8：执行自动同步脚本

```bash
bash sync_to_cloud.sh
```

**脚本将自动完成**：
1. ✅ 确认 MySQL 密码已设置
2. ✅ 备份服务器 MySQL 数据库
3. ✅ 从 Gitee 拉取最新代码
4. ✅ 导出本地 SQLite 数据
5. ✅ 传输数据到服务器
6. ✅ 导入数据到 MySQL
7. ✅ 同步媒体文件
8. ✅ 收集静态文件
9. ✅ 执行数据库迁移
10. ✅ 重启 Gunicorn 和 Nginx
11. ✅ 清理临时文件

---

## 📝 手动同步步骤（备选方案）

如果自动脚本遇到问题，可以手动执行：

### 手动步骤 1：备份服务器数据

```bash
ssh root@39.106.41.239
cd /var/www/eims
source venv/bin/activate

# 备份 MySQL
MYSQL_PWD=root123 mysqldump -u root eims > backup_before_sync.sql
```

### 手动步骤 2：拉取代码（从 Gitee）

```bash
ssh root@39.106.41.239
cd /var/www/eims

# 如果已有 Git 仓库
git pull origin main

# 如果首次部署，需要克隆仓库
# git clone https://gitee.com/liming20090607/eims2026.git .

# 如果 Git 提示需要认证，输入 Gitee 账号密码
# 用户名：liming20090607
# 密码：您的 Gitee 登录密码
```

### 手动步骤 3：导出本地数据

在本地执行：

```bash
python manage.py dumpdata --natural-foreign --natural-primary --indent=2 > local_data.json
```

### 手动步骤 4：传输数据

```bash
scp local_data.json root@39.106.41.239:/var/www/eims/
```

### 手动步骤 5：导入数据到 MySQL

```bash
ssh root@39.106.41.239
cd /var/www/eims
source venv/bin/activate

# 导入数据
python manage.py loaddata local_data.json
```

### 手动步骤 6：更新配置

```bash
# 收集静态文件
python manage.py collectstatic --noinput

# 执行迁移
python manage.py migrate --noinput
```

### 手动步骤 7：重启服务

```bash
# 重启 Gunicorn
supervisorctl restart eims

# 重启 Nginx
systemctl restart nginx
```

### 手动步骤 8：清理临时文件

```bash
rm -f local_data.json
ssh root@39.106.41.239 "rm -f /var/www/eims/local_data.json"
```

---

## ✅ 验证同步结果

### 验证 1：访问网站

在浏览器中访问：
- HTTP: http://39.106.41.239
- HTTPS: https://39.106.41.239（如果已配置 SSL）

### 验证 2：检查服务状态

```bash
ssh root@39.106.41.239

# 检查 Gunicorn
supervisorctl status eims

# 检查 Nginx
systemctl status nginx

# 检查 MySQL
systemctl status mysqld
```

### 验证 3：检查数据库

```bash
ssh root@39.106.41.239
mysql -u root -proot123 eims

# 查看表
SHOW TABLES;

# 查看用户数量
SELECT COUNT(*) FROM auth_user;

# 退出
exit;
```

### 验证 4：检查日志

```bash
# Gunicorn 日志
tail -f /var/log/eims/error.log

# Nginx 日志
tail -f /var/log/nginx/error.log

# Django 日志
tail -f /var/www/eims/logs/django_error.log
```

---

## 🔄 回滚方案

如果同步后出现问题，可以回滚：

### 回滚 MySQL 数据

```bash
ssh root@39.106.41.239
cd /var/www/eims

# 恢复数据库备份
MYSQL_PWD=root123 mysql -u root eims < backup_before_sync.sql
```

### 回滚代码

```bash
cd /var/www/eims
git log --oneline -10  # 查看提交历史
git reset --hard <previous_commit_hash>

# 重启服务
supervisorctl restart eims
systemctl restart nginx
```

---

## 🛠️ 常见问题排查

### 问题 1：MySQL 连接失败

**症状**：Django 报错 "Can't connect to MySQL server"

**解决**：

```bash
# 检查 MySQL 是否运行
systemctl status mysqld

# 检查端口
netstat -tuln | grep 3306

# 测试连接
mysql -u root -proot123 -e "SELECT 1;"
```

### 问题 2：权限错误

**症状**：导入数据时提示权限不足

**解决**：

```bash
mysql -u root -proot123
GRANT ALL PRIVILEGES ON eims.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
exit;
```

### 问题 3：静态文件 404

**症状**：CSS/JS 文件无法加载

**解决**：

```bash
cd /var/www/eims
source venv/bin/activate
python manage.py collectstatic --noinput
systemctl restart nginx
```

### 问题 4：Gitee 推送/拉取失败

**症状**：提示认证失败或权限错误

**解决**：

```bash
# 方法 1：检查远程仓库地址
git remote -v

# 如果地址不对，重新设置
git remote set-url origin https://gitee.com/liming20090607/eims2026.git

# 方法 2：清除缓存的凭据
rm ~/.git-credentials
git config --global --unset credential.helper

# 方法 3：使用 Access Token
git remote set-url origin https://<您的Token>@gitee.com/liming20090607/eims2026.git

# 方法 4：配置 SSH Key（推荐长期使用）
# 1. 生成 SSH Key
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 3. 在 Gitee 添加 SSH Key：
#    设置 → SSH 公钥 → 添加公钥

# 4. 修改远程仓库为 SSH 地址
git remote set-url origin git@gitee.com:liming20090607/eims2026.git
```

### 问题 5：Gunicorn 启动失败

**症状**：supervisorctl status eims 显示 FATAL

**解决**：

```bash
# 查看详细错误
cat /var/log/eims/error.log

# 测试 Django
cd /var/www/eims
source venv/bin/activate
python manage.py check --deploy

# 手动启动 Gunicorn 测试
gunicorn --bind 0.0.0.0:8000 EIMS2026.wsgi:application
```

---

## 📞 下一步

同步完成后：

1. ✅ 测试所有功能模块
2. ✅ 验证用户登录
3. ✅ 检查数据完整性
4. ✅ 配置 HTTPS（如果需要）
5. ✅ 设置自动备份脚本
6. ✅ 配置域名解析（如果有域名）

---

## 📚 相关文件

- `setup_mysql_root_password.sh` - MySQL 密码设置脚本
- `sync_to_cloud.sh` - 自动同步脚本
- `settings_production.py` - 生产环境配置
- `deploy.sh` - 部署脚本（备用）

---

## ⚠️ 安全提示

**当前配置仅用于开发/测试**：

- MySQL 密码 `root123` 很简单，生产环境应使用强密码
- Django DEBUG=False 已设置，但需要配置正确的 ALLOWED_HOSTS
- 建议尽快配置 HTTPS
- 定期备份数据库

**生产环境建议**：

```bash
# 创建专用数据库用户
mysql -u root -proot123
CREATE USER 'eims_user'@'localhost' IDENTIFIED BY 'StrongPassword123!';
GRANT ALL PRIVILEGES ON eims.* TO 'eims_user'@'localhost';
FLUSH PRIVILEGES;
exit;

# 更新 Django 配置
# DATABASES = {
#     'default': {
#         'USER': 'eims_user',
#         'PASSWORD': 'StrongPassword123!',
#         ...
#     }
# }
```

---

**最后更新**: 2026-03-21
**版本**: 1.0
